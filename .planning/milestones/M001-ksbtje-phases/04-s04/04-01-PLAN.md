---
phase: "04"
plan: "01"
type: "implementation"
---

<objective>
electron-builder 설정 완성 + 아이콘 생성 + 빌드 스크립트 추가
</objective>

<context>
Phase: 04 (s04) — Milestone: M001-ksbtje

electron-builder의 플랫폼별 빌드 타겟(mac: dmg, win: nsis)을 package.json build 섹션에 추가하고, 기본 아이콘을 생성하며, dist 스크립트를 등록한다. Vite renderer 빌드 + tsc main 빌드 후 electron-builder가 패키징하는 파이프라인을 완성한다.
</context>

<must_haves>

- mac.target = 'dmg', win.target = 'nsis' 설정
- build/icon.png 256x256 아이콘 파일 존재
- dist, dist:mac, dist:win 스크립트 등록
- npm run build exit 0

</must_haves>
