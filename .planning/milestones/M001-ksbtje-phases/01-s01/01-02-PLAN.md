---
phase: "01"
plan: "02"
type: "implementation"
---

<objective>
AuthService 구현 — BrowserWindow 로그인 + 쿠키 추출 + 토큰 검증
</objective>

<context>
Phase: 01 (s01) — Milestone: M001-ksbtje

Electron BrowserWindow로 account.weverse.io 로그인 페이지를 열고, 로그인 완료 후 session.cookies.get()으로 .weverse.io 도메인의 we2_access_token 쿠키를 추출하는 AuthService를 구현한다. 추출된 JWT로 GET /api/fan-api/v1/fans/me를 호출하여 유효성을 검증한다.
</context>

<must_haves>

- BrowserWindow partition으로 쿠키 격리
- session.cookies.get()으로 we2_access_token 추출
- GET /fans/me 호출로 토큰 유효성 검증
- 401 시 token-expired 이벤트 + 재로그인 안내
- JWT 토큰 로그 마스킹
- nodeIntegration: false + contextBridge를 통한 안전한 IPC

</must_haves>
