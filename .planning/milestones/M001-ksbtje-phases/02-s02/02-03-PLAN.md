---
phase: "02"
plan: "03"
type: "implementation"
---

<objective>
신청 오케스트레이터 + 안전 가드 + IPC 배선
</objective>

<context>
Phase: 02 (s02) — Milestone: M001-ksbtje

T01(API 클라이언트+페이로드 빌더)과 T02(타이밍 서비스)를 조합하여 전체 신청 흐름을 오케스트레이션하는 ApplyEngine을 구현한다. IPC 채널을 등록하여 renderer에서 신청 흐름을 제어할 수 있게 하고, preload.ts에 API를 추가한다.
</context>

<must_haves>

- POST 1회만 발사 (postFired 플래그로 재호출 차단)
- 시간 가드: startAt - 50ms 이전 POST 차단
- consentIds는 사용자 명시 전달값만 사용 (자동 채움 금지)
- 폴링: 0.3초 간격, 최대 15초, COMPLETED 외 상태는 에러
- IPC 채널 등록/해제 패턴이 S01과 일관
- apply:event 채널로 모든 단계 이벤트를 renderer에 전달

</must_haves>
