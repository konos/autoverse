---
phase: "03"
plan: "01"
type: "implementation"
---

<objective>
LogService 구현 + 마스킹 확장 + 단위 테스트
</objective>

<context>
Phase: 03 (s03) — Milestone: M001-ksbtje

main 프로세스에서 사용할 LogService 클래스를 구현한다. EventEmitter 기반으로 로그 엔트리를 emit하며, 일별 로그 파일(app.getPath('userData')/logs/YYYY-MM-DD.log)에 append한다. mask.ts에 maskMembershipNumber, maskName 함수를 추가하고, maskSensitive(text) 래퍼 함수로 R010 대상 7종을 일괄 마스킹한다.
</context>

<must_haves>

- LogEntry 타입이 level, timestamp, source, message 필드 포함
- maskSensitive()가 R010 대상 7종 전부 마스킹
- 로그 파일이 일별로 분리
- LogService가 'log-entry' 이벤트를 emit

</must_haves>
