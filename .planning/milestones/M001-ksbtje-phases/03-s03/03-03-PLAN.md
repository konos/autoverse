---
phase: "03"
plan: "03"
type: "implementation"
---

<objective>
LogPanel UI 컴포넌트 + App.tsx 통합
</objective>

<context>
Phase: 03 (s03) — Milestone: M001-ksbtje

renderer에 LogPanel React 컴포넌트를 구현한다. window.api.log.onEntry()로 실시간 로그를 수신하고 스크롤 가능한 리스트로 표시한다. error 레벨은 빨간색 강조. 하단에 로그 다운로드 버튼을 배치하여 window.api.log.download() 호출. App.tsx에서 LogPanel을 모든 step에서 항상 표시(접을 수 있는 패널).
</context>

<must_haves>

- LogPanel이 모든 AppStep에서 항상 표시
- error 로그가 빨간색으로 강조
- 로그 다운로드 버튼이 동작
- 로그 버퍼 500개 제한
- 자동 스크롤

</must_haves>
