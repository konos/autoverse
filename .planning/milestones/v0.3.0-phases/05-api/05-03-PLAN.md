---
phase: 05-api
plan: 03
type: execute
wave: 2
depends_on: ["05-01", "05-02"]
files_modified:
  - .planning/phases/05-api/05-SPIKE-RESULT.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [R019]
security_asvs_level: 1
security_block_on: high

user_setup:
  - service: weverse-account
    why: "R019 의 결정적 신호는 실계정 로그인 1회 관찰로만 얻을 수 있다. reCAPTCHA 는 실제 로그인 페이지가 사람을 위해 처리하며, 프로그램적 우회는 R013 으로 영구 제외이므로 무인 자동화 경로가 존재하지 않는다 (D-06)"
    dashboard_config:
      - task: "본인 소유 Weverse 계정의 이메일/비밀번호를 앱 로그인 화면에 직접 입력한다. 자격증명을 Claude 에게 전달하지 않는다"
        location: "Autoverse 앱 → 로그인 패널 (브라우저 모드, 기본값)"
      - task: "저장된 자격증명이 남아 있다면(앱 시작 시 다른 계정으로 자동 로그인이 시도될 수 있음) 스파이크 전에 삭제 여부를 결정한다"
        location: "~/Library/Application Support/weverse-fanevent-apply/credentials.enc"

estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "실계정 로그인 1회 수행 후 05-SPIKE-RESULT.md 가 존재하며 verdict(PASS/FAIL), tokenSource(cookie/cdp/none), ladderSource(direct/exchange/none), fanId 확보 여부, rung1·rung2 각각의 관측 결과를 담는다"
    - "사다리가 두 rung 모두 실패해도 재시도 없이 결과가 문서화되고 phase 가 halt 된다 — 실계정 로그인 재실행 0회 (R019 반증은 flaky 테스트가 아니라 결론이다)"
    - "REQUIREMENTS.md 의 R019 Validation 상태가 관측 결과로 갱신된다 (PASS 면 validated + 근거, FAIL 이면 blocked + 사유)"
    - "05-SPIKE-RESULT.md 어디에도 실제 이메일 주소·비밀번호·토큰 원문이 없다"
    - "에이전트가 직접 실행한 실서버 프로브가 있었다면 그 모든 요청의 이메일 값이 사용자 식별자와 무관한 더미(*@example.com)였음이 문서에 기록된다 — 프로브가 0건이었다면 0건이라고 기록된다"
    - "쿠키 후보 미발견과 CDP 미관측이 서로 구분되어 기록된다 (판정 불가 사유가 '원인 미상' 으로 뭉개지지 않는다)"
  artifacts:
    - path: ".planning/phases/05-api/05-SPIKE-RESULT.md"
      provides: "R019 사다리 실계정 판정 기록 — 이 phase 의 최종 산출물"
      contains: "verdict"
    - path: ".planning/REQUIREMENTS.md"
      provides: "관측 결과가 반영된 R019 상태"
      contains: "R019"
  key_links:
    - from: ".planning/phases/05-api/05-SPIKE-RESULT.md"
      to: "~/Library/Application Support/weverse-fanevent-apply/logs/"
      via: "판정 근거가 되는 로그 라인(accountTokenDiscovery / accountTokenCapture / accountTokenLadderSpike)을 인용"
      pattern: "accountTokenLadderSpike"
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/05-api/05-SPIKE-RESULT.md"
      via: "R019 Validation 칸이 판정 문서 경로를 근거로 인용"
      pattern: "05-SPIKE-RESULT"
---

<objective>
05-01 이 배선한 계정 토큰 확보 경로와 `acquireFaneventToken()` 사다리를 **실계정으로 1회 관찰해
R019 를 판정하고, 그 결과를 문서로 확정한다** (D-03, D-06).

Purpose: R019 는 이 마일스톤의 최대 미검증 리스크다. 05-01(halted) 에서는 로그인 자체가 막혀 사다리에 도달조차
못 했다 — 검증도 반증도 아닌 완전 미도달 상태였다. 이 플랜은 그 상태를 끝낸다.

**이 판정은 반복 가능한 자동 테스트가 아니라 1회성 사람 관찰이다** (05-VALIDATION.md 의 phase 고유 Nyquist 제약).
로그인은 사용자가 직접 수행하고(D-06), 실행자는 배선 확인·환경 준비·로그 판독만 한다.
자격증명(이메일/비밀번호)을 실행자가 다루지 않는다.

D-04 에 따라 이 phase 는 스파이크다 — 판정 결과로 API 모드 제품 경로를 확정하는 결정은 이 phase 밖에서 별도로 내린다.
사다리를 "통과시키기 위해" 코드를 고치는 행위는 이 플랜의 범위가 아니며 스파이크 정신에 반한다.

Output: `.planning/phases/05-api/05-SPIKE-RESULT.md` + REQUIREMENTS.md R019 상태 갱신.

**⚠ 절대 하지 말 것:**
- 실패 시 재시도. 두 rung 이 모두 실패하면 그것이 R019 반증이다 — 문서화하고 halt 한다 (05-01 의 halt 가 선례).
- 사용자의 실제 이메일 주소를 에이전트가 어떤 명령·스크립트·HTTP 요청에도 사용하는 것 (05-01 Process Issue 재발 방지).
- reCAPTCHA 토큰의 생성·해결·주입 (R013 영구 제외).
- 사용자에게 자격증명을 알려달라고 요청하는 것. `credentials.enc` 를 읽거나 복호화하려 시도하는 것.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-api/05-01-SUMMARY.md
@.planning/phases/05-api/05-CONTEXT.md
@.planning/phases/05-api/05-VALIDATION.md
@.planning/phases/05-api/05-RESEARCH.md
@.planning/phases/05-api/05-01-PLAN.md

`05-VALIDATION.md` 의 "Manual-Only Verifications" 표와 "이 phase 고유의 Nyquist 제약" 절이 이 플랜의 판정 절차 원본이다.
`05-RESEARCH.md` 의 Pitfall 4(교환 실패 시 departure 값 의심)와 Open Questions 3개가 로그 판독 시 확인할 항목이다.
</context>

## Artifacts this phase produces

이 플랜이 만드는 것은 판정 문서 하나뿐이다 (D-04 — 스파이크 산출물은 문서와 최소 배선).

| 산출물 | 종류 | 담당 |
|---|---|---|
| `.planning/phases/05-api/05-SPIKE-RESULT.md` | 신규 문서 — R019 실계정 판정 기록 | Task 3 |
| `.planning/REQUIREMENTS.md` R019 Validation 칸 갱신 | 문서 섹션 | Task 3 |

신규 코드 심볼은 이 플랜에서 만들지 않는다 — 전체 목록은 `05-01-PLAN.md` 의 동일 섹션에 있다.

<tasks>

<task type="auto">
  <name>Task 1: 체크포인트 환경 준비 + 에이전트 안전 프로토콜 자체 점검</name>
  <read_first>
    - `src/main/services/auth-service.ts` — 05-01 이 추가한 `runAccountTokenLadderSpike()` / `attachAccountTokenCapture()` 가 실제로 커밋되어 있는지, `credentialLogin()` 성공 분기에서 스파이크가 시작되는지 확인
    - `src/main/services/log-service.ts` — 로그 파일 경로 규칙(`app.getPath("userData")/logs/YYYY-MM-DD.log`, JSON Lines)과 `maskSensitive` 적용 지점
    - `src/renderer/components/LoginPanel.tsx` — 로그인/로그아웃 버튼의 노출 조건 (로그인 실패 상태에서는 "로그아웃 + 자격 증명 삭제" 버튼이 보이지 않는다는 기존 갭)
    - `.planning/phases/05-api/05-01-SUMMARY.md` — "Process Issue — 실행자의 사용자 이메일 오용" 섹션 전문
  </read_first>
  <files>(코드 변경 없음 — 준비/점검 태스크. 산출물은 실행 로그와 체크포인트에 전달할 사실들)</files>
  <action>
**(A) 에이전트 안전 프로토콜 자체 점검 — 다른 어떤 작업보다 먼저 수행하고 결과를 실행 로그에 남긴다.**
아래 5개 항목을 하나씩 확인하고, 각 항목에 대해 "확인함/해당없음" 을 명시적으로 기록한다. 하나라도 어긋나면
그 시점에서 멈추고 사용자에게 보고한다.
1. 이 태스크와 Task 2·3 을 수행하는 동안 **에이전트가 직접 실행하는 어떤 명령·스크립트·HTTP 요청에도 사용자의
   실제 이메일 주소를 넣지 않는다.** 실서버 프로브가 정말로 필요해지면 `*@example.com` 형태의, 사용자 식별자와
   무관한 더미만 사용한다 (D-06 비협상 기준선). 05-01 에서 실행자가 사용자의 실제 이메일로 계정 API 를 직접
   호출한 사고가 있었고, 이 항목은 그 재발 방지 장치다.
2. 사용자에게 이메일/비밀번호를 알려달라고 요청하지 않는다. 자격증명은 사용자가 앱 UI 에 직접 입력한다.
3. `~/Library/Application Support/weverse-fanevent-apply/credentials.enc` 를 읽거나 복호화하려 시도하지 않는다.
   존재 여부(`test -f`)와 수정 시각(`ls -l`)만 확인한다 — 내용은 확인하지 않는다.
4. 사람이 앱 UI 로 수행하는 실계정 로그인에는 더미 이메일 규칙을 적용하지 않는다. 그 로그인은 당연히 실계정으로
   이루어져야 하며, 여기에 검증 로직을 끼워 넣으면 스파이크가 성립하지 않는다 (05-RESEARCH Pitfall 5).
5. 로그인을 대신 자동화하려 시도하지 않는다 — 캡차는 사람을 위해 실제 로그인 페이지가 처리한다 (R013).

**(B) 빌드/배선 확인.** `npm run build` 와 `npm test` 를 실행해 05-01 의 배선이 실제 빌드에 반영됐는지 확인한다.
`grep -n 'runAccountTokenLadderSpike' src/main/services/auth-service.ts` 로 `credentialLogin()` 성공 분기의 호출부가
존재하는지 확인하고 줄 번호를 기록한다.

**(C) 로그 준비.** 앱 로그 파일의 정확한 경로를 계산해 기록한다
(`~/Library/Application Support/weverse-fanevent-apply/logs/<오늘 날짜 YYYY-MM-DD>.log`). 파일이 이미 있으면
현재 줄 수를 "시작 마커" 로 기록한다 (`wc -l`) — Task 3 이 이 지점 이후 줄만 판독하면 과거 실행의 로그와 섞이지 않는다.
파일이 없으면 "0줄, 앱 첫 실행 시 생성됨" 으로 기록한다.

**(D) 저장 자격증명 상태 고지 준비.** `credentials.enc` 의 존재 여부와 수정 시각을 확인해 기록한다.
존재한다면 **앱 시작 시 저장된(어쩌면 다른) 계정으로 자동 로그인이 시도되어 그 계정에 로그인 알림 메일이 발송될 수
있다** — 05-01 에서 실제로 발생한 피해다. CONTEXT.md D-06 은 "스파이크 전 credentials.enc 선삭제" 를 사용자가
선택하지 않은 안전장치로 기록하고 있으므로 **에이전트가 임의로 삭제하지 않는다.** 대신 이 사실을 Task 2 체크포인트
지시문에 그대로 실어 사용자가 판단하게 한다.

**(E) 앱 기동.** `AUTOVERSE_LOGIN_MODE` 환경변수를 설정하지 않은 상태(= 브라우저 모드, 기본값)로 앱을 백그라운드에서
기동한다. D-02 의 토큰 확보 경로는 헤드리스 `credentialLogin()` 이며 이는 브라우저 모드 경로다 — API 모드로 켜면
`tryAutoLogin()` 게이트가 걸려 스파이크가 돌지 않는다. 에이전트 셸에서 GUI 앱 기동이 실패하면 그 사실을 기록하고
Task 2 지시문에 `npm start` 를 사용자가 직접 실행하도록 안내한다.
  </action>
  <verify>
    <automated>npm run build && npx vitest run src/main/services/__tests__/auth-service.test.ts -t "runAccountTokenLadderSpike"</automated>
  </verify>
  <acceptance_criteria>
    - (A) 5개 항목 각각에 대한 "확인함/해당없음" 판정이 실행 로그에 기록되어 있다
    - 이 태스크 수행 중 에이전트가 실행한 명령 이력에 `@gmail.com` 등 실계정 도메인 이메일이 인자로 들어간 명령이 0건이다
    - `npm run build` 가 통과하고 `npm test` 가 green 이다
    - `grep -c 'runAccountTokenLadderSpike' src/main/services/auth-service.ts` 가 2 이상이다 (정의 + `credentialLogin()` 호출부)
    - 오늘 날짜의 로그 파일 경로와 시작 마커 줄 수가 기록되어 있다
    - `credentials.enc` 의 존재 여부와 수정 시각이 기록되어 있고, 파일 내용을 읽거나 복호화한 흔적이 없다
    - 앱 기동 시도 결과(성공/실패, 실패 시 사유)가 기록되어 있다
  </acceptance_criteria>
  <done>사용자가 로그인만 수행하면 되는 상태가 갖춰지고, 에이전트 측 안전 프로토콜 점검 결과와 판정에 필요한 로그 시작점이 확보된다.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: R019 핵심 판정 — 사용자가 실계정 로그인 1회 수행 (재시도 없음)</name>
  <precondition>user_setup 항목 1 완료 — 사용자가 본인 소유 Weverse 계정 자격증명을 보유하고 있고, 이번 스파이크에서 그 계정으로 1회 로그인할 의사를 밝혔다</precondition>
  <read_first>
    - Task 1 이 기록한 결과 4가지: 안전 프로토콜 5항목 판정, 로그 파일 경로와 시작 마커 줄 수, `credentials.enc` 존재 여부/수정 시각, 앱 기동 성공 여부. 이 값들을 아래 `<what-built>` 의 자리표시자에 실제 값으로 채워 사용자에게 제시한다
    - `.planning/phases/05-api/05-VALIDATION.md` — "이 phase 고유의 Nyquist 제약" 절과 "Manual-Only Verifications" 표 (재시도 금지의 근거)
    - `.planning/phases/05-api/05-CONTEXT.md` — D-06 전문 (실계정 로그인은 사용자가 수행, 에이전트 프로브는 더미 이메일만)
  </read_first>
  <what-built>
05-01 이 배선한 계정 토큰 확보 경로와 R019 사다리가 빌드에 반영되어 있고, 앱이 브라우저 모드로 준비되어 있다.
로그인이 성공하면 앱이 자동으로 1회 (1) `persist:weverse` 파티션의 전체 쿠키를 열거해 계정 토큰 후보를 찾고,
(2) 못 찾으면 CDP 가 `by-credentials` 200 응답에서 캡처한 `accessToken` 을 쓰고,
(3) 확보한 토큰을 `acquireFaneventToken()` 사다리(rung1 직접 사용 → 실패 시 rung2 교환)에 태우고,
(4) 결과를 로그 파일 한 줄로 남긴다.

이 스파이크는 **읽기 전용**이다 — `authService.token` 을 덮어쓰지 않으므로 기존 로그인/신청 동작에 영향을 주지 않는다.

**⚠ 저장된 자격증명 상태:** Task 1 이 확인한 `credentials.enc` 존재 여부/수정 시각을 여기에 그대로 옮겨 적을 것.
파일이 존재하면 앱 시작 시 **저장된(어쩌면 다른) 계정으로 자동 로그인이 시도되어 그 계정에 로그인 알림 메일이
발송될 수 있다** — 05-01 에서 실제로 발생한 피해다. 삭제 여부는 사용자가 결정한다 (에이전트가 임의로 지우지 않음).
지우고 싶다면 앱 UI 의 "로그아웃 + 자격 증명 삭제" 버튼(로그인 상태에서만 노출) 또는 위 파일 직접 삭제.
  </what-built>
  <how-to-verify>
**로그인은 사용자가 직접 수행합니다. Claude 는 이메일/비밀번호를 다루지 않습니다 (D-06).**

1. 앱이 떠 있는지 확인합니다 (Task 1 이 백그라운드로 기동했습니다. 안 떠 있으면 터미널에서 `npm start`).
2. 로그인 상태라면 먼저 로그아웃합니다 — 쿠키에 유효 토큰이 남아 있으면 자격증명 로그인 경로가 실행되지 않아
   스파이크가 돌지 않습니다.
3. 로그인 패널에 **본인 계정의** 이메일/비밀번호를 직접 입력하고 로그인합니다. (이 값을 채팅에 붙여넣지 마세요.)
4. 로그인 완료 후 **최대 약 40초** 기다립니다. 사다리는 최대 3회 HTTP 왕복(각 10초 타임아웃)을 합니다.
5. 앱 로그 패널 또는 로그 파일에 `accountTokenLadderSpike: verdict=` 로 시작하는 줄이 나타나면 관측 종료입니다.
6. **"완료" 라고만 알려주시면 됩니다.** 로그를 붙여넣을 필요 없습니다 — Claude 가 로그 파일을 직접 읽어 판독합니다.
   (로그는 마스킹을 거치지만, 그래도 화면 캡처나 로그 붙여넣기는 권하지 않습니다.)

**Claude 가 로그에서 읽어낼 것 (사용자가 확인할 필요는 없습니다):**
- `accountTokenDiscovery: partition=persist:weverse cookies=<N>` — 열거된 쿠키 수와 인벤토리
- `accountTokenDiscovery: candidate=<이름>@<도메인>` 또는 `candidate=none` — 계정 토큰 쿠키의 실제 이름 (Open Question 1 의 답)
- `accountTokenCapture(CDP): attach ok|failed=` / `responseSeen` / `detached reason=` — CDP 폴백이 실제로 동작했는지
- `acquireFaneventToken rung1 ...` / `rung2 ...` / `exchangeForService response keys=[...]` — 사다리 각 단 결과와
  `by-access-token` 응답 스키마 (Open Question 2 의 답)
- `accountTokenLadderSpike: verdict=<pass|fail> tokenSource=<..> ladderSource=<direct|exchange|none> fanId=<..>`

**판정 기준:**
- `verdict=pass` 이고 `fanId` 가 있으면 **R019 검증 성공**. 어느 rung 이 통했는지(`direct` / `exchange`)를 반드시 기록합니다.
- `verdict=fail` 이면 **R019 반증**입니다. 이것은 flaky 테스트가 아니라 결론입니다.

**⚠ 실패 시 실행자 지침 (halt 프로토콜):**
두 rung 이 모두 실패하면 **재시도하지 마세요.** 로그인을 다시 시도하게 하는 것은 사용자 계정에 로그인 알림 메일을
추가로 발생시키는 부작용이 있고, R019 의 결정적 신호는 애초에 반복 관찰로 얻는 것이 아닙니다. 관측된 사실을
Task 3 에서 그대로 문서화하고 phase 를 halt 합니다 (05-01 의 halt 가 이 프로젝트의 선례).
"통과시키기 위해" `X-ACC-SERVICE-ID`(departure) 값을 바꿔가며 재시도하는 것도 금지입니다 — 그런 후보는
"다음 단계 제안" 으로 문서에 적되 이 phase 에서 실행하지 않습니다 (05-RESEARCH Pitfall 4).
  </how-to-verify>
  <resume-signal>"완료" 라고 입력하세요 (성공/실패 여부는 Claude 가 로그로 판독합니다). 로그인을 수행하지 않기로 했다면 "중단" 이라고 입력하세요 — 그 경우에도 Task 3 이 "미수행" 사유를 기록합니다.</resume-signal>
  <acceptance_criteria>
    - 체크포인트 제시문에 Task 1 이 확인한 실제 값(로그 파일 경로, 시작 마커 줄 수, `credentials.enc` 존재 여부와 수정 시각, 앱 기동 결과)이 자리표시자가 아니라 구체값으로 들어가 있다
    - 사용자로부터 "완료" 또는 "중단" 응답을 받은 뒤에만 Task 3 으로 진행한다 (auto-mode 자동 승인 없음 — `gate="blocking-human"`)
    - 이 태스크 수행 중 에이전트가 사용자에게 이메일/비밀번호를 묻지 않았다
    - 이 태스크 수행 중 에이전트가 실서버로 로그인 요청을 보내지 않았다 (로그인 주체는 사람)
    - 실계정 로그인이 총 1회만 수행됐다 — 실패했더라도 "다시 해보세요" 를 요청하지 않았다
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 로그 판독 → 05-SPIKE-RESULT.md 확정 + R019 상태 기록 (성공·실패 공통 경로)</name>
  <read_first>
    - Task 1 이 기록한 로그 파일 경로와 시작 마커 줄 수
    - `.planning/REQUIREMENTS.md` — `### R019` 항목과 traceability 표의 R019 행 (05-02 가 이미 R017/R018 을 정정한 상태)
    - `.planning/phases/05-api/05-RESEARCH.md` — "Open Questions" 3개, "Assumptions Log" A1~A5 (판독으로 답이 나온 항목을 표시하기 위함)
    - `.planning/phases/05-api/05-01-SUMMARY.md` — halt 기록의 서술 형식 (반증을 기록하는 이 프로젝트의 선례)
  </read_first>
  <files>.planning/phases/05-api/05-SPIKE-RESULT.md, .planning/REQUIREMENTS.md</files>
  <action>
Task 1 이 기록한 시작 마커 이후의 로그 줄만 읽는다 (`tail -n +<마커+1> <로그파일>`) — 과거 실행 로그와 섞지 않는다.
JSON Lines 이므로 `message` 필드를 기준으로 위 앵커 문자열들을 찾는다.

`.planning/phases/05-api/05-SPIKE-RESULT.md` 를 새로 만든다. **성공이든 실패든 이 문서는 반드시 작성한다** —
실패는 산출물이 없는 상태가 아니라 "R019 가 반증됐다" 는 산출물이다. 아래 섹션을 모두 채운다.

1. **판정 (Verdict).** `PASS` / `FAIL` / `NOT-RUN`(사용자가 중단 선택) 중 하나와 한 문장 요약.
2. **관측 사실.** 쿠키 열거 결과(개수, 계정 토큰 후보의 이름·도메인·길이 또는 "후보 없음"),
   CDP 경로 결과(attach 성공 여부, `by-credentials` 응답 관측 여부, 캡처 성공 여부, detach 사유),
   확보한 토큰의 shape(`parts`/`len`/`looksLikeJwt`), `matchesWe2Cookie` 값,
   rung1 결과, rung2 실행 여부와 결과, `exchangeForService` 응답 키 목록, 최종 `fanId` 유무.
   **토큰 값·쿠키 값·응답 바디 전문은 옮겨 적지 않는다** (길이와 형태만).
3. **미지수 해소 현황.** `05-RESEARCH.md` 의 Open Questions 3개와 Assumptions Log A1~A5 각각에 대해
   이번 관측으로 "확인됨 / 반증됨 / 여전히 미확인" 을 표로 기록한다. 특히 A1(CDP `loadingFinished` 타이밍),
   A2(계정 토큰 쿠키 존재/이름), A3(`by-access-token` 응답 스키마), A4(departure 값), A5(토큰의 JWT 여부).
4. **로드맵 Success Criteria 3 에 대한 판단.** 확보한 토큰이 `/fans/me` 200 을 받았다면 `ApplyEngine` 이
   `authService.token` 뒤에서 그대로 소비할 수 있는 형태인지, `matchesWe2Cookie` 결과가 무엇을 시사하는지 서술한다.
   `npx vitest run src/main/services/__tests__/apply-engine.test.ts` 결과(회귀 없음)를 함께 기록한다.
5. **에이전트 안전 프로토콜 기록.** Task 1 (A) 의 5개 항목 판정 결과를 그대로 옮기고, 이 phase 에서 에이전트가
   직접 실행한 실서버 프로브의 건수를 명시한다 — 0건이면 "0건" 이라고 쓴다. 1건 이상이면 각각에 사용한 이메일 값이
   `*@example.com` 더미였음을 기록한다.
6. **다음 단계 (실행하지 않음).** rung2 가 실패했다면 `X-ACC-SERVICE-ID`(departure) 대체 후보 등 다음에 시도할
   가설을 **관찰된 사실과 구분해** 나열한다. 이 phase 에서 실행하지 않는다는 것을 명시한다.
   API 모드 제품 경로 확정은 D-04 에 따라 이 phase 밖의 결정임을 적는다.
7. **FAIL 인 경우 halt 절차.** 재시도하지 않았다는 사실, 무엇이 R019 를 반증하는지, Phase 06/07 이 이 결과로
   어떤 전제를 다시 검토해야 하는지(CONTEXT.md Deferred Ideas 의 "Phase 06/07 영향 정리" 항목)를 적는다.

이어서 `.planning/REQUIREMENTS.md` 를 `Edit` 으로만 수정한다 (`Write` 금지). `### R019` 항목의 `Validation` 줄과
traceability 표의 R019 행을 관측 결과로 갱신한다 — PASS 면 `validated` 로 올리고 근거로
`.planning/phases/05-api/05-SPIKE-RESULT.md` 경로와 어느 rung 이 통했는지를 적는다. FAIL 이면 `blocked` 로 내리고
같은 근거 경로와 반증 사유를 적는다. NOT-RUN 이면 `active` 를 유지하되 Notes 에 미수행 사유를 적는다.
05-02 가 정정한 R017/R018 항목은 건드리지 않는다.

**재시도 금지 재확인:** 이 태스크는 판독과 기록만 한다. 로그에 verdict 줄이 없으면(사용자가 로그인을 하지 않았거나
스파이크가 시작되지 않은 경우) 그 사실 자체를 `NOT-RUN` 으로 기록하고 끝낸다 — 로그인을 다시 요청하지 않는다.
  </action>
  <verify>
    <automated>test -f .planning/phases/05-api/05-SPIKE-RESULT.md && grep -qE 'PASS|FAIL|NOT-RUN' .planning/phases/05-api/05-SPIKE-RESULT.md && test "$(grep -cE '[A-Za-z0-9._-]{100,}' .planning/phases/05-api/05-SPIKE-RESULT.md)" = "0" && npx vitest run src/main/services/__tests__/apply-engine.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/05-api/05-SPIKE-RESULT.md` 가 성공한다 (성공/실패/미수행 모든 경우에 존재)
    - `grep -cE 'PASS|FAIL|NOT-RUN' .planning/phases/05-api/05-SPIKE-RESULT.md` 가 1 이상이다
    - `grep -c 'rung1' .planning/phases/05-api/05-SPIKE-RESULT.md` 와 `grep -c 'rung2' .planning/phases/05-api/05-SPIKE-RESULT.md` 가 각각 1 이상이다 (두 rung 모두 결과가 기록됨 — D-03)
    - `grep -cE 'A1|A2|A3|A4|A5' .planning/phases/05-api/05-SPIKE-RESULT.md` 가 5 이상이다 (Assumptions Log 5개 항목 모두 처리)
    - `grep -cE '[A-Za-z0-9._-]{100,}' .planning/phases/05-api/05-SPIKE-RESULT.md` 가 0 이다 (토큰 원문 길이의 문자열 없음)
    - `grep -cE '[a-zA-Z0-9._%+-]+@(gmail|naver|daum|kakao|hanmail)\.' .planning/phases/05-api/05-SPIKE-RESULT.md` 가 0 이다 (실계정 이메일 없음)
    - `grep -c '프로브' .planning/phases/05-api/05-SPIKE-RESULT.md` 가 1 이상이다 (에이전트 실서버 프로브 건수 기록)
    - `grep -c '05-SPIKE-RESULT' .planning/REQUIREMENTS.md` 가 1 이상이다 (R019 상태의 근거 인용)
    - `grep -c '### R018' .planning/REQUIREMENTS.md` 가 1 이고 `grep -c '^- Status: blocked' .planning/REQUIREMENTS.md` 가 1 이상이다 (05-02 의 정정이 손상되지 않음)
    - `npx vitest run src/main/services/__tests__/apply-engine.test.ts` 가 통과한다 (Success Criteria 3 회귀 없음)
    - 실계정 로그인이 이 플랜 실행 중 총 1회만 수행됐다 (재시도 0회)
  </acceptance_criteria>
  <done>R019 의 판정이 근거와 함께 문서로 확정되고, 성공이든 반증이든 다음 실행자가 같은 관측을 반복하지 않아도 되는 상태가 된다. 실패인 경우 재시도 없이 halt 절차가 기록으로 완결된다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 사람(사용자) → 앱 로그인 폼 | 실계정 자격증명이 시스템에 들어오는 유일한 지점. 에이전트는 이 경계 반대편에 있어야 한다 |
| 에이전트 → 외부 실서버 | 에이전트가 직접 HTTP 를 쏘는 경로. 05-01 에서 여기로 사용자 실이메일이 새어 나갔다 |
| 로그 파일 → 판정 문서 → git 저장소 | 관측 재료가 영구 기록으로 승격되는 지점. 여기서 마스킹이 뚫리면 토큰이 커밋된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-12 | Information Disclosure | 에이전트가 실서버 프로브에 사용자 실이메일을 재사용 (05-01 Process Issue 선례) | high | mitigate | Task 1 (A) 의 5항목 자체 점검을 다른 작업보다 먼저 수행하고 결과를 기록. 프로브가 필요하면 `*@example.com` 더미만 사용. 체크포인트를 `gate="blocking-human"` 으로 두어 auto-mode 에서도 사람이 반드시 개입 |
| T-05-13 | Information Disclosure | 판정 문서에 토큰/쿠키 값·응답 바디 전문이 옮겨 적힘 | high | mitigate | 액션이 "길이와 형태만" 을 명시하고, 인수 기준이 100자 이상 연속 토큰 문자열 부재와 실계정 이메일 패턴 부재를 grep 으로 확인 |
| T-05-14 | Spoofing / 사용자 피해 | 앱 시작 시 저장된 다른 계정으로 자동 로그인이 트리거되어 알림 메일 발송 (05-01 결함 A 선례) | high | mitigate | Task 1 (D) 가 `credentials.enc` 상태를 확인해 체크포인트 지시문에 고지하고, 삭제 여부는 사용자가 결정. 에이전트는 파일 내용을 읽지 않고 존재/수정시각만 확인 |
| T-05-15 | Repudiation | 실패를 flaky 로 오인해 재시도하면서 계정에 반복 로그인 알림 발생 + 판정이 흐려짐 | medium | mitigate | 체크포인트 지시문과 Task 3 액션 양쪽에 재시도 금지·halt 프로토콜 명시. 인수 기준에 "실계정 로그인 총 1회, 재시도 0회" 포함 |
| T-05-16 | Elevation of Privilege | 판정 통과를 위해 캡차 우회/토큰 주입 시도 | high | mitigate | R013 영구 제외를 objective 의 금지 목록에 명시. 사다리를 통과시키기 위한 파라미터 브루트포스(departure 값 변경 재시도)도 금지하고 "다음 단계 제안" 으로만 문서화 |

**패키지 정당성 게이트: 해당 없음.** 이 플랜은 패키지 설치 태스크를 포함하지 않는다.
</threat_model>

<verification>
1. `test -f .planning/phases/05-api/05-SPIKE-RESULT.md` — 판정 문서 존재 (모든 결과 경로에서)
2. `npm test` 전체 green, `npm run typecheck:main` 0 errors
3. `npx vitest run src/main/services/__tests__/apply-engine.test.ts` — Success Criteria 3 회귀 없음
4. `grep -cE '[A-Za-z0-9._-]{100,}' .planning/phases/05-api/05-SPIKE-RESULT.md` → 0
5. `git diff --stat` 이 `.planning/` 밖 파일 변경을 보이지 않는다 (이 플랜은 코드를 바꾸지 않는다)
</verification>

<success_criteria>
- R019 가 실계정 관측 1회로 판정되고(PASS / FAIL / NOT-RUN), 근거가 문서에 남는다
- 어느 rung 이 통했는지, 혹은 왜 둘 다 실패했는지가 로그 인용과 함께 기록된다
- RESEARCH.md 의 Open Questions 3개와 Assumptions A1~A5 가 각각 "해소/반증/미확인" 으로 정리된다
- 실패의 경우 재시도 없이 halt 절차가 완결되고, Phase 06/07 이 다시 검토해야 할 전제가 명시된다
- 판정 문서와 REQUIREMENTS.md 어디에도 실계정 식별자·토큰 원문이 없다
</success_criteria>

<output>
Create `.planning/phases/05-api/05-03-SUMMARY.md` when done.
</output>
