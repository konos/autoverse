---
phase: 05-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [R017, R018]
security_asvs_level: 1
security_block_on: high

estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "REQUIREMENTS.md 의 R017 항목에 구 3단계 로그인 순서 서술이 더 이상 존재하지 않고, HAR 실측 계약(자격증명 로그인 단독 호출 + otpSessionId 필드가 reCAPTCHA Enterprise 토큰)이 그 자리를 대신한다"
    - "REQUIREMENTS.md 의 R018 Status 가 blocked 이고, 왜 보류인지의 사유와 근거 파일 경로가 항목 안에 적혀 있으며, Primary owning slice 가 v0.3.0/Phase 05 가 아니다"
    - "REQUIREMENTS.md 의 traceability 표와 Coverage Summary 숫자가 R018 상태 변경과 일치한다 (표와 본문이 서로 모순되지 않는다)"
    - "PROJECT.md 의 '검증된 API 계약' 표가 3단계 로그인 순서와 '캡차는 세션 생성 시에만 사용' 주장을 더 이상 사실로 서술하지 않고, 반증 근거 파일 경로를 인용한다"
    - "ROADMAP.md Phase 05 의 Goal 과 Success Criteria 가 반증된 이메일 OTP 전제를 이 phase 의 목표로 서술하지 않는다"
    - "정정된 어떤 문서에도 실제 사용자 이메일 주소·비밀번호·토큰 원문이 예시로 들어가지 않는다"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "R017 정정 + R018 보류(blocked) + 매핑 해제된 요구사항 계약"
      contains: "R018"
    - path: ".planning/PROJECT.md"
      provides: "반증 사실이 반영된 API 계약 표"
      contains: "05-01-SUMMARY.md"
    - path: ".planning/ROADMAP.md"
      provides: "재설계된 Phase 05 Goal / Success Criteria"
      contains: "Phase 05"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/05-api/05-01-SUMMARY.md"
      via: "R017/R018 정정 사유가 HAR 근거 문서 경로를 인용한다"
      pattern: "05-01-SUMMARY"
    - from: ".planning/PROJECT.md"
      to: ".planning/phases/05-api/05-01-SUMMARY.md"
      via: "API 계약 표의 정정 사유가 같은 근거 문서를 인용한다"
      pattern: "05-01-SUMMARY"
---

<objective>
반증된 로그인 계약을 아직 "검증됨"으로 주장하고 있는 프로젝트 문서를 실측에 맞게 정정한다 (D-05).

Purpose: CONTEXT.md D-05 의 근거 그대로 — *"문서가 반증된 계약을 계속 '검증됨'으로 주장하는 상태를 먼저 끝낸다.
다음 실행자가 같은 함정에 빠지는 것을 막는 것이 스파이크보다 우선한다."* 05-01(halted) 의 실패는 코드 실패가 아니라
문서가 틀린 계약을 사실로 단언한 결과였다. 이 플랜은 그 원인을 제거한다.

이 phase 에서 R017 은 **로그인 구현이 아니라 서술 정정**으로 처리되고, R018 은 **구현이 아니라 보류 처리**로
처리된다 (D-05). OTP 관련 코드는 이 phase 에서 한 줄도 쓰지 않는다.

D-05 는 CONTEXT.md 에서 reversible 로 기록되어 있다(문서 상태 변경) — reversibility 게이트를 삽입하지 않는다.

**Flagged assumption (edge probe, no-silent-drop):** 결정적 edge probe 가 R017 에 대해 `idempotency`
("What happens if this runs twice on the same input?") 와 `concurrency`
("If interrupted or run in parallel, what is guaranteed?") 두 엣지를 surface 했다. 이 phase 는 D-05 에 따라
R017 을 **서술 정정으로만** 처리하고 로그인 실행 코드를 한 줄도 만들지 않으므로, 두 엣지가 가정한 실행 경로가
이번 phase 범위에 **존재하지 않는다 — N/A**. 이는 침묵에 의한 누락이 아니라 명시적·이유 있는 제외다.
R017 이 실제 실행 코드를 갖게 되는 시점(스파이크 결과로 API 모드 제품 경로가 확정될 때)에 두 엣지를 같은
full-coverage 기준선에서 **다시** 결정해야 한다. 참고: R017 의 실행 경로 중 이번 phase 가 실제로 배선하는
부분(계정 토큰 확보)의 idempotency/concurrency 는 `05-01` 의 단일 비행 가드 truths 와 threat `T-05-04` 가 커버한다.

Output: 정정된 `REQUIREMENTS.md` / `PROJECT.md` / `ROADMAP.md`.

**⚠ 절대 하지 말 것:** R018 항목을 삭제하는 것. 보류(`blocked`)는 기록을 남기는 것이고 삭제는 기록을 지우는 것이다.
캡차 우회 관련 문구를 추가하는 것 (R013 영구 제외 — 문서에도 우회 방법을 적지 않는다).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-api/05-01-SUMMARY.md
@.planning/phases/05-api/05-CONTEXT.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md

정본은 `05-01-SUMMARY.md` 의 "무효화된 전제 (Invalidated Assumptions)" 섹션이다. 정정 문구는 그 섹션의 표를
근거로 작성한다 — 새 사실을 지어내지 않는다.

`.planning/phases/05-api/COVERAGE.md` 는 이미 작성·커밋되어 있다. 다시 만들지 않는다.
</context>

## Artifacts this phase produces

이 플랜은 새 코드 심볼을 만들지 않는다. 산출물은 기존 3개 문서의 정정된 내용뿐이다.

| 산출물 | 종류 | 담당 |
|---|---|---|
| `.planning/REQUIREMENTS.md` R017 항목 정정 본문 | 문서 섹션 | Task 1 |
| `.planning/REQUIREMENTS.md` R018 항목 `blocked` 전환 + 사유 + 매핑 해제 | 문서 섹션 | Task 1 |
| `.planning/REQUIREMENTS.md` traceability 표 / Coverage Summary 정합 | 문서 섹션 | Task 1 |
| `.planning/PROJECT.md` "검증된 API 계약" 표 정정 | 문서 섹션 | Task 2 |
| `.planning/ROADMAP.md` Phase 05 Goal / Success Criteria 정정 | 문서 섹션 | Task 2 |

신규 코드 심볼 목록은 `05-01-PLAN.md` 의 동일 섹션에 있다.

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md 정정 — R017 서술 교체, R018 보류 처리 (D-05)</name>
  <read_first>
    - `.planning/REQUIREMENTS.md` — 특히 `### R017`(132행 부근), `### R018`(143행 부근), `### R019`(154행 부근), traceability 표(265-285행 부근), `## Coverage Summary`(287행 부근). 기존 항목의 필드 순서(`Class` / `Status` / `Description` / `Why it matters` / `Source` / `Primary owning slice` / `Validation` / `Notes`)를 그대로 따를 것
    - `.planning/phases/05-api/05-01-SUMMARY.md` — "무효화된 전제 (Invalidated Assumptions)" 섹션의 대조표가 정정 문구의 유일한 근거
    - `.planning/phases/05-api/05-CONTEXT.md` — D-05 의 정확한 요구사항 두 줄 (R018 사유 문구가 D-05 안에 이미 초안으로 적혀 있다)
  </read_first>
  <files>.planning/REQUIREMENTS.md</files>
  <action>
<!-- planner-discipline-allow: otp-sessions -->
<!-- planner-discipline-allow: by-credentials-with-otp -->
`.planning/REQUIREMENTS.md` 를 `Edit` 로만 수정한다 (`Write` 로 전체를 다시 쓰면 이 파일의 다른 23개 요구사항 항목이
날아간다). 아래 네 지점을 각각 별도의 scoped Edit 으로 처리한다.

**(1) R017 (API 3단계 자격증명 로그인).** 제목의 "3단계" 표현부터 실측과 어긋난다 — 항목 제목을
`### R017 — API 자격증명 로그인 (실측 계약)` 으로 바꾼다. `Description` 을 HAR 실측 계약으로 교체한다:
이메일/비밀번호로 위버스 계정 API 에 로그인하는 경로는 `POST /v4/auth/token/by-credentials` **단독 호출**이며,
요청 바디의 `otpSessionId` 필드는 OTP 세션 식별자가 아니라 **reCAPTCHA Enterprise 토큰(실측 2489자)** 을 담는
자리라는 사실을 쓴다. `Notes` 에서 유효한 부분(Base URL, 5종 필수 헤더, 평문 비밀번호 TLS 전송)은 유지하되,
`otpSessionId는 필수 필드 — 누락 시 -26000` 서술은 "이 필드가 캡차 토큰이므로 순수 HTTP 클라이언트는 채울 수
없고(R013 영구 제외), 실제 로그인 페이지가 캡차를 스스로 처리하는 헤드리스 BrowserWindow 경로가 현재 유일하게
동작하는 로그인 경로다" 로 교체한다. `-25044` 의 의미를 "이메일 OTP 인증 필요" 가 아니라 **"캡차 토큰 없음/무효"** 로
정정한다. Notes 마지막 줄에 남아 있는 "실서버 프로브로 계약이 검증됐다" 는 취지의 문장은
`2026-08-25 HAR(436 entries) 실측으로 정정됨 — 근거: .planning/phases/05-api/05-01-SUMMARY.md` 로 바꾼다.
**R017 항목 본문 안에서는 구 3단계 순서에 등장하던 두 엔드포인트 이름(`/v2/auth/otp-sessions`,
`/v3/auth/token/by-credentials-with-otp`)을 한 번도 쓰지 않는다** — 그 반증 서술은 (2) 의 R018 항목이 담당한다.
같은 파일 안에서 "실측 계약을 서술하는 자리" 와 "무엇이 반증됐는지 기록하는 자리" 를 분리하는 것이 목적이다.

**(2) R018 (이메일 OTP 코드 입력 및 인증).** `Status` 를 `active` → `blocked` 로 내린다 (이 파일에서 처음 쓰이는
status 값이므로, `## Active` 섹션에 그대로 두되 항목 제목 옆에 보류 표시를 남긴다).
`Primary owning slice` 를 `v0.3.0/Phase 05` → `none (unmapped — 2026-08-25 Phase 05 매핑 해제)` 로 바꾼다.
`Notes` 에 D-05 가 지정한 사유를 그대로 기록한다: HAR 상 실제 로그인 흐름에 OTP 단계가 존재하지 않으며
(`/v2/auth/otp-sessions`, `/v3/auth/token/by-credentials-with-otp`, `/v2/auth/otp` 호출 0건), 캡차 실패 시의 폴백
경로로만 존재할 가능성이 남아 있으나 05-01 실계정 스파이크에서 OTP 메일이 오지 않아 미입증이라는 점,
그리고 `-25044` 가 OTP 요구가 아니라 캡차 실패 신호였다는 점. 근거로 `.planning/phases/05-api/05-01-SUMMARY.md`
경로를 명시한다. `Why it matters` 의 "(-25044 실측 확인)" 근거 문장도 같은 정정에 맞춘다. **항목을 삭제하지 않는다.**

**(3) traceability 표.** `| R017 | ... |` 행의 Validation 칸에 정정 사실을 한 줄로 기록하고,
`| R018 | core-capability | blocked | none | none | 2026-08-25 Phase 05 매핑 해제 — HAR 상 OTP 단계 부재, 미입증 |`
형태로 R018 행을 갱신한다. R019 행은 이 플랜에서 건드리지 않는다 (05-03 이 관측 결과로 갱신한다).

**(4) Coverage Summary.** `Active requirements` / `Mapped to slices` / `Unmapped active requirements` 숫자를
R018 보류 반영해 다시 계산한다. `v0.3.0: ... R017/R018/R019 → Phase 05` 문구를 `R017/R019 → Phase 05,
R018 → blocked/unmapped` 로 고친다. 표와 본문 숫자가 서로 어긋나지 않아야 한다.
  </action>
  <verify>
    <automated>test "$(sed -n '/^### R017/,/^### R018/p' .planning/REQUIREMENTS.md | grep -c 'otp-sessions')" = "0" && test "$(sed -n '/^### R018/,/^### R019/p' .planning/REQUIREMENTS.md | grep -c '^- Status: blocked')" = "1" && grep -q '| R018 | core-capability | blocked |' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### R017/,/^### R018/p' .planning/REQUIREMENTS.md | grep -c 'otp-sessions'` 가 0 이다
    - `sed -n '/^### R017/,/^### R018/p' .planning/REQUIREMENTS.md | grep -c 'by-credentials-with-otp'` 가 0 이다
    - `sed -n '/^### R017/,/^### R018/p' .planning/REQUIREMENTS.md | grep -c 'reCAPTCHA'` 가 1 이상이다 (실측 계약이 서술됨)
    - `sed -n '/^### R018/,/^### R019/p' .planning/REQUIREMENTS.md | grep -c '^- Status: blocked'` 가 1 이다
    - `sed -n '/^### R018/,/^### R019/p' .planning/REQUIREMENTS.md | grep -c 'Phase 05'` 로 확인했을 때 `Primary owning slice` 줄이 Phase 05 를 가리키지 않는다 (매핑 해제)
    - `sed -n '/^### R018/,/^### R019/p' .planning/REQUIREMENTS.md | grep -c '05-01-SUMMARY.md'` 가 1 이상이다 (근거 인용)
    - `grep -c '| R018 | core-capability | blocked |' .planning/REQUIREMENTS.md` 가 1 이다
    - `grep -c '### R018' .planning/REQUIREMENTS.md` 가 1 이다 (항목이 삭제되지 않음)
    - `sed -n '/^### R017/,/^### R018/p' .planning/REQUIREMENTS.md | grep -c '05-01-SUMMARY.md'` 가 1 이상이다 (정정 근거 인용 — "실서버 프로브로 검증됨" 주장을 대체)
    - Coverage Summary 의 숫자가 traceability 표의 실제 행 상태와 일치한다 (수동 대조 가능한 형태로 기록)
  </acceptance_criteria>
  <done>R017 은 실측 계약으로 서술이 교체되고, R018 은 삭제 없이 blocked + 사유 + Phase 05 매핑 해제 상태가 되며, 표/요약 숫자가 정합한다. 이 파일만 읽는 다음 실행자가 3단계 OTP 로그인을 사실로 오인할 수 없다.</done>
</task>

<task type="auto">
  <name>Task 2: PROJECT.md API 계약 표 + ROADMAP Phase 05 Goal/Success Criteria 정정</name>
  <read_first>
    - `.planning/PROJECT.md` — "검증된 API 계약 (2026-08-25 실측)" 표(33-45행 부근)와 그 아래 "핵심 제약 (실측 확인)" 문단
    - `.planning/ROADMAP.md` — `### Phase 05` 블록의 `**Goal**`, `**Success Criteria**` 1~3, 그 아래 이미 달려 있는 `⚠ Phase 05 HALTED` 콜아웃
    - `.planning/phases/05-api/05-01-SUMMARY.md` — 정정 근거
    - `.planning/phases/05-api/05-CONTEXT.md` — `<domain>` 절의 "원래 목표에서 달라진 점" (새 Goal 문구의 근거)
  </read_first>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <action>
두 파일 모두 `Edit` 으로만 수정한다 (`Write` 금지 — ROADMAP.md 전체 재작성은 Phase 06/07 항목을 파괴한다).

**(1) PROJECT.md** (33-45행 부근의 계약 표 블록). 표 제목이 이 계약을 "검증됨" 으로 단정하고 있다 —
정정 사실이 드러나는 제목으로 바꾼다 (예: `계정 API 계약 (2026-08-25 HAR 실측으로 정정)`).
표의 8개 행 중 세 행을 고친다. **세션 생성 행**과 **OTP 로그인 행**: 값 칸을 "실제 웹 클라이언트 로그인 흐름에
등장하지 않음(HAR 호출 0건) — R018 보류" 로 바꾼다 (행 자체는 기록으로 남긴다). **로그인 행**: 바디 필드
`otpSessionId` 의 설명을 "reCAPTCHA Enterprise 토큰(실측 2489자) — OTP 세션 식별자가 아니다" 로 고친다.
**reCAPTCHA 행**: 현재 이 행은 캡차가 세션 생성 단계에서만 쓰인다고 서술하고 있는데 이것이 반증된 부분이다 —
"reCAPTCHA Enterprise — 자격증명 로그인 요청 자체의 필수 입력. 순수 HTTP 로는 채울 수 없다" 로 교체한다.
표 바로 아래 "핵심 제약 (실측 확인)" 문단의 결론 문장(순수 HTTP 로그인 시 서버가 OTP 를 요구하므로 API 모드에서
자동 재로그인이 불가능하다는 취지)을 실측에 맞게 교체한다 — 실제 제약은 "순수 HTTP 로그인은 캡차 관문에서
막히며(`-25044` 는 캡차 실패 신호), 캡차 우회는 R013 으로 영구 제외이므로 현재 동작하는 유일한 로그인 경로는
실제 로그인 페이지를 띄우는 헤드리스 BrowserWindow 다" 이다.
Base URL / 5종 필수 헤더 / 평문 비밀번호 행은 여전히 유효하므로 그대로 둔다. 정정한 블록 끝에 근거 경로
`.planning/phases/05-api/05-01-SUMMARY.md` 를 명시한다.

**(2) ROADMAP.md Phase 05.** `**Goal**` 한 줄을 CONTEXT.md `<domain>` 절에 맞춰 교체한다 — 이 phase 의 목표는
"이메일/비밀번호 + 이메일 OTP 로 로그인 완료" 가 아니라 "이미 동작 중인 헤드리스 로그인에서 계정 토큰을 확보해
account → 팬이벤트 토큰 사다리(R019)가 실계정에서 성립하는지 1회 관찰로 판정하고, 반증된 로그인 계약을 문서에서
제거하는 것" 이다. `**Requirements**` 줄은 `R017, R019 (R018 은 2026-08-25 보류·매핑 해제)` 로 고친다.
`**Success Criteria**` 1 번과 2 번 항목 앞에 `~~취소선~~` 대신 `**[VOID — 2026-08-25 HAR 반증]**` 접두를 붙이고
본문은 기록으로 남긴다 (지우지 않는다 — 무엇이 왜 무효가 됐는지가 이 프로젝트의 학습 자산이다).
3 번 항목은 그대로 두되 "이 phase 의 유일한 유효 판정 기준" 이라고 표시한다.
이미 달려 있는 `⚠ Phase 05 HALTED` 콜아웃은 지우지 말고, 맨 아래에 한 줄을 덧붙여 재설계 완료 사실과
새 플랜 3개(05-01/05-02/05-03)를 가리킨다. 상단 마일스톤 목록(24행 부근)의 Phase 05 한 줄 설명도
구 3단계 로그인 서술 대신 재설계된 목표로 바꾼다.

두 파일 어디에도 실제 계정 이메일 주소·비밀번호·토큰 원문을 예시로 넣지 않는다. 예시가 필요하면
`*@example.com` 형태의 더미만 쓴다 (D-06 비협상 기준선).
  </action>
  <verify>
    <automated>grep -q '05-01-SUMMARY.md' .planning/PROJECT.md && grep -q 'reCAPTCHA Enterprise' .planning/PROJECT.md && test "$(sed -n '/^### Phase 05/,/^### Phase 06/p' .planning/ROADMAP.md | grep -c 'VOID')" = "2" && grep -q '### Phase 06' .planning/ROADMAP.md && grep -q '### Phase 07' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '05-01-SUMMARY.md' .planning/PROJECT.md` 가 1 이상이다 (정정 근거 인용)
    - `grep -c 'reCAPTCHA Enterprise' .planning/PROJECT.md` 가 1 이상이다 (실측 계약이 서술됨)
    - `sed -n '/API 계약/,/^## Architecture/p' .planning/PROJECT.md | grep -c 'OTP 세션 생성 시에만'` 가 0 이다 (반증된 캡차 서술 제거)
    - `sed -n '/API 계약/,/^## Architecture/p' .planning/PROJECT.md | grep -c '매 로그인마다 OTP'` 가 0 이다
    - `sed -n '/API 계약/,/^## Architecture/p' .planning/PROJECT.md | grep -c '검증된 API 계약'` 가 0 이다 (반증된 "검증됨" 단정 제거)
    - `sed -n '/^### Phase 05/,/^### Phase 06/p' .planning/ROADMAP.md | grep -c 'VOID'` 가 2 이다 (Success Criteria 1·2 가 무효로 표시됨)
    - `sed -n '/^### Phase 05/,/^### Phase 06/p' .planning/ROADMAP.md | grep -c '이메일 OTP만으로 로그인을 완료'` 가 0 이다 (Goal 이 반증 전제를 사실로 서술하지 않음)
    - `sed -n '/^### Phase 05/,/^### Phase 06/p' .planning/ROADMAP.md | grep -c 'R019'` 가 1 이상이다
    - `grep -c '### Phase 06' .planning/ROADMAP.md` 가 1 이고 `grep -c '### Phase 07' .planning/ROADMAP.md` 가 1 이다 (다른 phase 항목이 파괴되지 않음)
    - `grep -cE '[a-zA-Z0-9._%+-]+@(gmail|naver|daum|kakao|hanmail)\.' .planning/PROJECT.md .planning/ROADMAP.md` 가 0 이다 (실계정 이메일 미기입)
  </acceptance_criteria>
  <done>PROJECT.md 와 ROADMAP.md 가 반증된 3단계 OTP 로그인 계약을 사실로 주장하지 않으며, 무엇이 왜 무효가 됐는지의 기록은 보존된다. 다른 phase 항목과 유효한 계약 정보는 손상되지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 계획 문서 → 미래 실행자 | 문서가 주장하는 "검증된 계약" 이 다음 실행자의 코드 전제가 된다. 틀린 주장은 코드 결함보다 넓게 전파된다 (05-01 이 그 실증 사례) |
| 정정 작업자 → 저장소 히스토리 | 잘못된 편집 방식(전체 덮어쓰기)이 다른 요구사항/phase 기록을 소실시킬 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-08 | Tampering | `.planning/REQUIREMENTS.md` / `ROADMAP.md` 전체 덮어쓰기로 인한 기록 소실 | high | mitigate | 두 태스크 모두 `Write` 금지·scoped `Edit` 강제. 인수 기준에 다른 요구사항/phase 항목의 존속(`### Phase 06`, `### Phase 07`, `### R018` 계수)을 명시적으로 포함 |
| T-05-09 | Repudiation | 정정이 근거 없이 이루어져 미래에 되돌려짐 | medium | mitigate | 모든 정정 문구가 `.planning/phases/05-api/05-01-SUMMARY.md` 경로를 인용하도록 강제하고 grep 으로 검증 |
| T-05-10 | Information Disclosure | 정정 예시에 실계정 이메일/비밀번호/토큰 원문이 들어감 | high | mitigate | 실계정 식별자 기입 금지 + 더미(`*@example.com`) 규칙(D-06)을 액션에 명시하고, 흔한 실계정 도메인 패턴 부재를 grep 으로 검증 |
| T-05-11 | Information Disclosure | 캡차 우회 방법이 문서에 기록됨 | high | mitigate | R013 영구 제외 — 우회 방법을 서술하지 않고 "우회 불가·헤드리스 경로가 유일" 이라는 사실만 기록 |

**패키지 정당성 게이트: 해당 없음.** 이 플랜은 문서만 수정하며 패키지 설치 태스크가 없다.
</threat_model>

<verification>
1. `sed -n '/^### R017/,/^### R018/p' .planning/REQUIREMENTS.md | grep -c 'otp-sessions'` → 0
2. `grep -c '^- Status: blocked' .planning/REQUIREMENTS.md` → 1 이상
3. `grep -c '### R0' .planning/REQUIREMENTS.md` 가 정정 전과 동일 (요구사항 항목 소실 없음)
4. `grep -c '### Phase 0' .planning/ROADMAP.md` 가 정정 전과 동일 (phase 항목 소실 없음)
5. `git diff --stat` 이 이 3개 파일 외의 변경을 보이지 않는다
</verification>

<success_criteria>
- REQUIREMENTS.md 만 읽는 사람이 3단계 OTP 로그인을 현재 유효한 계약으로 오인할 수 없다
- R018 이 삭제가 아니라 사유가 붙은 보류로 기록되고 Phase 05 에서 매핑 해제된다
- PROJECT.md 와 ROADMAP.md 가 같은 반증 사실에 정합하며, 무효화의 이유가 기록으로 남는다
- 정정 과정에서 다른 요구사항·phase 기록이 소실되지 않는다
</success_criteria>

<output>
Create `.planning/phases/05-api/05-02-SUMMARY.md` when done.
</output>
