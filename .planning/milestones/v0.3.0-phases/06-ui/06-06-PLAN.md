---
phase: 06-ui
plan: 06
type: execute
wave: 4
depends_on: ["06-01", "06-02", "06-05"]
files_modified:
  - src/renderer/components/ApiModeNoticeModal.tsx
  - src/renderer/components/LoginPanel.tsx
  - src/renderer/components/login-panel-view.ts
  - src/renderer/components/__tests__/login-panel-view.test.ts
  - src/renderer/App.tsx
  - src/renderer/styles.css
autonomous: true
requirements: [R016, R020, R021]

estimate:
  tokens: 68000
  raw_tokens: 68000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "사용자가 API 모드를 처음 선택하면 고지를 확인해야만 진행할 수 있다 — 확인 없이는 모드가 저장되지 않는다 (R021, D-09, ROADMAP SC2 정정본)"
    - "고지 문구는 Weverse 보안 확인 시 실패 가능성과 자동 재로그인 부재 두 가지만 담으며, 반증된 이메일 코드 서사를 담지 않는다 (D-08)"
    - "고지 버전이 올라가면 이미 확인한 사용자에게도 다음 API 모드 선택 시 다시 확인을 요구한다 (D-10)"
    - "API 모드가 활성인 동안에는 짧은 제약 안내가 상시 노출된다 (D-09 후반부)"
    - "실패 안내는 기존 오류 슬롯 하나에만 표시되며 새 표시 영역이나 두 번째 모달을 만들지 않는다 (D-15)"
    - "환경변수 미설정 시 잠금 배지와 상세 문구가 아예 렌더링되지 않는다 — 자리를 비워두지도 않는다 (UI-SPEC E2 empty)"
    - "잠금 배지는 '환경변수로 고정됨' 문구와 함께 현재 실제로 적용 중인 모드 라벨을 보여주고, 환경변수 원문 값은 절대 표시하지 않으며 두 탭 모두 비활성이다 (UI-SPEC E2 populated)"
    - "잠금 배지 상세 문구는 탭 아래 블록으로 자연스럽게 줄바꿈되며 말줄임으로 잘리지 않는다 — 어떤 모드가 적용 중인지 가려질 수 없다 (UI-SPEC E2 overflow)"
    - "잠금 배지에 끼워 넣는 모드 라벨은 두 개의 고정 문자열 중 하나뿐이라 최대 길이가 알려져 있고 잘라내기 로직이 필요 없다 (UI-SPEC E2 long-text)"
    - "고지 확인 저장이 실패하면 모달이 닫히지 않고 모드도 바뀌지 않으며 모달 안에 저장 실패 안내가 표시된다 (UI-SPEC E3 error)"
    - "창을 아무리 작게 줄여도 고지 모달의 확인 버튼에 도달할 수 있다 — 본문만 스크롤되고 버튼 행은 스크롤 영역 밖에 고정된다 (UI-SPEC E3 overflow)"
    - "고지 모달의 두 문구는 고정 문자열이라 자연 줄바꿈만으로 표시되고 잘라내기 로직이 없다 (UI-SPEC E3 long-text)"
    - "상시 안내 배너는 한 문장이 카드 폭 안에서 줄바꿈될 뿐 말줄임되지 않고 닫기 버튼도 없다 (UI-SPEC E4 overflow)"
    - "상시 안내 배너 문구는 작성된 고정 문자열이며 동적으로 길어질 여지가 없다 (UI-SPEC E4 long-text)"
    - "실패가 없을 때 오류 영역은 아예 렌더링되지 않는다 — 빈 상자나 예약된 높이가 남지 않는다 (UI-SPEC E5 empty)"
    - "여섯 가지 실패 사유 전부가 같은 오류 슬롯에 역할 속성을 유지한 채 표시된다 (UI-SPEC E5 error)"
    - "실패 표시는 문장 하나에 선택적 식별자 칩과 선택적 전환 버튼이 따라붙는 형태다 (UI-SPEC E5 populated)"
    - "식별자가 없는 사유는 칩 없이 문장만 렌더링된다 — 정의되지 않은 값이 화면에 찍히지 않는다 (UI-SPEC E5 partial)"
    - "새 실패는 이전 메시지를 교체한다 — 실패가 목록으로 쌓이지 않는다 (UI-SPEC E5 zero-one-many)"
  prohibitions:
    - "비모달 표시 방식으로 고지를 띄우지 않는다 — 차단형이 아니면 '확인해야만 진행' 요건을 만족하지 못한다"
    - "취소나 Esc 를 확인으로 취급하지 않는다 — 확인 버튼 클릭만이 저장과 모드 전환을 일으킨다"
    - "고지 문구에 헤드리스 동작 방식·미검증 내부 사정 같은 구현 세부를 넣지 않는다"
    - "UI-SPEC 이 확정한 문구를 구현 시점에 고쳐 쓰거나 다듬지 않는다"
    - "새 UI 라이브러리나 아이콘 패키지를 도입하지 않는다 — 기존 CSS 토큰과 클래스만 쓴다"
    - "실패 안내용 새 표시 영역이나 두 번째 모달을 만들지 않는다"
  artifacts:
    - src/renderer/components/ApiModeNoticeModal.tsx
    - src/renderer/components/login-panel-view.ts
    - src/renderer/components/__tests__/login-panel-view.test.ts
  key_links:
    - "확인 버튼 클릭 ↔ `settings:ack-notice` ↔ `settings:set-login-mode` — 두 부수효과가 결합돼 있으며 순서가 어긋나면 확인 없이 모드가 저장된다"
    - "`CredentialLoginResult.reason` ↔ `mapLoginFailure().suggestBrowserSwitch` ↔ 전환 버튼 노출 조건"
    - "`lockedByEnv` ↔ 탭 disabled ↔ 배지 렌더링 — 셋이 같은 값에서 파생되지 않으면 UI 가 거짓말한다"
---

<objective>
로그인 방식 선택기 주변의 사용자 대면 계층을 완성한다: 최초 API 선택 시의 차단형 고지 모달,
환경변수 잠금 상태 표시, API 모드 상시 안내, 그리고 실패 사유별 한국어 안내 렌더링.

Purpose: 이 phase 를 관통하는 기준은 "UI 가 거짓말하지 않는다" 다. 고지·잠금 표시·실패 안내
셋 중 하나라도 빠지면 사용자는 앱이 말하는 것과 다른 현실을 마주한다.
Output: `ApiModeNoticeModal.tsx`, 잠금 배지와 상시 배너를 갖춘 `LoginPanel`, 사유별 실패 안내.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-UI-SPEC.md
@.planning/phases/06-ui/06-PATTERNS.md
@.planning/phases/06-ui/06-01-SUMMARY.md
@.planning/phases/06-ui/06-05-SUMMARY.md
</context>

<planner_assumptions>
⚠ **FLAGGED ASSUMPTION — R021 / concurrency (edge probe: unresolved / unclassified)**
결정적 edge probe 가 R021 에 대해 "중단되거나 병렬로 실행되면 무엇이 보장되는가?"를 물었고
`unresolved` 로 남았다. 이 플랜은 다음을 **가정**하고 진행한다:

- 고지 확인 저장(`settings:ack-notice`)과 모드 저장(`settings:set-login-mode`)은 확인 버튼 클릭
  한 번에서 **순차로** 일어난다. 앞의 것이 실패하면 뒤의 것을 실행하지 않는다 — 따라서 "확인은
  기록됐는데 모드는 안 바뀜" 또는 그 반대의 어긋난 상태가 남지 않는다. 두 쓰기 사이에 프로세스가
  죽는 극단적 경우에는 확인만 기록되고 모드는 이전 값으로 남는다(안전한 방향의 실패).
- 모달이 열려 있는 동안 사용자는 배경과 상호작용할 수 없다(네이티브 차단형 모달의 inert 처리).
  따라서 모달 표시 중 탭이 다시 눌리는 경합은 발생하지 않는다.
- 이 가정이 틀리다면(예: 향후 비모달 표시로 전환) 확인 이전에 모드가 저장되는 창이 열린다. 그
  시점에 두 쓰기를 하나의 원자적 설정 갱신으로 합칠 것.

이 가정은 조용히 넘어가지 않고 SUMMARY 에 그대로 옮겨 적어야 한다.
</planner_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: 차단형 고지 모달 컴포넌트 + 전용 스타일</name>
  <files>src/renderer/components/ApiModeNoticeModal.tsx, src/renderer/styles.css</files>
  <read_first>
    - src/renderer/styles.css (전체 343줄 — 특히 `.card`/`.btn`/`.btn-primary`/`.btn-secondary`/`.button-row`/`.consent-body`(264-272, 스크롤 본문 패턴의 유일한 선례)/`.token-preview`(94-101)/`:root` 색 토큰)
    - src/renderer/components/LoginPanel.tsx (209-218 — 버튼 행 관용구. 모달이 시각적으로 붙어야 할 기존 문법)
    - .planning/phases/06-ui/06-UI-SPEC.md (§Copywriting Contract "Notice modal" 표 = 문구 정본, §Color 의 backdrop 값, §Spacing Scale, §Typography, §UI Considerations E3)
    - .planning/phases/06-ui/06-RESEARCH.md (§Architecture Patterns Pattern 3 — 네이티브 모달 골격과 `.show()` 대신 `.showModal()` 을 써야 하는 이유)
    - .planning/phases/06-ui/06-PATTERNS.md (§ApiModeNoticeModal.tsx — 이 저장소에 모달 선례가 없다는 사실과 조립 지침)
  </read_first>
  <action>
    `src/renderer/components/ApiModeNoticeModal.tsx` 를 신규 작성한다. 이 저장소에는 모달 선례가
    전혀 없으므로 `06-RESEARCH.md` Pattern 3 의 네이티브 `<dialog>` 골격을 출발점으로 삼는다.

    props 는 `{ open: boolean; saving: boolean; saveError: string | null; onAcknowledge: () => void;
    onCancel: () => void }` 다. `useEffect` 에서 `open` 이 true 가 되면 `showModal()` 을 호출한다 —
    비차단 표시 메서드를 쓰면 포커스 트랩·배경 inert·backdrop 이 전부 사라져 "확인해야만 진행"
    요건이 무너진다. `open` 이 false 가 되면 닫는다.

    구조: 제목 `<h2 id="api-mode-notice-title">` + `aria-labelledby` 로 연결, 본문 `<p>` 2개,
    버튼 행 2개(확인 / 취소). 본문 `<p>` 2개는 **스크롤 가능한 내부 래퍼** 안에 넣고 버튼 행은
    그 래퍼 **밖**에 둔다 — 창을 아무리 줄여도 확인 버튼에 도달할 수 있어야 한다(UI-SPEC E3
    overflow: `main.ts` 가 최소 높이를 설정하지 않아 창은 임의로 작아질 수 있다).

    Esc 키는 `<dialog>` 의 `onCancel` 이벤트로 들어온다. 이 핸들러가 **확인 경로와 완전히 분리된
    취소 경로**로 이어져야 한다 — 시각적으로 닫히는 것만으로 확인 처리가 되면 안 된다.
    취소 버튼과 Esc 는 같은 `onCancel` prop 을 호출한다.

    `saveError` 가 있으면 모달을 닫지 않고 버튼 행 위에 `.error-message` 로 표시한다. `saving`
    중에는 확인 버튼에 `aria-busy` 를 걸고 비활성화한다.

    문구는 `06-UI-SPEC.md` "Notice modal" 표에서 **그대로 복사**한다 — 제목, 두 문장, 확인/취소
    라벨 모두 최종 확정본이다. 헤드리스 동작 방식이나 내부 검증 상태 같은 구현 세부를 추가하지
    않는다. 반증된 이메일 코드 서사는 어떤 형태로도 넣지 않는다.

    `styles.css` 에 추가할 규칙(기존 토큰만 사용, 새 색 커스텀 프로퍼티 추가 금지):
    `dialog::backdrop` 배경은 UI-SPEC §Color 가 지정한 반투명 검정 값 그대로,
    다이얼로그 본문 래퍼는 `.consent-body`(264-272)의 스크롤 패턴을 본떠 `max-height` +
    `overflow-y: auto`, 다이얼로그 자체는 `.card` 계열의 표면색·반경·패딩을 따른다.
    한국어 줄바꿈을 위해 `word-break: keep-all` 을 본문에 적용한다. 간격은 UI-SPEC §Spacing Scale
    의 4배수 토큰만, 글자 크기·굵기는 §Typography 의 3사이즈/2웨이트만 쓴다.
    같은 작업에서 잠금 배지와 상시 안내 배너의 클래스도 함께 정의한다(Task 2 가 사용):
    배지는 muted 텍스트 + 기존 테두리 색의 중립 스타일이고 강조색을 쓰지 않는다,
    배너는 `.consent-body` 와 같은 상자 스타일에 경고색 3px 왼쪽 테두리를 더한 형태이며
    상호작용 요소가 아니다.
  </action>
  <verify>
    <automated>npm run typecheck</automated>
    <automated>npm run build:renderer</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` 가 0 으로 종료한다
    - `npm run build:renderer` 가 0 으로 종료한다
    - `grep -c "showModal()" src/renderer/components/ApiModeNoticeModal.tsx` 가 1 이다
    - `grep -c "\.show()" src/renderer/components/ApiModeNoticeModal.tsx` 가 0 이다
    - `grep -c "aria-labelledby=\"api-mode-notice-title\"" src/renderer/components/ApiModeNoticeModal.tsx` 가 1 이다
    - `grep -c "onCancel" src/renderer/components/ApiModeNoticeModal.tsx` 가 2 이상이다 (Esc 경로 + 취소 버튼)
    - `grep -c "API 로그인 방식 안내" src/renderer/components/ApiModeNoticeModal.tsx` 가 1 이다
    - `grep -c "확인했습니다" src/renderer/components/ApiModeNoticeModal.tsx` 가 1 이다
    - `grep -c "dialog::backdrop" src/renderer/styles.css` 가 1 이다
    - `grep -c "overflow-y: auto" src/renderer/styles.css` 가 2 이상이다 (기존 1 + 모달 본문 1)
    - `grep -c "^  --color-" src/renderer/styles.css` 가 변경 전과 동일하다 (새 색 토큰 미추가)
    - `git diff --stat package.json` 이 비어 있다
    - 동작: 창 높이를 최소로 줄여도 확인 버튼이 화면 안에 남는다
  </acceptance_criteria>
  <reversibility rating="reversible">새 컴포넌트 파일 하나와 CSS 규칙 추가다. 삭제하면 이전 상태로 정확히 돌아간다.</reversibility>
  <done>차단형 고지 모달이 포커스 트랩·Esc·backdrop 을 갖추고, 취소 경로가 확인 경로와 분리돼 있으며, 좁은 창에서도 확인 버튼에 도달할 수 있다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 선택기 UI 완성 — 잠금 배지 · 상시 안내 · 모달 배선</name>
  <files>src/renderer/components/login-panel-view.ts, src/renderer/components/__tests__/login-panel-view.test.ts, src/renderer/components/LoginPanel.tsx, src/renderer/App.tsx</files>
  <read_first>
    - src/renderer/components/LoginPanel.tsx (06-01 이 재구조화한 상태 — props 로 승격된 탭, 상태 행 아래로 옮겨진 탭 블록)
    - src/renderer/App.tsx (06-01 결과 — loginMode/lockedByEnv/noticeAck 상태와 핸들러)
    - src/shared/api-mode-notice.ts (06-01 결과 — 버전 상수와 재노출 판단 함수)
    - src/renderer/components/__tests__/profile-form-validation.test.ts (렌더링 없는 렌더러 로직 테스트 관례)
    - .planning/phases/06-ui/06-UI-SPEC.md (§Copywriting Contract 잠금 배지·상시 배너 문구, §Interaction Contract 1~5, §UI Considerations E2·E4)
  </read_first>
  <behavior>
    - `resolveTabView("api", false)` → API 탭이 활성이고 두 탭 모두 조작 가능하다
    - `resolveTabView("browser", true)` → 브라우저 탭이 활성이고 두 탭 모두 비활성이며 배지가 표시된다
    - `describeLockedMode("api")` → 상세 문구에 `API 로그인` 라벨이 들어가고 환경변수 원문 값은 들어가지 않는다
    - `describeLockedMode("browser")` → 상세 문구에 `브라우저 로그인` 라벨이 들어간다
    - `shouldShowInlineNotice("api")` → true, `shouldShowInlineNotice("browser")` → false
    - `decideTabClick({ next: "api", current: "browser", lockedByEnv: false, ackedVersion: null })` → 모달을 열어야 한다는 결과
    - `decideTabClick({ next: "api", current: "browser", lockedByEnv: false, ackedVersion: 현재버전 })` → 모달 없이 즉시 저장
    - `decideTabClick({ next: "api", current: "api", ... })` → 아무 것도 하지 않는다
    - `decideTabClick({ next: "browser", current: "api", ... })` → 모달 없이 즉시 저장
    - `decideTabClick({ ..., lockedByEnv: true })` → 어떤 입력이든 아무 것도 하지 않는다 (IPC 호출도 모달도 없다)
  </behavior>
  <action>
    (1) `src/renderer/components/login-panel-view.ts` 를 신규 작성한다 — JSX 없는 순수 모듈이며
    LoginPanel 의 판단 로직만 담는다. 이 저장소의 기존 렌더러 테스트는 판단 함수를 테스트 파일
    안에 복제해 두었지만, 여기서는 **실제 모듈을 import 해서 테스트한다**(복제본이 아니라 배포되는
    코드를 검증하기 위해서다). 내보낼 함수는 `<behavior>` 에 나열된 4개:
    `resolveTabView()`, `describeLockedMode()`, `shouldShowInlineNotice()`, `decideTabClick()`.
    `decideTabClick()` 의 반환은 `{ action: "none" | "save" | "notice"; mode?: LoginMode }` 같은
    판별 가능한 형태여야 하며, 판단을 컴포넌트 안에 남기지 않는다.

    (2) `LoginPanel.tsx` — 탭 행 옆에 잠금 배지를, 탭 행 아래에 상시 안내 배너를 붙인다.
    - **잠금 배지:** `lockedByEnv` 가 true 일 때만 렌더링한다. false 면 배지도 상세 문구도 아예
      렌더링하지 않는다(자리를 비워두지도 않는다). 배지 라벨은 UI-SPEC 확정 문구
      `환경변수로 고정됨` 정확히 그대로다. 상세 문구는 `describeLockedMode(loginMode)` 결과이며
      **현재 적용 중인 모드 라벨**(`API 로그인` / `브라우저 로그인`)을 끼워 넣는다 — 환경변수
      원문 값은 어떤 경우에도 표시하지 않는다. 상세 문구는 탭 아래 블록으로 렌더링하고
      말줄임 처리를 하지 않는다(어떤 모드가 적용 중인지 가려지면 D-06 의 목적 자체가 무너진다).
      배지가 보일 때 두 탭 모두 `disabled` 다.
    - **상시 안내 배너:** `shouldShowInlineNotice(loginMode)` 가 true 일 때 UI-SPEC 확정 문구
      한 문장을 배너 클래스로 렌더링한다. 제목도 닫기 버튼도 없다 — API 모드가 활성인 한 계속
      보여야 한다. 로그인 상태와 무관하게 표시한다.
    - **탭 클릭:** 핸들러는 `decideTabClick()` 결과만 보고 분기한다. `notice` 면 모달을 열고
      **모드를 저장하지 않는다**. `save` 면 즉시 `onSetLoginMode(mode)` 를 호출한다. `none` 이면
      아무 IPC 도 호출하지 않는다.

    (3) `LoginPanel.tsx` / `App.tsx` — 모달 배선. 확인 버튼 클릭 시 **먼저**
    `window.api.settings.ackNotice(현재 버전)` 을 await 하고, 성공한 뒤에만
    `setLoginMode("api")` 를 호출한다(순서 역전 금지 — 확인 없이 모드가 저장되는 창을 만들지
    않는다). 확인 저장이 실패하면 모달을 닫지 않고 `saveError` 를 모달로 내려보내며 모드도 바꾸지
    않는다. 취소/Esc 는 두 IPC 를 모두 호출하지 않고 모달만 닫는다 — 탭 활성 표시는 `loginMode`
    에서만 파생되므로 저장하지 않는 것이 곧 이전 선택 유지다. 별도의 되돌리기 로직을 만들지 말 것.
    모달 표시 여부 상태는 LoginPanel 이 소유하고, 확인 후 `onAckNotice` 로 App 의 `noticeAck`
    상태를 갱신해 같은 세션에서 재노출되지 않게 한다.

    (4) 테스트 `src/renderer/components/__tests__/login-panel-view.test.ts` 신규 —
    `<behavior>` 의 9개 케이스를 한국어 `it()` 이름으로 전수 작성하고 실제 모듈을 import 한다.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/components/__tests__/login-panel-view.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/renderer/components/__tests__/login-panel-view.test.ts` 가 0 으로 종료한다
    - `npm run typecheck` 가 0 으로 종료한다
    - `grep -c "from \"../login-panel-view\"" src/renderer/components/__tests__/login-panel-view.test.ts` 가 1 이다 (복제본이 아니라 실제 모듈을 테스트)
    - `grep -c "환경변수로 고정됨" src/renderer/components/LoginPanel.tsx` 가 1 이다
    - `grep -c "AUTOVERSE_LOGIN_MODE 환경변수가 설정되어 있어" src/renderer/components/login-panel-view.ts` 가 1 이다
    - `grep -c "process.env" src/renderer/components/LoginPanel.tsx` 가 0 이다 (렌더러는 env 원문에 접근하지 않는다)
    - `grep -c "API 로그인은 Weverse 보안 확인" src/renderer/components/LoginPanel.tsx` 가 1 이다
    - `grep -c "decideTabClick" src/renderer/components/LoginPanel.tsx` 가 1 이상이다
    - `grep -c "ackNotice" src/renderer/App.tsx` 가 1 이상이다
    - 동작: 환경변수를 설정하고 앱을 실행하면 두 탭이 비활성이고 배지와 상세 문구가 보이며, 탭을 눌러도 아무 일도 일어나지 않는다
    - 동작: 처음 API 탭을 누르면 모달이 뜨고, 취소하면 브라우저 탭이 그대로 활성으로 남는다
    - 동작: 확인 후 앱을 재시작하고 API 탭을 다시 눌러도 모달이 뜨지 않는다
  </acceptance_criteria>
  <reversibility rating="reversible">렌더러 상태 배선과 표시 로직이며 되돌리기 비용이 낮다.</reversibility>
  <done>선택기가 자신의 실제 상태(활성 모드·잠금 여부·제약)를 항상 정직하게 표시하고, 최초 API 선택이 확인 없이는 진행되지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 실패 안내 렌더링 — 문장 · 식별자 칩 · 브라우저 전환 버튼</name>
  <files>src/renderer/components/login-panel-view.ts, src/renderer/components/__tests__/login-panel-view.test.ts, src/renderer/components/LoginPanel.tsx</files>
  <read_first>
    - src/renderer/components/LoginPanel.tsx (Task 2 결과 — 오류 슬롯 위치와 자격증명 로그인 핸들러)
    - src/shared/login-failure.ts (06-02 + 06-05 결과 — `mapLoginFailure()` 반환 필드)
    - src/shared/types.ts (06-05 결과 — `CredentialLoginResult` 의 `reason` / `identifier` / `message`)
    - src/renderer/styles.css (94-101 `.token-preview`, 179-183 `.error-message`)
    - .planning/phases/06-ui/06-UI-SPEC.md (§Copywriting Contract "Failure reason → copy" + "Identifier format", §UI Considerations E5 전 행)
  </read_first>
  <behavior>
    - `buildFailureView({ success: false, reason: "captcha", message: "…" })` → 전환 버튼을 노출해야 한다는 결과
    - `buildFailureView({ success: false, reason: "form-error", message: "…" })` → 전환 버튼 없음, 칩 없음
    - `buildFailureView({ success: false, reason: "network-error", message: "…", identifier: "ECONNRESET" })` → 칩 텍스트에 식별자가 포함된다
    - `buildFailureView({ success: false, reason: "unknown", message: "…" })` → identifier 가 없으면 칩 자체가 없다 (정의되지 않은 값이 문자열로 새어 나오지 않는다)
    - `buildFailureView({ success: true })` → 표시할 것이 없다는 결과
    - `buildFailureView` 결과를 연속으로 두 번 계산해도 이전 결과가 누적되지 않는다 (항상 최신 하나)
  </behavior>
  <action>
    (1) `login-panel-view.ts` 에 `buildFailureView(result: CredentialLoginResult | null)` 를 추가한다.
    반환은 `{ visible: boolean; message: string; identifier?: string; showBrowserSwitch: boolean }`
    형태다. 문구는 이미 main 이 매핑·마스킹해서 `message` 에 실어 보내므로 **렌더러가 문구를 다시
    만들지 않는다** — 대신 `reason` 을 `mapLoginFailure()` 에 넣어 `suggestBrowserSwitch` 만
    가져온다(문구를 되파싱하지 않기 위해 `reason` 을 계약에 넣었다). `identifier` 속성이 없으면
    칩 관련 필드를 결과에 담지 않는다.

    (2) `LoginPanel.tsx` — 자격증명 로그인 실패 시 `buildFailureView()` 결과로 기존 오류 슬롯
    하나만 갱신한다. 실패가 없으면 그 요소를 아예 렌더링하지 않는다(기존 조건부 렌더 유지).
    새 실패는 이전 메시지를 교체하며 목록으로 쌓지 않는다. 슬롯의 역할 속성(`role="alert"`)과
    클래스는 그대로 유지한다 — 새 표시 영역이나 두 번째 모달을 만들지 않는다(D-15).
    - **식별자 칩:** `identifier` 가 있을 때만 문장 뒤에 작은 칩으로 렌더링한다. UI-SPEC 이
      지정한 형태는 괄호 안에 `식별자:` 접두를 둔 monospace 소형 칩이며 기존 토큰 미리보기
      클래스를 재사용한다. 값은 main 이 이미 마스킹한 것이므로 렌더러에서 다시 가공하지 않는다.
    - **전환 버튼:** `showBrowserSwitch` 가 true 일 때만 UI-SPEC 확정 라벨
      `브라우저 로그인으로 전환` 버튼을 오류 슬롯 아래에 렌더링한다. 클릭하면 브라우저 모드로
      즉시 전환한다(브라우저 모드는 고지 대상이 아니므로 모달 없이 저장한다). 잠금 상태에서는
      이 버튼도 비활성이다 — 잠긴 상태에서 모드를 바꾸는 우회 경로를 만들지 않는다.

    (3) 테스트 — `<behavior>` 6개 케이스를 `login-panel-view.test.ts` 에 추가한다.
    `06-VALIDATION.md` 의 Per-Task Verification Map 에서 이 플랜에 해당하는 행의 Status 를
    갱신한다.
  </action>
  <verify>
    <automated>npx vitest run src/renderer/components/__tests__/login-panel-view.test.ts</automated>
    <automated>npm test &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/renderer/components/__tests__/login-panel-view.test.ts` 가 0 으로 종료한다
    - `npm test` 와 `npm run typecheck` 가 각각 0 으로 종료한다
    - `grep -c "export function buildFailureView" src/renderer/components/login-panel-view.ts` 가 1 이다
    - `grep -c "브라우저 로그인으로 전환" src/renderer/components/LoginPanel.tsx` 가 1 이다
    - `grep -c "token-preview" src/renderer/components/LoginPanel.tsx` 가 1 이상이다
    - `grep -c "role=\"alert\"" src/renderer/components/LoginPanel.tsx` 가 2 이하다 (기존 슬롯 재사용, 새 영역 미추가)
    - `grep -c "maskSensitive" src/renderer/components/LoginPanel.tsx` 가 0 이다 (마스킹은 main 책임, 렌더러는 재가공하지 않는다)
    - 테스트에 식별자 부재 케이스가 존재하고, 결과 객체에 칩 관련 필드가 없음을 단언한다
    - 동작: 캡차로 실패하면 안내 문장과 함께 전환 버튼이 보이고, 누르면 브라우저 탭으로 바뀐다
  </acceptance_criteria>
  <done>여섯 사유 전부가 하나의 오류 슬롯에서 정확한 한국어 문장으로 표시되고, 식별자와 전환 버튼이 사유에 맞게만 나타난다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (플랜 06-06 몫)

**신규 파일**
- `src/renderer/components/ApiModeNoticeModal.tsx`
- `src/renderer/components/login-panel-view.ts`
- `src/renderer/components/__tests__/login-panel-view.test.ts`

**신규 심볼**
- `ApiModeNoticeModal` (default export), `ApiModeNoticeModalProps`
- `resolveTabView()`, `describeLockedMode()`, `shouldShowInlineNotice()`, `decideTabClick()`, `buildFailureView()`

**신규 CSS 규칙 (`src/renderer/styles.css`)**
- `dialog::backdrop`, 고지 모달 컨테이너/스크롤 본문 클래스, 잠금 배지 클래스, 상시 안내 배너 클래스

**신규 사용자 문구 (모두 06-UI-SPEC.md 확정본)**
- 모달: `API 로그인 방식 안내`, 본문 2문장, `확인했습니다`, `취소`
- 배지: `환경변수로 고정됨` + 상세 문구
- 배너: API 모드 상시 안내 한 문장
- 실패 안내: `브라우저 로그인으로 전환`, 식별자 칩 포맷
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main → 렌더러 (IPC 반환값) | 실패 문구·식별자는 main 이 마스킹한 값이다. 렌더러는 재가공하지 않고 그대로 표시한다 |
| 사용자 입력(클릭·Esc) → 설정 쓰기 | 어떤 상호작용이 "확인"으로 간주되는지가 R021 의 핵심 |
| 환경변수 상태 → 화면 표시 | 원문 값이 화면에 노출되면 안 되고, 동시에 잠금 사실은 반드시 보여야 한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-22 | Repudiation | Esc/취소가 확인으로 처리돼 고지 없이 API 모드가 저장됨 | high | mitigate | 확인 경로와 취소 경로를 분리하고, 확인 저장 성공 후에만 모드를 저장하는 순서를 강제 (Task 1·2) + `decideTabClick` 전수 테스트 |
| T-06-23 | Information Disclosure | 잠금 배지가 환경변수 원문 값을 노출 | low | mitigate | IPC 가 불리언만 전달하고, 배지 상세 문구는 고정된 모드 라벨만 끼워 넣는다. 렌더러의 `process.env` 접근 0건을 grep 으로 검증 |
| T-06-24 | Information Disclosure | 식별자 칩이 마스킹되지 않은 값을 표시 | high | mitigate | 마스킹은 main 단일 지점(06-05)에서 수행하고 렌더러는 재가공 금지. 렌더러의 마스킹 호출 0건 grep 으로 이중 지점을 방지 |
| T-06-25 | Denial of Service (사용자) | 좁은 창에서 확인 버튼에 도달 불가 → 차단형 모달에 갇힘 | medium | mitigate | 본문만 스크롤, 버튼 행은 스크롤 영역 밖 고정 (Task 1, UI-SPEC E3 overflow) |
| T-06-26 | Tampering | 잠금 상태에서 전환 버튼으로 모드를 우회 변경 | medium | mitigate | 잠금 시 전환 버튼도 비활성 (Task 3) |
| T-06-SC | Tampering | npm/pip/cargo 설치 | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — UI 라이브러리·아이콘 패키지 도입 금지가 prohibitions 에 명시돼 있고 `package.json` diff 공백을 검증한다 |

블로킹 임계값: high. T-06-22 / T-06-24 / T-06-SC 는 이 플랜 안에서 코드 + 테스트 + grep 게이트로 해소된다.
</threat_model>

<verification>
- `npm test` 전체 통과 + `npm run typecheck` 통과 + `npm run build` 성공
- `git diff --stat package.json` 이 비어 있다
- 새 `--color-*` 커스텀 프로퍼티가 추가되지 않았다
- 오류 표시 영역이 하나뿐이다 (새 메시지 영역 미추가, D-15)
</verification>

<success_criteria>
- 최초 API 모드 선택이 고지 확인 없이는 진행되지 않는다 (R021, ROADMAP SC2 정정본)
- 고지 문구가 D-08 의 두 가지만 담는다
- 환경변수 잠금 상태가 화면에 정직하게 표시되고 조작이 봉쇄된다 (D-06)
- API 모드 활성 중 제약 안내가 상시 노출된다 (D-09)
- 여섯 실패 사유가 하나의 오류 슬롯에 정확한 한국어로 표시된다 (R020, D-15)
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-06-SUMMARY.md` when done.
`<planner_assumptions>` 의 R021/concurrency 가정을 그대로 옮겨 적고, 확인/취소 두 경로의
부수효과 차이를 명시할 것.
</output>
