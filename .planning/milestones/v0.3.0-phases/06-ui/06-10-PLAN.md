---
phase: 06-ui
plan: 10
type: execute
wave: 2
depends_on: ["06-09"]
gap_closure: true
files_modified:
  - src/shared/mask.ts
  - src/shared/__tests__/mask.test.ts
  - src/main/services/auth-service.ts
  - src/main/services/__tests__/auth-service.test.ts
autonomous: true
requirements: [R020]

estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "렌더러로 돌아가는 실패 문구와 식별자는 마스킹을 통과한 값뿐이다 — 토큰·비밀번호·URL 쿼리스트링이 화면에 노출되지 않는다 (06-05-PLAN must_haves, R010)"
    - "키 이름이 앞에 붙지 않은 문맥 없는 JWT 형태 문자열도 마스킹된다 — 서버가 임의 문장 안에 토큰을 섞어 돌려줘도 원문이 화면이나 로그에 남지 않는다 (R010, 06-REVIEW WR-02)"
    - "자격증명 로그인에서 도달 가능한 모든 실패 반환이 buildFailureResult() 마스킹 관문을 거친다 — 관문을 우회하는 미분류 경로가 남지 않는다 (D-14, 06-REVIEW WR-03)"
    - "헤드리스 로그인 디버그 덤프가 사용자 이메일 원문을 로그 파일에 남기지 않는다 (R010, 06-REVIEW IN-02)"
    - "새 마스킹 규칙이 정상 진단 텍스트(도메인·파일 경로·버전 문자열·타임스탬프)를 훼손하지 않는다 — 관측성을 지키면서 노출만 줄인다"
  prohibitions:
    - "LoginFailureReason union 에 값을 추가하지 않는다 — 6가지 신호 계약을 건드리지 않는다"
    - "버튼 비활성 실패의 사용자 대면 문구를 바꾸지 않는다 — 마스킹 관문을 통과시키는 것이지 안내를 일반화해 후퇴시키는 것이 아니다"
    - "기존 mask.test.ts 테스트를 수정하거나 삭제하지 않는다 — 새 규칙은 기존 규칙의 동작을 바꾸지 않아야 한다"
    - "새 npm 의존성을 추가하지 않는다"
    - "실계정 로그인이나 Weverse 서버 접속으로 검증하지 않는다"
  artifacts:
    - src/shared/mask.ts
    - src/shared/__tests__/mask.test.ts
  key_links:
    - "`SENSITIVE_PATTERNS` ↔ `maskSensitive()` ↔ `buildFailureResult()` 와 `logService` 자동 마스킹 — 규칙 하나를 추가하면 두 소비자 모두에 즉시 반영된다"
    - "`btnEnabled` 실패 분기 ↔ `buildFailureResult()` — 이 연결이 없으면 관문 밖에서 렌더러로 나가는 반환 경로가 남는다"
    - "헤드리스 디버그 덤프의 필드 선택 ↔ 로그 파일 내용 — 무엇을 덤프에 담느냐가 곧 로그에 남는 것이다"
---

<objective>
gap 2 와 **같은 뿌리**를 가진 나머지 마스킹 관문 우회 경로 세 곳을 닫는다: 문맥 없는 토큰 형태
문자열이 규칙을 통과하는 구조적 한계(WR-02), 관문을 거치지 않는 7번째 미분류 실패 반환(WR-03),
그리고 사용자 이메일 원문을 로그에 남기는 헤드리스 디버그 덤프(IN-02).

Purpose: 06-09 는 `validateToken()` 이 렌더러로 원문을 흘리는 것을 구조적으로 막았다. 그러나
"`buildFailureResult()` 가 유일한 마스킹 관문" 이라는 이 phase 의 전제는 두 가지 방식으로 여전히
불완전하다 — ① 관문을 통과해도 규칙이 잡지 못하는 형태의 비밀이 있고(WR-02) ② 관문을 아예 거치지
않는 반환 경로가 하나 더 남아 있다(WR-03). CR-02 가 준 교훈은 "관문이 있다" 와 "모든 경로가 관문을
지난다" 는 다른 명제라는 것이며, 이 플랜은 그 차이를 실제로 없앤다.

Output: 문맥 무관 JWT 마스킹 규칙과 그 경계 테스트, 관문을 지나도록 재배선된 버튼 비활성 실패
경로, 이메일 원문을 담지 않는 디버그 덤프.
</objective>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| `SENSITIVE_PATTERNS` 의 문맥 무관 JWT 규칙 | 기존 배열에 신규 항목 1개 | `mask.ts` 최초의 **키 이름에 의존하지 않는** 규칙 |
| `mask.test.ts` 의 신규 describe | 테스트 확장 | 양성 케이스 + 훼손 방지(경계) 케이스 |
| `buildFailureResult()` 의 `overrideMessage` 파라미터 | 기존 private 메서드의 신규 선택 파라미터 | 확정 문구를 유지하면서 관문을 통과시키기 위한 자리. 기존 호출부는 전부 그대로 동작한다 |
| `auth-service.test.ts` 의 신규 테스트 | 테스트 확장 | 버튼 비활성 실패가 관문을 지난다는 단언 + 디버그 덤프 필드 단언 |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-REVIEW.md
@.planning/phases/06-ui/06-VERIFICATION.md
@.planning/phases/06-ui/06-05-SUMMARY.md
@.planning/phases/06-ui/06-09-SUMMARY.md
</context>

<review_disposition>
06-REVIEW.md Warning/Info 중 이 플랜이 판정한 항목. 이로써 06-REVIEW.md 의 10개 발견(Critical 2 +
Warning 4 + Info 4)이 **전부** 세 플랜 중 한 곳에서 명시적으로 처리되거나 근거와 함께 미뤄졌다.

| ID | 판정 | 근거 |
|---|---|---|
| WR-02 (`maskSensitive()` 가 `key: value` 패턴만 매칭) | **SCOPE IN — Task 1** | gap 2 와 뿌리를 공유한다. 더 직접적으로는, 06-09 가 검증자의 "원문을 `maskSensitive()` 로 감싸라" 제안을 **기각한 근거가 바로 이 결함**이다. 기각 근거를 남긴 채 그 결함을 방치하면 다음 사람이 같은 최소 조치를 다시 제안하게 된다. `buildFailureResult()` 의 `identifier` 는 여전히 서버가 준 임의 문자열(`ApiAuthError.message`)을 담으므로 이 구멍은 열린 채다 |
| WR-03 (버튼 비활성 실패가 `buildFailureResult()` 를 완전히 우회) | **SCOPE IN — Task 2** | "관문을 거치지 않고 렌더러로 나간다" 는 CR-02 와 **문자 그대로 같은 결함 패턴**이 두 번째 위치에 있는 것이다. 현재 문구 자체에는 민감정보가 없지만, 리뷰가 지적했듯 이 분기는 이미 페이지 상태 덤프를 만들고 있어 그 정보가 문구로 새어 들어가기 가장 쉬운 자리다. 같은 파일·같은 뿌리이므로 비용이 낮다 |
| IN-02 (헤드리스 디버그 덤프가 이메일 평문을 로그에 남김) | **SCOPE IN — Task 2** | WR-03 과 **같은 코드 블록**(버튼 비활성 분기)에 있다. R010 은 개인정보 로그 마스킹 요구사항이고 이메일은 그 대상이다. 바로 옆 293행이 이미 길이만 담는 관용구를 쓰고 있어 고칠 형태가 코드 안에 이미 존재한다. 이 phase 가 바로 이 DOM 폴링 영역을 손댔음에도 인접 지점을 남겼다는 리뷰의 지적이 정확하다 |

06-REVIEW.md 발견 처리 현황 총괄:
- CR-01 → 06-08 (해소)
- CR-02 → 06-09 (해소)
- WR-01 → 06-08 (해소) · IN-03 → 06-08 (부분 — 문서화만, 채널 신설은 범위 밖) · IN-04 → 06-08 (DEFER, UAT #2 로)
- WR-04 → 06-09 (DEFER, Phase 07) · IN-01 → 06-09 (DEFER, Phase 07)
- WR-02 · WR-03 · IN-02 → 이 플랜 (해소)
</review_disposition>

<tracer_rationale>
선행 트레이서를 만들지 않는다. 이 플랜은 이미 배선이 끝난 경로 안의 규칙 하나와 분기 하나를
고치는 국소 수정이며, 관통할 미증명 레이어가 없다. 두 태스크 모두 기존 테스트 파일 위에서
TDD 로 진행한다.
</tracer_rationale>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 문맥 무관 JWT 형태 마스킹 규칙을 추가하고, 정상 진단 텍스트를 훼손하지 않음을 경계 테스트로 고정한다 (WR-02)</name>
  <files>src/shared/mask.ts, src/shared/__tests__/mask.test.ts</files>
  <read_first>
    - src/shared/mask.ts (전체 — `SENSITIVE_PATTERNS` 배열 전부와 각 규칙의 주석. 특히 T-05-17 로 추가된 snake_case URL 쿼리 규칙이 왜 종결자에 `&`/`#` 를 포함해야 했는지, 그리고 각 규칙이 왜 "키 뒤에 곧바로 `:`/`=` 가 오는 경우만" 매칭하도록 좁혀졌는지)
    - src/shared/__tests__/mask.test.ts (전체 — 기존 테스트의 구성. 새 규칙이 이들 중 어느 것도 깨뜨리지 않아야 한다)
    - src/main/services/auth-service.ts (440-511행 `buildFailureResult()` — `identifier` 에 실리는 값의 출처가 서버가 준 임의 문자열이라는 사실을 코드에서 확인할 것)
    - src/main/services/api-auth-client.ts (`ApiAuthError` 가 만들어지는 지점 — 서버 JSON `message` 필드가 그대로 예외 메시지가 되는 경로)
    - .planning/phases/06-ui/06-REVIEW.md (§WR-02 — 결함 서술과 제안된 정규식)
  </read_first>
  <behavior>
    양성(마스킹돼야 함):
    - Test 1: 키 이름 접두사 **없이** 문장 한가운데 놓인 3분절 JWT 형태 문자열이 원문 그대로
      남지 않는다. 예: 서버가 이미 사용된 토큰이라며 토큰을 문장에 섞어 돌려주는 형태.
    - Test 2: 같은 문장에 JWT 형태 문자열이 두 개 있으면 둘 다 마스킹된다(전역 치환).
    - Test 3: 기존 `accessToken=` 형태는 지금과 **동일하게** 마스킹된다 — 새 규칙이 기존 규칙의
      결과를 이중으로 훼손하지 않는다.

    음성(훼손되면 안 됨 — 이 그룹이 이 태스크의 진짜 리스크다):
    - Test 4: 도메인 문자열이 그대로 남는다 (예: 이 저장소가 실제로 로그에 남기는 계정 API 호스트).
    - Test 5: 점이 들어간 파일 경로와 모듈 경로가 그대로 남는다.
    - Test 6: 시맨틱 버전 문자열이 그대로 남는다.
    - Test 7: 이 저장소의 실제 진단 로그 문장 하나를 통째로 넣어도 한 글자도 바뀌지 않는다
      (`auth-service.ts` 의 토큰 shape 진단 라인처럼 길이·분절 수만 담는 형태).
  </behavior>
  <action>
    `src/shared/mask.ts` 의 `SENSITIVE_PATTERNS` 배열 **맨 끝**에 규칙 하나를 추가한다. 이 규칙은
    기존 규칙들과 달리 키 이름에 의존하지 않고 **JWT 의 구조 자체**(base64url 문자 집합의 3분절이
    점으로 이어진 형태)를 매칭한다. 각 분절에 최소 길이 하한을 두어 일반 식별자·도메인·파일
    경로가 우연히 걸리지 않게 한다. 치환은 기존 규칙들과 같이 `maskToken()` 을 쓴다.

    배열 맨 끝에 두는 이유: 앞선 키-값 규칙들이 먼저 돌아 이미 마스킹된 자리는 `maskToken()` 의
    생략 표기가 들어가 3분절 형태가 아니게 되므로, 이 규칙이 그 결과를 다시 건드리지 않는다.
    이 순서 의존성을 주석으로 명시한다.

    주석에 반드시 남길 것: 이 규칙이 06-REVIEW WR-02 의 처치이며, 기존 규칙 전부가 키 이름 문맥에
    의존해 서버가 준 임의 문자열 안의 토큰을 놓친다는 것, 그리고 **이 규칙조차 완전하지 않다**는
    것 — 토큰이 JWT 구조가 아니면 여전히 통과한다. 따라서 06-09 가 택한 "서버 텍스트를 사용자
    문구 경로에 아예 넣지 않는다" 는 구조적 처치가 1차 방어선이고 이 규칙은 2차 방어선이다.
    이 서열을 주석에 못 박아, 다음 사람이 이 규칙을 근거로 원문 전달을 다시 허용하지 않게 한다.

    `src/shared/__tests__/mask.test.ts` 에 새 describe 블록으로 `<behavior>` Test 1~7 을 추가한다.
    음성 케이스(Test 4~7)를 양성 케이스와 같은 비중으로 쓴다 — 이 규칙의 실패 모드는 "못 잡는
    것" 보다 "정상 로그를 훼손하는 것" 이 관측하기 어렵고 더 나쁘다. 기존 테스트는 한 줄도
    수정하지 않는다.

    구현 후 전체 스위트를 돌려 기존 테스트 중 하나라도 깨지면 그것은 새 규칙이 너무 넓다는
    신호다 — 테스트를 고치지 말고 정규식의 하한을 올릴 것.
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/mask.test.ts</automated>
    <automated>npm test</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run typecheck:main</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/__tests__/mask.test.ts` 가 0 종료 코드로 끝나고 새 describe 의
      7개 테스트가 전부 통과한다.
    - `git diff --numstat HEAD~1 -- src/shared/__tests__/mask.test.ts` 의 삭제 줄 수가 0 이다 —
      기존 테스트 무수정.
    - `npm test` 가 0 실패로 끝나고 총 테스트 수가 315보다 크다.
    - `grep -vE '^\s*(//|\*|/\*)' src/shared/mask.ts | grep -c 'SENSITIVE_PATTERNS'` == 2 —
      배열 선언과 `maskSensitive()` 안의 순회, 두 곳뿐이다(규칙 배열이 분기되지 않았다).
    - 새 규칙이 배열의 마지막 항목이다: `awk '/SENSITIVE_PATTERNS/,/^\];/' src/shared/mask.ts | grep -n 'maskToken' | tail -1` 의 결과가 새로 추가한 규칙 줄을 가리킨다.
    - `npm run typecheck` 와 `npm run typecheck:main` 이 각각 0 에러다.
    - `git diff --stat HEAD~1 -- package.json package-lock.json` 이 공백이다.
  </acceptance_criteria>
  <reversibility rating="costly">`maskSensitive()` 는 렌더러 반환 문구와 **모든 로그 라인**이 함께 지나는 전역 관문이다. 너무 넓은 정규식은 정상 진단 텍스트를 조용히 훼손하고, 훼손된 로그는 사후에 복원할 수 없다. 규칙 자체를 되돌리는 것은 한 줄이지만 그 사이에 기록된 로그는 되돌아오지 않는다 — 그래서 음성(훼손 방지) 케이스를 양성 케이스와 같은 비중으로 요구한다.</reversibility>
  <done>키 이름 접두사 없이 문장에 섞인 JWT 형태 문자열이 마스킹되고, 도메인·경로·버전·기존 진단 로그는 한 글자도 바뀌지 않는다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 버튼 비활성 실패를 마스킹 관문에 태우고, 디버그 덤프에서 이메일 원문을 제거한다 (WR-03 · IN-02)</name>
  <files>src/main/services/auth-service.ts, src/main/services/__tests__/auth-service.test.ts</files>
  <read_first>
    - src/main/services/auth-service.ts (290-340행 — DOM 폴링 버튼 활성 대기, 버튼 비활성 실패 분기, 디버그 덤프 `executeJavaScript` 템플릿. 293행 부근에 **길이만 담는 기존 관용구**가 이미 있다는 점을 확인할 것. 이어서 440-511행 `buildFailureResult()` 와 445-460행의 형제 분기들이 어떻게 `_emit` 하는지)
    - src/main/services/__tests__/auth-service.test.ts (538-625행 `asFailureBuilders` 캐스팅 관용구와 `buildFailureResult` describe — 새 테스트가 붙을 자리와 형태)
    - src/shared/login-failure.ts (`mapLoginFailure()` 의 `unknown` 분기 — override 문구를 쓸 때 어떤 사유로 분류되는지)
    - .planning/phases/06-ui/06-REVIEW.md (§WR-03 과 §IN-02 — 두 결함 서술)
    - .planning/phases/06-ui/06-UI-SPEC.md (§Copywriting Contract — 문구를 바꾸지 않아야 하는 근거)
  </read_first>
  <behavior>
    - Test 1: `buildFailureResult()` 에 확정 문구를 override 로 넘기면 반환된 `message` 가 그
      문구와 같고, `reason` 이 채워져 있다(정의되지 않은 채로 렌더러에 나가지 않는다).
    - Test 2: override 문구에 토큰 형태 문자열이 섞여 있으면 반환된 `message` 에 원문이 남지 않는다
      — override 경로도 마스킹 관문을 지난다. 픽스처는 Task 1 이 추가한 규칙이 잡는 형태와 기존
      키-값 형태 **둘 다** 쓴다.
    - Test 3: override 를 넘기지 않은 기존 호출은 지금과 완전히 동일한 결과를 낸다 — 기존 6가지
      사유 매핑이 영향을 받지 않는다.
    - Test 4 (IN-02, 소스 수준 단언): 디버그 덤프 템플릿이 이메일 입력의 **값**을 담지 않고 길이만
      담는다. 실제 `executeJavaScript` 를 돌릴 수 없으므로 템플릿 문자열에 대한 grep/정규식 단언
      으로 고정한다 — 이 저장소가 이미 다른 곳에서 쓰는 소스 수준 검증 방식이다.
  </behavior>
  <action>
    <!-- planner-discipline-allow: emailValue -->

    (1) **WR-03 — 관문 통과.** `buildFailureResult()` 에 네 번째 선택 파라미터
    `overrideMessage?: string` 을 추가한다. 값이 주어지면 `mapLoginFailure()` 가 돌려준 문구 대신
    이 문자열을 쓰되, 기존 문구와 **완전히 동일하게** `maskSensitive()` 를 통과시킨 뒤 결과의
    `message` 에 넣는다. 나머지 동작(사유 결정, `logDetail` 처리, `identifier` 마스킹)은 그대로다.
    기존 세 파라미터 호출부는 한 곳도 바꾸지 않는다 — 선택 파라미터 추가는 후방 호환이다.
    JSDoc 에 이 파라미터가 "확정된 사용자 문구가 이미 있는 분기를 안내를 후퇴시키지 않고 관문에
    태우기 위한 자리" 임을 명시한다.

    이어서 버튼 비활성 실패 분기(312-332행 부근)가 객체 리터럴을 직접 `return` 하는 대신
    `buildFailureResult(null, "unknown", undefined, <현재 문구>)` 형태로 결과를 만들어 반환하게
    바꾼다. **현재의 사용자 대면 문구는 글자 그대로 유지한다** — 이 태스크의 목적은 관문 통과이지
    안내를 일반화해 후퇴시키는 것이 아니다. `LoginFailureReason` union 에는 새 값을 추가하지
    않는다(6가지 신호 계약 불변).

    같은 분기에 형제 분기들(445-460행 부근)과 동일한 형태의 `login-failed` 이벤트 발행을 추가한다
    — 지금은 이 분기만 이벤트를 내지 않아 상단 배너와 인라인 오류가 서로 다른 정보를 갖는다.
    `cleanupHeadless()` 호출 순서는 형제 분기들의 순서를 따른다.

    (2) **IN-02 — 이메일 원문 제거.** 같은 분기의 디버그 덤프 `executeJavaScript` 템플릿에서
    이메일 입력의 값을 담는 필드를 **길이를 담는 필드로 교체**한다. 바로 위 293행 부근이 이미
    같은 관용구(입력값 대신 길이)를 쓰고 있으므로 그 이름 규칙과 형태를 그대로 따른다. 비밀번호
    쪽은 이미 길이만 담고 있으므로 건드리지 않는다. 나머지 덤프 필드(버튼 발견 여부, 버튼 비활성
    여부, 현재 URL)는 유지한다.

    (3) `src/main/services/__tests__/auth-service.test.ts` 에 `<behavior>` Test 1~4 를 추가한다.
    Test 1~3 은 기존 `buildFailureResult` describe 안에 나란히 붙이고, Test 4 는 소스 파일을 읽어
    템플릿을 단언하는 별도 케이스로 둔다. 기존 테스트는 한 줄도 수정하지 않는다.
  </action>
  <verify>
    <automated>npx vitest run src/main/services/__tests__/auth-service.test.ts</automated>
    <automated>grep -c 'emailValue' src/main/services/auth-service.ts</automated>
    <automated>npm test</automated>
    <automated>npm run typecheck:main</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'emailValue' src/main/services/auth-service.ts` == 0 — 이메일 원문 필드가 사라졌다.
    - `awk '/if \(!btnEnabled\)/,/^      \}$/' src/main/services/auth-service.ts | grep -c 'emailLen'` >= 1
      — 길이 필드로 교체됐다(정확한 이름은 293행 부근의 기존 관용구를 따른다).
    - `awk '/if \(!btnEnabled\)/,/^      \}$/' src/main/services/auth-service.ts | grep -c 'this.buildFailureResult('` == 1
      — 이 분기가 관문을 지난다.
    - `awk '/if \(!btnEnabled\)/,/^      \}$/' src/main/services/auth-service.ts | grep -c 'success: false'` == 0
      — 객체 리터럴을 직접 만들어 반환하지 않는다.
    - `awk '/if \(!btnEnabled\)/,/^      \}$/' src/main/services/auth-service.ts | grep -c 'this._emit('` == 1
      — 형제 분기들과 동일하게 이벤트를 발행한다.
    - 사용자 대면 문구가 보존됐다: `grep -c '로그인 버튼이 활성화되지 않았습니다' src/main/services/auth-service.ts` == 1
    - `git diff HEAD~1 -- src/shared/login-failure.ts` 가 공백이다 — 6가지 신호 계약 무변경.
    - `git diff --numstat HEAD~1 -- src/main/services/__tests__/auth-service.test.ts` 의 삭제 줄 수가 0 이다.
    - `npx vitest run src/main/services/__tests__/auth-service.test.ts` 가 0 종료 코드로 끝난다.
    - `npm test` 가 0 실패로 끝나고 총 테스트 수가 315보다 크다.
    - `npm run typecheck:main` 과 `npm run typecheck` 가 각각 0 에러다.
    - `npm run build` 가 0 으로 종료한다.
    - `git diff --stat HEAD~1 -- package.json package-lock.json` 이 공백이다.
  </acceptance_criteria>
  <reversibility rating="reversible">선택 파라미터 추가(후방 호환)와 한 분기의 재배선, 덤프 필드 한 개 교체. 되돌림이 국소적이다.</reversibility>
  <done>자격증명 로그인에서 도달 가능한 실패 반환 중 마스킹 관문을 우회하는 것이 남아 있지 않고, 헤드리스 디버그 덤프가 사용자 이메일 원문을 로그에 남기지 않는다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 서버가 준 임의 문자열(`ApiAuthError.message`) → `identifier` → 렌더러 화면 | 구조를 가정할 수 없는 텍스트가 마스킹 규칙의 완전성에만 의존해 화면에 도달하는 경로 |
| Weverse 로그인 페이지 DOM → 디버그 덤프 → 로그 파일 | 사용자가 입력한 개인정보가 진단 목적으로 수집돼 디스크에 남는 경로 |
| `maskSensitive()` → 모든 로그 라인 | 전역 관문. 규칙이 넓으면 정상 진단 텍스트를 조용히 훼손한다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-39 | Information Disclosure | 문맥 없는 JWT 형태 문자열이 키-값 규칙을 통과해 화면·로그에 원문으로 남음 (WR-02) | medium | mitigate | Task 1 이 구조 기반 규칙을 2차 방어선으로 추가한다. 1차 방어선은 06-09 가 세운 "서버 텍스트를 사용자 문구 경로에 넣지 않는다" 이며, 주석이 이 서열을 못 박는다 |
| T-06-40 | Information Disclosure | 버튼 비활성 실패 분기가 마스킹 관문을 우회하는 7번째 경로로 남아, 향후 진단 정보가 문구에 추가되면 새 유출 진입점이 됨 (WR-03) | medium | mitigate | Task 2 가 이 분기를 `buildFailureResult()` 에 태운다. 문구는 보존하고 관문만 추가한다 |
| T-06-41 | Information Disclosure | 헤드리스 디버그 덤프가 사용자 이메일 평문을 로그 파일에 남김 (IN-02, R010) | medium | mitigate | Task 2 가 값 필드를 길이 필드로 교체한다. 바로 위에 이미 존재하는 관용구를 따르므로 새 설계가 필요 없다 |
| T-06-42 | Tampering (진단 손상) | 과도하게 넓은 JWT 정규식이 도메인·파일 경로·버전 문자열 등 정상 진단 텍스트를 훼손해 관측성을 잃음 | medium | mitigate | Task 1 의 음성 케이스 4건(Test 4~7)을 양성 케이스와 같은 비중으로 요구하고, 기존 스위트 무수정 통과를 acceptance criterion 으로 강제한다. 기존 테스트가 깨지면 테스트가 아니라 정규식을 좁힌다 |
| T-06-43 | Repudiation | 2차 방어선의 존재를 근거로 다음 사람이 원문 전달을 다시 허용함 | medium | mitigate | Task 1 의 주석이 1차/2차 방어선의 서열과 이 규칙의 불완전성(JWT 구조가 아닌 토큰은 여전히 통과)을 명시한다 |
| T-06-SC | Tampering | npm/pip/cargo 설치 | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — `06-RESEARCH.md` § Package Legitimacy Audit 신규 의존성 0건, `[ASSUMED]`/`[SUS]` 없음. 두 태스크 모두 `package.json`/`package-lock.json` 무변경을 acceptance criterion 으로 강제한다 |

블로킹 임계값: high (ASVS L1). 이 플랜의 코드 위협은 전부 medium — 임계값 아래이지만 gap 2 와
뿌리를 공유하고 변경 비용이 낮아 미루지 않고 닫는다. T-06-SC 만 high 이며 무설치로 해소된다.
</threat_model>

<verification>
- `npx vitest run src/shared/__tests__/mask.test.ts` — 기존 + 신규 7개 전부 통과
- `npx vitest run src/main/services/__tests__/auth-service.test.ts` — 기존 + 신규 4개 전부 통과
- `npm test` — 전체 스위트 0 실패, 총 테스트 수 > 315
- `npm run typecheck` / `npm run typecheck:main` — 각 0 에러
- `npm run build` — 0 종료
- `grep -c 'emailValue' src/main/services/auth-service.ts` — 0
- `grep -c '로그인 버튼이 활성화되지 않았습니다' src/main/services/auth-service.ts` — 1
- `git diff --stat 06-10-시작-커밋 -- package.json package-lock.json` — 공백

**실행자가 하지 말아야 할 것:** 실계정 로그인, Weverse 서버 접속. 이 플랜의 모든 검증은 단위
테스트 · 소스 수준 단언 · 타입체크 · 빌드로 닫힌다(05-CONTEXT D-06).
</verification>

<success_criteria>
1. 키 이름 접두사 없이 문장에 섞인 JWT 형태 문자열이 마스킹되고, 도메인·파일 경로·버전 문자열·
   기존 진단 로그 문장은 한 글자도 바뀌지 않는다.
2. 자격증명 로그인에서 도달 가능한 모든 실패 반환이 `buildFailureResult()` 를 지난다 —
   관문 밖에서 렌더러로 나가는 경로가 남지 않는다.
3. 버튼 비활성 실패의 사용자 대면 문구가 그대로 보존됐고, 형제 분기들과 동일하게 이벤트를 낸다.
4. 헤드리스 디버그 덤프에 사용자 이메일 원문이 없다.
5. 6가지 실패 신호 계약(`LoginFailureReason`)과 기존 테스트가 전부 무변경이다.
6. 신규 의존성 0건, 전체 스위트 green, 타입체크 2종 0 에러, 빌드 0 종료.
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-10-SUMMARY.md` when done.

SUMMARY 에 반드시 남길 것:
- 새 JWT 규정규식의 최종 형태와, 하한을 그 값으로 정한 근거(어떤 음성 케이스가 그 값을 강제했는지).
- 기존 스위트를 돌렸을 때 훼손된 케이스가 있었는지 — 있었다면 정규식을 어떻게 좁혔는지.
- 버튼 비활성 실패의 사용자 문구가 바뀌지 않았다는 확인(변경 전후 문자열 인용).
- 이 플랜으로 06-REVIEW.md 10개 발견의 처리 현황이 어떻게 마감됐는지 — `<review_disposition>`
  의 총괄 표를 그대로 옮길 것.
</output>
