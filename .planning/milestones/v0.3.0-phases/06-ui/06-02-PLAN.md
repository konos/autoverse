---
phase: 06-ui
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/shared/login-failure.ts
  - src/shared/__tests__/login-failure.test.ts
autonomous: true
requirements: [R020]

estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "캡차 신호는 메일을 기다리게 하는 안내가 아니라 '보안 확인 때문에 앱 안 로그인 불가 + 브라우저 로그인 사용' 설명으로 매핑된다 (D-13, R020)"
    - "매핑되지 않은 실패도 사용자가 다음 행동을 알 수 있는 한국어 문장과 식별자를 함께 받는다 — 조용히 무시되는 실패 경로가 없다 (D-14)"
    - "실패 사유 union 에 새 값이 추가되면 exhaustive switch 가 컴파일 타임에 누락을 잡는다"
    - "Weverse 폼이 표시한 오류 문구는 trim 후 120자를 넘으면 말줄임표와 함께 잘려 반환되고, 잘리지 않은 원문은 로그 경로로 따로 간다 (UI-SPEC E5 overflow)"
    - "매핑된 사유(캡차·타임아웃)는 식별자 없이 문장만 반환한다 — 식별자 자리에 정의되지 않은 값이 찍히는 렌더링이 불가능하다 (UI-SPEC E5 partial)"
    - "120자 상한이 이 영역의 유일한 길이 계약이며 다른 상한을 별도로 도입하지 않는다 (UI-SPEC E5 long-text)"
  prohibitions:
    - "실패 안내 문구에 2026-08-25 HAR 실측으로 반증된 이메일 일회용 코드 서사를 재도입하지 않는다"
    - "렌더러로 돌아가는 실패 문구에 마스킹을 거치지 않은 원문 식별자를 넣지 않는다"
    - "이 순수 모듈에서 electron 이나 main 프로세스 모듈을 import 하지 않는다"
  artifacts:
    - src/shared/login-failure.ts
    - src/shared/__tests__/login-failure.test.ts
  key_links:
    - "`LoginFailureReason` union ↔ auth-service 의 DOM 폴링 반환값 ↔ `CredentialLoginResult.reason` — 세 곳이 같은 어휘를 쓰지 않으면 06-05 의 배선이 성립하지 않는다"
    - "`LoginFailureGuidance.suggestBrowserSwitch` ↔ LoginPanel 의 '브라우저 로그인으로 전환' 버튼 노출 조건 (06-06)"
---

<objective>
`credentialLogin()` 이 실제로 마주치는 실패 신호를 타입드 사유로 고정하고, 각 사유를
`06-UI-SPEC.md` 가 확정한 한국어 안내로 옮기는 순수 함수를 만든다.

Purpose: R020 의 실체다. 이 모듈이 없으면 캡차 오진(D-13)을 고칠 자리가 없고, 실패 안내가
auth-service 곳곳의 `if` 로 흩어져 새 사유가 생길 때마다 누락된다.
Output: `src/shared/login-failure.ts` + 사유 전수 테스트.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-UI-SPEC.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: 실패 사유 union + 한국어 안내 매핑 (exhaustive switch)</name>
  <files>src/shared/login-failure.ts, src/shared/__tests__/login-failure.test.ts</files>
  <read_first>
    - src/shared/form-parser.ts (1-20줄 — shared 순수 모듈의 인터페이스 결과 반환 관례: `XxxResult` 인터페이스 + 단일 export 함수)
    - src/shared/mask.ts (전체 75줄 — `maskSensitive()` 의 시그니처와, 이 모듈이 그것을 *직접 호출하지 않는* 이유를 이해하기 위해)
    - .planning/phases/06-ui/06-UI-SPEC.md (§Copywriting Contract "Failure reason → copy" 표 — 문구의 정본. 한 글자도 바꾸지 말 것)
    - .planning/phases/06-ui/06-PATTERNS.md (§`src/shared/login-failure.ts` — 이 파일에 적용할 패턴 지정)
    - .planning/phases/06-ui/06-RESEARCH.md (§Architecture Patterns Pattern 4 — 시작 골격)
  </read_first>
  <behavior>
    - `mapLoginFailure("captcha")` → `message` 가 UI-SPEC 캡차 문구와 정확히 일치하고 `suggestBrowserSwitch === true`, `identifier` 는 undefined
    - `mapLoginFailure("timeout")` → UI-SPEC 타임아웃 문구, `suggestBrowserSwitch === true`, `identifier` 는 undefined
    - `mapLoginFailure("form-error", "비밀번호가 올바르지 않습니다.")` → 전달된 텍스트를 그대로 message 로 반환, `suggestBrowserSwitch === false`
    - `mapLoginFailure("form-error", undefined)` → 빈 화면이 되지 않도록 일반 실패 문장으로 폴백
    - `mapLoginFailure("network-error", "ECONNRESET")` → UI-SPEC 네트워크 문구 + `identifier === "ECONNRESET"`
    - `mapLoginFailure("token-ladder-failed", "ApiAuthError: rung1")` → UI-SPEC 사다리 문구 + identifier 보존
    - `mapLoginFailure("unknown", "x")` → UI-SPEC 미매핑 문구 + identifier 보존
    - 모든 사유에 대해 `message` 가 비어 있지 않다 (union 전수 반복 테스트)
    - 모든 사유에 대해 반환 문구에 반증된 이메일 코드 서사가 등장하지 않는다 (전수 부정 단언)
  </behavior>
  <action>
    RED → GREEN → REFACTOR 로 진행한다. 먼저 `src/shared/__tests__/login-failure.test.ts` 를 작성해
    실패시키고, 그 다음 `src/shared/login-failure.ts` 를 구현한다.

    모듈 구성은 `form-parser.ts` 관례를 따른다 — electron import 없음, `./types` 에서만 타입을
    가져오고, union 타입 + 결과 인터페이스 + 단일 export 함수 형태다.

    `LoginFailureReason` union 의 값은 정확히 6개다:
    `"captcha"`, `"form-error"`, `"timeout"`, `"network-error"`, `"token-ladder-failed"`, `"unknown"`.
    이 어휘는 06-05 가 `auth-service.ts` 의 DOM 폴링 결과와 `CredentialLoginResult.reason` 에
    그대로 쓸 계약이므로 이름을 임의로 바꾸지 말 것.

    `LoginFailureGuidance` 는 `{ message: string; suggestBrowserSwitch: boolean; identifier?: string }` 다.

    `mapLoginFailure(reason: LoginFailureReason, detail?: string): LoginFailureGuidance` 는 union 에
    대한 exhaustive `switch` 를 쓴다(default 로 뭉개지 말 것 — 새 사유 추가 시 컴파일 에러가 나는
    것이 이 설계의 목적이다. `unknown` 은 명시적 case 로 둔다).

    문구는 `06-UI-SPEC.md` § Copywriting Contract 의 "Failure reason → copy" 표에서 **그대로 복사**
    한다. RESEARCH.md Pattern 4 에도 비슷한 문구가 있으나 UI-SPEC 이 나중이고 체커 승인을 받은
    정본이므로 충돌 시 UI-SPEC 을 따른다. 특히 캡차 사유는 D-13 이 고치려는 바로 그 오진 지점이므로,
    2026-08-25 HAR 실측으로 반증된 "메일로 코드가 발송됐다"는 서사를 어떤 형태로도 재도입하지 않는다
    (호출 0건 — 사용자를 오지 않을 메일 앞에 앉히게 된다).

    `identifier` 는 `network-error` / `token-ladder-failed` / `unknown` 세 사유에서만 채운다.
    `captcha` / `timeout` / `form-error` 는 `identifier` 를 아예 넣지 않는다(속성 부재 = 칩 미표시,
    UI-SPEC E5 partial). **이 모듈은 마스킹을 직접 하지 않는다** — 마스킹은 호출부(06-05 의
    `auth-service.ts`)의 책임이며, 그 이유(렌더러로 반환되는 필드는 로그 자동 마스킹 경로를 타지
    않는다)를 파일 머리 주석에 남긴다.
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/login-failure.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/__tests__/login-failure.test.ts` 가 0 으로 종료한다
    - `grep -c "export type LoginFailureReason" src/shared/login-failure.ts` 가 1 이다
    - `grep -c "export function mapLoginFailure" src/shared/login-failure.ts` 가 1 이다
    - `grep -c "electron" src/shared/login-failure.ts` 가 0 이다
    - `grep -c "maskSensitive" src/shared/login-failure.ts` 가 0 이다 (마스킹은 호출부 책임)
    - `grep -vE '^\s*(//|\*|/\*)' src/shared/login-failure.ts | grep -c "OTP"` 가 0 이다
    - `grep -c "Weverse가 보안 확인을 요구해" src/shared/login-failure.ts` 가 1 이다
    - 테스트 파일이 6개 사유를 모두 명시적으로 호출한다: `grep -cE '"captcha"|"form-error"|"timeout"|"network-error"|"token-ladder-failed"|"unknown"' src/shared/__tests__/login-failure.test.ts` 가 6 이상이다
  </acceptance_criteria>
  <!-- planner-discipline-allow: OTP -->
  <!-- planner-discipline-allow: maskSensitive -->
  <reversibility rating="reversible">순수 함수 하나와 union 타입이며, 사유가 늘거나 문구가 바뀌어도 한 파일 안에서 끝난다.</reversibility>
  <done>6개 사유 전부가 UI-SPEC 확정 문구로 매핑되고, 캡차가 더 이상 메일 안내로 흐르지 않는다.</done>
</task>

<task type="tdd" tdd="true">
  <name>Task 2: 폼 오류 텍스트 전처리 — trim · 120자 상한 · 말줄임</name>
  <files>src/shared/login-failure.ts, src/shared/__tests__/login-failure.test.ts</files>
  <read_first>
    - src/shared/login-failure.ts (Task 1 결과)
    - src/shared/__tests__/login-failure.test.ts (Task 1 결과)
    - .planning/phases/06-ui/06-UI-SPEC.md (§Copywriting Contract "form-error" 행 + §UI Considerations E5 overflow/long-text)
  </read_first>
  <behavior>
    - `truncateFormError("  로그인 실패  ")` → `"로그인 실패"` (앞뒤 공백 제거, 길이 변화 없음)
    - 정확히 120자인 입력 → 그대로 반환하고 말줄임표가 붙지 않는다
    - 121자 입력 → 반환 길이가 121자보다 짧고 마지막 문자가 말줄임표다
    - 빈 문자열/공백만 있는 입력 → 폴백 문장을 쓰도록 빈 결과를 반환한다
    - `mapLoginFailure("form-error", <130자 텍스트>)` 의 `message` 가 잘린 형태다
    - `mapLoginFailure("form-error", <130자 텍스트>)` 가 원문을 별도 필드로 유실 없이 노출한다 (로그 경로용)
  </behavior>
  <action>
    Weverse 폼이 DOM 으로 표시한 오류 텍스트는 유일하게 동적인 사용자 문구다. UI-SPEC 이 정한
    전처리 3단계를 이 모듈 안에 고정한다: (1) `.trim()`, (2) 120자 초과 시 말줄임표 한 글자를 붙여
    자름, (3) 마스킹은 호출부에서 방어적으로 한 번 더 적용.

    `FORM_ERROR_MAX_LENGTH = 120` 상수와 `truncateFormError(text: string): string` 을 내보내고,
    `mapLoginFailure` 의 `form-error` case 가 이것을 사용하게 한다. 잘리지 않은 원문은 로그 패널로
    가야 하므로(UI-SPEC E5 overflow) `LoginFailureGuidance` 에 `logDetail?: string` 을 추가해
    원문을 그대로 담고, `message` 에는 잘린 값을 담는다 — 호출부가 `logDetail` 을 로그로,
    `message` 를 화면으로 보낸다.

    상한은 120자 하나뿐이다. 다른 사유에 별도의 길이 제한을 새로 도입하지 말 것(UI-SPEC E5
    long-text: 이 상한이 이 영역의 유일한 길이 계약이다).

    Task 1 과 동일하게 테스트 먼저 작성한다.
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/login-failure.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/__tests__/login-failure.test.ts` 가 0 으로 종료한다
    - `grep -c "FORM_ERROR_MAX_LENGTH = 120" src/shared/login-failure.ts` 가 1 이다
    - `grep -c "export function truncateFormError" src/shared/login-failure.ts` 가 1 이다
    - `grep -c "logDetail" src/shared/login-failure.ts` 가 1 이상이다
    - `npm run typecheck` 가 0 으로 종료한다
    - 테스트에 경계값 3종(119/120/121자 계열) 케이스가 존재한다
  </acceptance_criteria>
  <done>동적 오류 텍스트가 화면에서는 잘리고 로그에서는 온전히 남는다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (플랜 06-02 몫)

**신규 파일**
- `src/shared/login-failure.ts`
- `src/shared/__tests__/login-failure.test.ts`

**신규 심볼**
- `LoginFailureReason` (union: `captcha` / `form-error` / `timeout` / `network-error` / `token-ladder-failed` / `unknown`)
- `LoginFailureGuidance` (`message`, `suggestBrowserSwitch`, `identifier?`, `logDetail?`)
- `mapLoginFailure()`
- `truncateFormError()`
- `FORM_ERROR_MAX_LENGTH`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Weverse DOM → 앱 | 로그인 페이지가 렌더한 오류 텍스트는 외부 입력이다. 길이·내용 모두 신뢰하지 않는다 |
| 예외 메시지 → 사용자 화면 | 스택/URL 이 섞일 수 있는 문자열이 렌더러로 나가는 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-06 | Information Disclosure | `LoginFailureGuidance.identifier` 가 토큰/쿼리스트링을 담은 채 렌더러로 전달 | high | mitigate | 이 모듈은 identifier 를 가공하지 않고 그대로 통과시키며, **호출부가 마스킹을 적용하는 것이 계약**임을 파일 주석·prohibitions·06-05 의 태스크로 3중 고정. 06-05 에 마스킹 회귀 테스트가 있다 |
| T-06-07 | Denial of Service (UI) | 외부 DOM 텍스트가 비정상적으로 길어 화면을 밀어냄 | low | mitigate | `FORM_ERROR_MAX_LENGTH = 120` 상한 + 말줄임 (Task 2) |
| T-06-08 | Spoofing | Weverse 폼 텍스트를 그대로 통과시키면 외부 문자열이 앱 안내처럼 보임 | low | accept | 이 텍스트는 실제 로그인 폼이 표시한 내용이며 사용자에게 그대로 전달하는 것이 D-12 의 명시적 결정이다. 마스킹 + 길이 상한으로 피해 표면을 제한하고 수용한다 |
| T-06-SC | Tampering | npm/pip/cargo 설치 | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — `06-RESEARCH.md` § Package Legitimacy Audit 이 신규 의존성 0건 확인, `[ASSUMED]`/`[SUS]` 없음 |

블로킹 임계값: high. T-06-06 은 이 플랜의 계약 + 06-05 의 회귀 테스트로 완결된다.
</threat_model>

<verification>
- `npx vitest run src/shared/__tests__/login-failure.test.ts` 통과
- `npm test` 전체 통과 (신규 파일이 기존 스위트를 깨지 않음)
- `npm run typecheck` 통과
</verification>

<success_criteria>
- 6개 실패 사유가 전부 UI-SPEC 확정 한국어 문구로 매핑된다 (R020)
- 캡차 사유가 이메일 코드 서사를 전혀 포함하지 않는다 (D-13)
- 미매핑 실패도 다음 행동 + 식별자를 제공한다 (D-14)
- 동적 오류 텍스트가 120자에서 잘리고 원문은 로그용으로 보존된다
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-02-SUMMARY.md` when done.
확정된 `LoginFailureReason` 6개 값과 `LoginFailureGuidance` 필드 목록을 SUMMARY 에 명시할 것
(06-05·06-06 이 이 어휘에 배선된다).
</output>
