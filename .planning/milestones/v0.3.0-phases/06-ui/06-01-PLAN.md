---
phase: 06-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/main/services/settings-store.ts
  - src/main/services/__tests__/settings-store.test.ts
  - src/shared/api-mode-notice.ts
  - src/shared/__tests__/api-mode-notice.test.ts
  - src/main/login-mode.ts
  - src/main/__tests__/login-mode.test.ts
  - src/shared/types.ts
  - src/main/preload.ts
  - src/main/ipc-handlers.ts
  - src/renderer/App.tsx
  - src/renderer/components/LoginPanel.tsx
autonomous: true
requirements: [R016, R021]

estimate:
  tokens: 58000
  raw_tokens: 58000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "사용자가 로그인 방식 탭을 클릭하면 선택값이 userData/settings.json 에 즉시 기록되고, 앱을 재시작해도 같은 방식이 선택된 상태로 복원된다 (R016, D-04)"
    - "설정 파일이 없거나 손상된 JSON 이어도 앱은 예외를 던지지 않고 기본값 browser 로 동작하며 경고 로그만 남긴다 (D-05)"
    - "AUTOVERSE_LOGIN_MODE 가 설정되어 있으면 저장된 값과 무관하게 env 값이 적용되고 IPC 응답의 lockedByEnv 가 true 다 (D-06)"
    - "로그인된 상태에서 방식을 바꿔도 현재 세션(로그인 상태·토큰·fanId)이 유지되고 다음 로그인부터 새 방식이 적용된다 (D-07)"
    - "탭을 연속으로 빠르게 클릭해도 설정 파일은 항상 완전한 JSON 이며 마지막 클릭의 값이 남는다 — 부분 기록된 파일을 읽는 경로가 존재하지 않는다 (원자적 tmp+rename)"
    - "설정 저장이 실패하면 탭은 이전 선택으로 되돌아가고 기존 .error-message 슬롯에 저장 실패 안내가 표시된다 — 저장에 실패했는데 선택이 바뀐 것처럼 보이지 않는다 (UI-SPEC E1 error)"
    - "항상 정확히 한 개의 탭만 활성(.btn-primary)이고 나머지 탭은 .btn-secondary 다 (UI-SPEC E1 populated)"
    - "이메일과 비밀번호 중 하나라도 비어 있으면 로그인 버튼이 disabled 를 유지한다 — 탭 구조 변경 후에도 기존 가드가 그대로다 (UI-SPEC E6 empty)"
    - "로그인 진행 중에는 버튼 라벨이 '로그인 중...' 이 되고 aria-busy 가 유지된다 (UI-SPEC E6 loading)"
    - "자격증명 폼은 자체 오류 영역을 갖지 않고 실패 안내를 공용 .error-message 슬롯에 위임한다 (UI-SPEC E6 error)"
    - "한 필드만 채워진 상태도 빈 상태와 동일하게 로그인 버튼이 disabled 다 (UI-SPEC E6 partial)"
  prohibitions:
    - "설정 파일에 이메일·비밀번호·토큰 등 민감정보를 저장하지 않는다"
    - "이 phase 에서 새 npm 런타임 의존성을 추가하지 않는다"
    - "설정 파일 읽기·쓰기 실패를 조용히 삼키지 않는다 — 읽기는 경고 로그 + 기본값, 쓰기는 호출부까지 실패를 전파한다"
  artifacts:
    - src/main/services/settings-store.ts
    - src/main/services/__tests__/settings-store.test.ts
    - src/shared/api-mode-notice.ts
    - src/shared/__tests__/api-mode-notice.test.ts
  key_links:
    - "preload.ts 의 settings 네임스페이스 ↔ shared/types.ts 의 IpcApi.settings ↔ ipc-handlers.ts 의 채널 문자열 — 세 곳의 이름/시그니처가 일치해야 렌더러가 컴파일된다"
    - "ipc-handlers.ts 가 resolveLoginMode(process.env, 저장값) 형태로 env 와 영속 설정을 합류시키는 지점 — 여기가 D-06 우선순위의 유일한 결정 지점이다"
    - "App.tsx 의 loginMode/lockedByEnv 상태 ↔ LoginPanel props — 렌더러 탭의 소유권이 useState 에서 영속 설정으로 이전되는 경계"
---

<objective>
로그인 방식 선택값을 앱 재시작 후에도 유지되는 1급 설정으로 승격하고, 그 값이 흐르는
경로(디스크 → main → IPC → 렌더러 탭)를 끝에서 끝까지 배선한다.

Purpose: R016 의 "선택이 다음 실행에도 유지된다"는 이 phase 의 뼈대다. 이후 모든 플랜
(고지 모달, 실패 안내, 코드 제거)이 "현재 활성 모드"라는 단일 사실 위에 얹힌다.
Output: `settings-store.ts`(신규), `api-mode-notice.ts`(신규), `settings:*` IPC 4채널,
props 로 승격된 LoginPanel 탭, OTP 입력 화면이 제거된 LoginPanel.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-PATTERNS.md
@.planning/phases/06-ui/06-UI-SPEC.md
</context>

<assumption_delta_decision>
검출: `chosen` — 지금까지 로그인 모드는 `resolveLoginMode(env)` 가 **환경변수에서 파생하는 값**이었고,
이 phase 에서 **사용자가 고르고 앱이 기억하는 값**이 된다. 상수였던 것이 매개변수가 되는 전형적
전이다.

- **이제 1급이 되는 명사:** *사용자가 선택해 영속된 로그인 방식* (`settings.json`의 `loginMode`).
- **결정: promote.** 영속 설정을 1차 소스로 승격하고, `AUTOVERSE_LOGIN_MODE` 는 개발/QA 용
  **덮어쓰기 변형**으로 강등한다. add-alongside(둘 다 대등하게 유지)를 택하지 않은 이유는, 그럴 경우
  사용자가 고른 값과 실제 동작이 말없이 어긋나기 때문이다 — D-06 이 요구하는 "환경변수로 고정됨"
  표시가 바로 이 강등을 UI 에서 정직하게 드러내는 장치다.
- **불변식 테스트(채택):** 지원되는 모든 모드에 대해 `설정 저장 → 스냅샷 재조회` 가 라운드트립하고,
  env 가 걸려 있을 때만 저장값이 무시된다는 것을 Task 1 의 테스트가 전수로 확인한다. 미래에 누군가
  모드를 다시 env 단일 소스로 되돌리면 이 테스트가 즉시 빨개진다.
</assumption_delta_decision>

<planner_assumptions>
⚠ **FLAGGED ASSUMPTION — R016 / concurrency (edge probe: unresolved / unclassified)**
결정적 edge probe 가 R016 에 대해 "중단되거나 병렬로 실행되면 무엇이 보장되는가?"를 물었고
`unresolved` 로 남았다. 이 플랜은 다음을 **가정**하고 진행한다:

- 앱은 단일 Electron 인스턴스·단일 메인 창으로 실행되며, 설정 파일에 동시에 쓰는 두 번째 프로세스는
  없다. 따라서 파일 락은 도입하지 않는다.
- 중단(프로세스 종료·전원 차단) 내구성은 tmp 파일 + `renameSync` 의 원자성에만 의존한다. `fsync()` 는
  호출하지 않는다 — 손실 허용 가능한 로컬 UI 설정이라는 판단(06-RESEARCH.md Pattern 2).
- 이 가정이 틀리다면(예: 향후 다중 창/다중 인스턴스) 마지막 쓰기가 이기는 대신 값이 서로 덮어써질 수
  있다. 그 시점에 재검토할 것.

이 가정은 조용히 넘어가지 않고 SUMMARY 에 그대로 옮겨 적어야 한다.
</planner_assumptions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: 로그인 방식 선택 영속 종단 슬라이스 — 디스크에서 탭까지 한 경로</name>
  <files>src/main/services/settings-store.ts, src/main/services/__tests__/settings-store.test.ts, src/main/login-mode.ts, src/main/__tests__/login-mode.test.ts, src/shared/types.ts, src/main/preload.ts, src/main/ipc-handlers.ts, src/renderer/App.tsx, src/renderer/components/LoginPanel.tsx</files>
  <read_first>
    - src/main/services/profile-store.ts (파일 저장소 관례의 정본 — 클래스 + 모듈 싱글턴, getXPath() 헬퍼, existsSync 가드. 단 오류 정책은 정반대로 뒤집어야 한다)
    - src/main/services/__tests__/profile-store.test.ts (vi.hoisted 로 electron app/safeStorage 를 모킹하고 실제 임시 디렉토리에 쓰는 테스트 관례)
    - src/main/login-mode.ts (26줄 전체 — 확장할 원본 시그니처)
    - src/main/__tests__/login-mode.test.ts (32줄 전체 — 깨뜨리면 안 되는 6개 문자열 동등 단언)
    - src/main/preload.ts (51줄 전체 — 네임스페이스 4개의 형태)
    - src/main/ipc-handlers.ts (157줄 전체 — register/unregister 1:1 대칭)
    - src/shared/types.ts (225-298 — AuthEvent / CredentialLoginResult / IpcApi)
    - src/renderer/App.tsx (159줄 전체 — LoginPanel 마운트 지점과 auth 이벤트 구독)
    - src/renderer/components/LoginPanel.tsx (284줄 전체 — 탭·폼·OTP 화면의 현재 구조)
    - .planning/phases/06-ui/06-PATTERNS.md (각 파일의 정확한 패턴 지정)
  </read_first>
  <behavior>
    - `SettingsStore.getLoginMode()` — 파일 없음 → `"browser"`
    - `SettingsStore.setLoginMode("api")` 후 새 인스턴스로 `getLoginMode()` → `"api"` (재시작 라운드트립)
    - 손상된 JSON 파일 → `getLoginMode()` 가 던지지 않고 `"browser"` 반환, `logService.warn` 1회 호출
    - `loginMode` 필드가 알 수 없는 값(`"legacy"`)이면 `"browser"` 로 정규화
    - `setLoginMode()` 두 번 연속 호출 후 파일을 파싱하면 항상 유효 JSON 이고 마지막 값이 남는다
    - `setLoginMode()` 는 임시 경로에 쓴 뒤 `fs.renameSync` 로 교체한다 (스파이로 확인)
    - `getLoginModeSnapshot(env)` — env 미설정 + 저장값 `"api"` → `{ mode: "api", lockedByEnv: false }`
    - `getLoginModeSnapshot(env)` — env `"browser"` + 저장값 `"api"` → `{ mode: "browser", lockedByEnv: true }`
    - `resolveLoginMode({}, "api")` → `"api"` / `resolveLoginMode({}, undefined)` → `"browser"`
    - `isLoginModeLockedByEnv({ AUTOVERSE_LOGIN_MODE: "" })` → `false` (빈 문자열은 잠금이 아니다)
    - `isLoginModeLockedByEnv({ AUTOVERSE_LOGIN_MODE: "apix" })` → `true` (값이 있으면 오타여도 잠금)
    - 기존 6개 `resolveLoginMode(fakeEnv)` 단언은 인자 하나로 호출되며 그대로 통과한다
  </behavior>
  <action>
    이 태스크는 **트레이서**다. 모드 값 하나가 디스크에서 탭까지 실제로 흐르는 단일 경로를
    프로덕션 품질로 배선한다. 고지 모달·잠금 배지·실패 안내는 이 태스크에서 만들지 않는다.

    (1) `src/main/services/settings-store.ts` 신규 — `profile-store.ts` 의 구조(클래스 + 모듈
    싱글턴 `export const settingsStore`, `getSettingsPath()` 헬퍼, `fs.existsSync` 가드,
    `logService` 사용)를 그대로 미러링하되 **오류 정책을 반전**한다: `safeStorage` 를 쓰지 않고
    평문 JSON 이며(D-05), 읽기 실패·파싱 실패·스키마 불일치는 던지지 않고
    `logService.warn("SettingsStore", ...)` 후 기본값으로 폴백한다. 파일명 `settings.json`,
    저장 위치 `app.getPath("userData")`. 파일 형태는 `{ schemaVersion: 1, loginMode: "api" | "browser",
    apiModeNoticeAckedVersion: number | null }` 이고 기본값은 `loginMode: "browser"`,
    `apiModeNoticeAckedVersion: null`. 공개 메서드는 `getLoginMode()`, `setLoginMode(mode)`,
    `getLoginModeSnapshot(env = process.env)`, `getNoticeAck()`, `ackNotice(version)` 다.
    쓰기는 `06-RESEARCH.md` Pattern 2 의 `writeSettings()` — `fs.writeFileSync(tmpPath)` 후
    `fs.renameSync(tmpPath, filePath)` 원자적 교체(`fsync` 없음). **읽기와 달리 쓰기 실패는 삼키지
    않고 호출부로 전파**한다(Task 3 가 이 실패를 UI 로 끌어올린다).
    `getLoginModeSnapshot()` 은 `resolveLoginMode(env, this.getLoginMode())` 와
    `isLoginModeLockedByEnv(env)` 를 합류시켜 `{ mode, lockedByEnv }` 를 반환한다 — env 대 저장값
    우선순위가 결정되는 유일한 지점이며, 종단 경로를 테스트 한 번으로 관통시키는 이음매다.
    `getNoticeAck()`/`ackNotice()` 는 이 태스크에서 저장·조회만 구현하고 소비는 Task 2 가 맡는다.

    (2) `src/main/login-mode.ts` — `06-PATTERNS.md` 가 지정한 확장을 그대로 적용한다:
    `resolveLoginMode(env, persistedMode?)` 두 번째 선택적 매개변수 추가(env 가 `"api"` 면 우선,
    아니면 `persistedMode === "api"` 확인, 그 외 `"browser"`)와 별도 함수
    `isLoginModeLockedByEnv(env)`(값이 있고 trim 후 길이 > 0 이면 true). **반환 타입을 객체로 바꾸지
    말 것** — 기존 6개 테스트가 문자열 동등을 단언한다. 파일 상단 주석의 "Phase 06 이 영속 설정으로
    갈아끼울 수 있도록" 예고 문장을 실제 구현 사실로 갱신한다. `LoginMode` 타입은
    `src/shared/types.ts` 로 옮기고 이 파일은 거기서 import 해 동일 이름으로 재수출한다
    (의존 방향을 main → shared 로 유지; 기존 import 경로를 깨지 않기 위해 재수출은 필수).

    (3) `src/shared/types.ts` — `LoginMode = "api" | "browser"` 정의를 이 파일로 이전하고,
    `LoginModeSnapshot = { mode: LoginMode; lockedByEnv: boolean }`,
    `NoticeAckSnapshot = { ackedVersion: number | null; currentVersion: number }` 를 추가한다.
    `IpcApi` 에 `settings` 네임스페이스를 추가: `getLoginMode(): Promise<LoginModeSnapshot>`,
    `setLoginMode(mode: LoginMode): Promise<void>`, `getNoticeAck(): Promise<NoticeAckSnapshot>`,
    `ackNotice(version: number): Promise<void>`.

    (4) `src/main/preload.ts` — `profile` 네임스페이스와 동일한 형태로 `settings` 블록을 추가한다.
    채널 문자열은 `settings:get-login-mode`, `settings:set-login-mode`, `settings:get-notice-ack`,
    `settings:ack-notice` 다(CONTEXT 가 재량으로 남긴 이름을 여기서 확정 — 기존 `namespace:kebab-case`
    관례와 일치). 이 태스크에서 `auth` 네임스페이스는 건드리지 않는다.

    (5) `src/main/ipc-handlers.ts` — 위 4개 채널 핸들러를 기존 한 줄 위임 스타일로 등록하고,
    `unregisterIpcHandlers()` 에 대응하는 `ipcMain.removeHandler(...)` 4줄을 1:1 로 추가한다.
    `settings:get-login-mode` 는 `settingsStore.getLoginModeSnapshot()` 을 그대로 반환한다.
    `auth:credential-login` / `auth:submit-otp` 의 모드 분기는 이 태스크에서 **건드리지 않는다**
    (플랜 06-04 소관).

    (6) `src/renderer/App.tsx` — 마운트 시 `window.api.settings.getLoginMode()` 를 호출해
    `loginMode`/`lockedByEnv` 상태를 채우고, `onSetLoginMode(mode)` 핸들러에서
    `window.api.settings.setLoginMode(mode)` 를 await 한 뒤 로컬 상태를 갱신한다. 이 상태들과
    핸들러를 `LoginPanel` 에 props 로 내려보낸다. **모드 변경은 `authStatus` 나 `step` 을 건드리지
    않는다** — D-07 에 따라 현재 세션은 그대로 유지된다.

    (7) `src/renderer/components/LoginPanel.tsx` — 로컬 `useState<LoginMode>` 탭 상태와 로컬
    `LoginMode` 타입 정의를 제거하고 `props.loginMode` / `props.onSetLoginMode` 를 쓴다. 활성 판정은
    `"credential"` 이 아니라 `"api"` 기준으로 바꾼다. API 탭 라벨은 `06-UI-SPEC.md` 확정 문구인
    `API 로그인`, 브라우저 탭은 `브라우저 로그인` 이다(옛 라벨 문자열은 남기지 않는다). 탭 블록을
    `!status.isLoggedIn` 조건 **밖으로** 끌어내 상태 행 바로 아래에 항상 렌더링한다(UI-SPEC
    Interaction Contract 1) — 자격증명 폼과 브라우저 로그인 버튼만 기존 조건 안에 남긴다.
    같은 작업으로 D-02 의 렌더러 몫을 처리한다: OTP 코드 상태·OTP 제출 핸들러·OTP 입력 JSX 블록과
    `window.api.auth.submitOtp` 호출, 그리고 자격증명 로그인 결과에서 OTP 를 요구하던 분기를 모두
    삭제한다. 삭제 후 자격증명 폼의 렌더 조건은 모드가 API 인지 여부만 본다. 이메일/비밀번호 폼의
    disabled 가드, `aria-busy`, `로그인 중...` 라벨, `.error-message` 슬롯은 **그대로 보존**한다.

    (8) 테스트: `src/main/services/__tests__/settings-store.test.ts` 신규 —
    `profile-store.test.ts` 의 `vi.hoisted` + `vi.mock("electron")` + 실제 `os.tmpdir()` 하위
    디렉토리 패턴을 그대로 따르고 `safeStorage` 모킹만 뺀다. 위 `<behavior>` 의 모든 케이스를
    한국어 `it()` 이름으로 덮는다. `src/main/__tests__/login-mode.test.ts` 는 기존 6개 `it` 을
    보존한 채 `persistedMode` 인자 케이스와 `isLoginModeLockedByEnv` 케이스를 같은 스타일로 추가한다.
  </action>
  <verify>
    <automated>npx vitest run src/main/services/__tests__/settings-store.test.ts src/main/__tests__/login-mode.test.ts</automated>
    <automated>npm run typecheck &amp;&amp; npm run typecheck:main</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/services/__tests__/settings-store.test.ts src/main/__tests__/login-mode.test.ts` 가 0 으로 종료한다
    - `npm run typecheck` 와 `npm run typecheck:main` 이 각각 0 으로 종료한다 (preload ↔ types ↔ ipc-handlers ↔ 렌더러 4자 정합의 증거)
    - `grep -c "export function getSettingsPath\|getSettingsPath" src/main/services/settings-store.ts` 가 1 이상이다
    - `grep -c "renameSync" src/main/services/settings-store.ts` 가 1 이상이다
    - `grep -c "safeStorage" src/main/services/settings-store.ts` 가 0 이다
    - `grep -c "settings:get-login-mode" src/main/preload.ts` 와 `grep -c "settings:get-login-mode" src/main/ipc-handlers.ts` 가 각각 1 이상이다
    - `grep -c "removeHandler(\"settings:" src/main/ipc-handlers.ts` 가 4 이다
    - `grep -cE "needOtp|otpCode|handleSubmitOtp|submitOtp" src/renderer/components/LoginPanel.tsx` 가 0 이다
    - `grep -c "useState<LoginMode>" src/renderer/components/LoginPanel.tsx` 가 0 이다
    - `grep -c "API 로그인" src/renderer/components/LoginPanel.tsx` 가 1 이상이다
    - 동작: 앱을 실행해 API 탭을 누르고 종료한 뒤 다시 실행하면 API 탭이 선택된 상태로 뜬다
    - 동작: 로그인된 상태에서 탭을 바꿔도 상태 배지가 "로그인 완료" 를 유지한다
  </acceptance_criteria>
  <!-- planner-discipline-allow: needOtp -->
  <!-- planner-discipline-allow: otpCode -->
  <!-- planner-discipline-allow: handleSubmitOtp -->
  <!-- planner-discipline-allow: submitOtp -->
  <!-- planner-discipline-allow: needOtp|otpCode|handleSubmitOtp|submitOtp -->
  <reversibility rating="reversible">설정 저장 위치와 IPC 채널 이름 모두 국소적 변경이다. 단 preload 계약은 한 번 열면 유지 부담이 생기므로 이름을 신중히 확정했다 (D-05).</reversibility>
  <done>모드 값이 디스크→main→IPC→탭까지 한 경로로 흐르고, 재시작 라운드트립과 env 우선순위가 자동 테스트로 증명되며, OTP 입력 화면이 렌더러에서 사라졌다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 고지 확인 상태 영속 + 재확인 판단 순수 함수</name>
  <files>src/shared/api-mode-notice.ts, src/shared/__tests__/api-mode-notice.test.ts, src/main/services/settings-store.ts, src/main/services/__tests__/settings-store.test.ts</files>
  <read_first>
    - src/shared/form-parser.ts (1-20줄 — shared 순수 모듈의 import 관례: electron import 없음, types 만 참조)
    - src/renderer/components/__tests__/profile-form-validation.test.ts (렌더링 없이 판단 로직만 테스트하는 이 저장소의 렌더러 테스트 관례)
    - src/main/services/settings-store.ts (Task 1 이 만든 파일 — 확장 대상)
    - .planning/phases/06-ui/06-UI-SPEC.md (Interaction Contract 2·4 — 재노출 규칙의 정본)
  </read_first>
  <behavior>
    - `shouldShowApiModeNotice(null, 1)` → `true` (한 번도 확인 안 함)
    - `shouldShowApiModeNotice(1, 1)` → `false` (같은 버전 확인 완료 → 재노출 없음)
    - `shouldShowApiModeNotice(0, 1)` → `true` (버전 상승 → 재확인 요구, D-10)
    - `shouldShowApiModeNotice(2, 1)` → `false` (미래 버전 값이 남아 있어도 재노출하지 않는다)
    - `API_MODE_NOTICE_VERSION` 은 정수 `1` 이다
    - `settingsStore.getNoticeAck()` — 파일 없음 → `{ ackedVersion: null, currentVersion: 1 }`
    - `settingsStore.ackNotice(1)` 후 새 인스턴스에서 `getNoticeAck().ackedVersion` → `1`
    - `ackNotice()` 는 같은 파일의 `loginMode` 값을 덮어쓰지 않는다 (부분 갱신)
    - 손상된 파일에서 `getNoticeAck()` → `{ ackedVersion: null, currentVersion: 1 }`, 예외 없음
  </behavior>
  <action>
    `src/shared/api-mode-notice.ts` 를 신규 작성한다. electron 을 import 하지 않는 순수 모듈이며
    (`mask.ts` / `form-parser.ts` 와 동일 관례) 두 개만 내보낸다: 정수 상수
    `API_MODE_NOTICE_VERSION = 1` 과 판단 함수
    `shouldShowApiModeNotice(ackedVersion: number | null, currentVersion: number): boolean` —
    `ackedVersion === null || ackedVersion < currentVersion` 이 규칙 전부다(UI-SPEC Interaction
    Contract 2). D-10 이 요구하는 "문구가 바뀔 때만 초기화"는 이 상수를 올리는 것으로 달성된다는
    사실을 파일 머리 주석에 남긴다 — 고지 문구를 수정하는 사람이 같은 파일에서 버전을 발견하도록
    상수와 규칙을 한 파일에 둔다.

    `settings-store.ts` 의 `getNoticeAck()` / `ackNotice(version)` 을 이 상수와 연결한다:
    `getNoticeAck()` 는 `{ ackedVersion: <파일값 또는 null>, currentVersion: API_MODE_NOTICE_VERSION }`
    를 반환하고, `ackNotice(version)` 은 `apiModeNoticeAckedVersion` 만 갱신한 뒤 나머지 필드를
    보존한 채 원자적으로 다시 쓴다. 파일의 `apiModeNoticeAckedVersion` 이 숫자가 아니면 `null` 로
    정규화한다.

    테스트: `src/shared/__tests__/api-mode-notice.test.ts` 신규 — 위 `<behavior>` 의 판단 함수
    케이스를 한국어 `it()` 이름으로 전수 작성한다. `settings-store.test.ts` 에는 ack 저장·조회
    케이스와 "부분 갱신이 다른 필드를 지우지 않는다" 케이스를 추가한다.
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/api-mode-notice.test.ts src/main/services/__tests__/settings-store.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/__tests__/api-mode-notice.test.ts src/main/services/__tests__/settings-store.test.ts` 가 0 으로 종료한다
    - `grep -c "export const API_MODE_NOTICE_VERSION" src/shared/api-mode-notice.ts` 가 1 이다
    - `grep -c "export function shouldShowApiModeNotice" src/shared/api-mode-notice.ts` 가 1 이다
    - `grep -c "electron" src/shared/api-mode-notice.ts` 가 0 이다
    - `grep -c "ackNotice" src/main/services/settings-store.ts` 가 1 이상이다
    - 동작: 확인 버전을 저장한 뒤 상수를 2로 올리면 `shouldShowApiModeNotice` 가 다시 true 를 반환한다
  </acceptance_criteria>
  <reversibility rating="reversible">버전 표현(정수)과 저장 필드 하나의 선택이며 파일 스키마 안에서 언제든 바꿀 수 있다 (D-10).</reversibility>
  <done>고지 확인 여부가 재시작을 넘어 유지되고, 재노출 판단이 렌더링 없이 테스트되는 순수 함수 하나로 고정됐다.</done>
</task>

<task type="auto">
  <name>Task 3: 설정 저장 실패 시 선택 롤백 — UI 가 거짓말하지 않게</name>
  <files>src/main/services/settings-store.ts, src/main/services/__tests__/settings-store.test.ts, src/renderer/App.tsx, src/renderer/components/LoginPanel.tsx</files>
  <read_first>
    - src/main/services/settings-store.ts (Task 1·2 결과 — 쓰기 경로의 현재 오류 처리)
    - src/renderer/App.tsx (Task 1 결과 — onSetLoginMode 핸들러와 loginError 상태)
    - src/renderer/components/LoginPanel.tsx (Task 1 결과 — .error-message 슬롯 위치)
    - .planning/phases/06-ui/06-UI-SPEC.md (§UI Considerations E1 error — 사용자 결정으로 backstop 에서 명시 테스트로 승격된 항목)
  </read_first>
  <action>
    설정 쓰기가 실패했는데 탭만 바뀌어 있는 상태를 구조적으로 불가능하게 만든다(UI-SPEC E1 error).

    main 쪽: `setLoginMode()` / `ackNotice()` 의 쓰기 실패는 이미 Task 1 에서 호출부로 전파되도록
    했다. 여기서는 실패 시 `logService.error("SettingsStore", ...)` 로 원인을 남기고, **부분 기록된
    임시 파일이 남지 않도록** 실패 경로에서 임시 파일을 정리한 뒤 다시 던지는지 확인한다.

    렌더러 쪽: `App.tsx` 의 모드 변경 핸들러를 "낙관적 갱신 후 실패 시 롤백"이 아니라 **성공 후에만
    상태를 갱신**하는 순서로 구현한다 — `window.api.settings.setLoginMode(mode)` 가 reject 하면
    로컬 `loginMode` 상태를 건드리지 않고, `06-UI-SPEC.md` 가 확정한 문구
    `설정 저장에 실패했습니다. 다시 시도해주세요.` 를 기존 오류 표시 경로로 내려보낸다. 탭의 활성
    표시는 오직 `props.loginMode` 에서만 파생되므로, 상태를 갱신하지 않는 것이 곧 "이전 선택으로
    되돌아간 것"과 같다 — 별도의 되돌리기 로직을 만들지 말 것.

    이 문구는 `LoginPanel` 의 기존 `.error-message` 슬롯(`role="alert"`)에 표시되며 새 표시 영역을
    만들지 않는다(D-15).
  </action>
  <verify>
    <automated>npx vitest run src/main/services/__tests__/settings-store.test.ts</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `settings-store.test.ts` 에 쓰기 실패(`fs.writeFileSync` 또는 `fs.renameSync` 가 던지도록 스파이) 케이스가 있고, 해당 테스트가 `setLoginMode()` 의 reject/throw 를 단언한다
    - 같은 테스트가 실패 후 디스크의 기존 설정 값이 변경되지 않았음을 단언한다
    - `grep -c "설정 저장에 실패했습니다. 다시 시도해주세요." src/renderer/App.tsx` 가 1 이다
    - `npx vitest run src/main/services/__tests__/settings-store.test.ts` 가 0 으로 종료한다
    - `npm run typecheck` 가 0 으로 종료한다
    - 동작: 저장이 실패하는 상황에서 탭의 활성 표시가 이전 선택 그대로 남는다
  </acceptance_criteria>
  <done>저장 실패와 화면 표시가 항상 일치한다 — 실패했는데 바뀐 것처럼 보이는 상태가 코드 경로상 존재하지 않는다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (플랜 06-01 몫)

**신규 파일**
- `src/main/services/settings-store.ts`
- `src/main/services/__tests__/settings-store.test.ts`
- `src/shared/api-mode-notice.ts`
- `src/shared/__tests__/api-mode-notice.test.ts`

**신규 심볼**
- `SettingsStore` (클래스), `settingsStore` (모듈 싱글턴)
- `SettingsStore.getLoginMode()`, `.setLoginMode()`, `.getLoginModeSnapshot()`, `.getNoticeAck()`, `.ackNotice()`
- `getSettingsPath()`, `SETTINGS_FILENAME`, `SETTINGS_SCHEMA_VERSION`, `DEFAULT_LOGIN_MODE`
- `API_MODE_NOTICE_VERSION`, `shouldShowApiModeNotice()`
- `isLoginModeLockedByEnv()` (`src/main/login-mode.ts`)
- `resolveLoginMode()` 의 2번째 매개변수 `persistedMode`
- `LoginMode`(shared/types.ts 로 이전), `LoginModeSnapshot`, `NoticeAckSnapshot`
- `IpcApi.settings` 네임스페이스

**신규 IPC 채널**
- `settings:get-login-mode`, `settings:set-login-mode`, `settings:get-notice-ack`, `settings:ack-notice`

**신규 컴포넌트 props (LoginPanel)**
- `loginMode`, `lockedByEnv`, `onSetLoginMode`, `noticeAck`, `onAckNotice`

**신규 사용자 문구**
- `API 로그인`, `설정 저장에 실패했습니다. 다시 시도해주세요.`

**신규 런타임 상태**
- `userData/settings.json` (평문 JSON, 이 phase 가 처음 만든다)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 렌더러 → main (contextBridge IPC) | 렌더러가 보내는 모드 값은 신뢰하지 않는다 — main 이 `"api" \| "browser"` 로 정규화한다 |
| 디스크 → main (`settings.json`) | 사용자/타 프로세스가 직접 편집 가능한 평문 파일. 파싱은 방어적이어야 한다 |
| `process.env` → main | 개발/QA 덮어쓰기 입력. UI 에 원문을 노출하지 않는다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-01 | Tampering | `settings.json` 파싱 (`settings-store.ts`) | low | mitigate | 필드별 화이트리스트 정규화(`loginMode !== "api"` 면 `"browser"`, ack 가 number 아니면 null) + 파싱 실패 시 기본값 폴백. Task 1 의 손상 파일 테스트가 회귀를 막는다 |
| T-06-02 | Tampering | 쓰기 중단으로 인한 부분 기록 파일 | low | mitigate | tmp 파일 + `renameSync` 원자 교체, 실패 시 tmp 정리 후 재던짐 (Task 1·3) |
| T-06-03 | Information Disclosure | 잠금 상태 표시가 `AUTOVERSE_LOGIN_MODE` 원문을 노출 | low | mitigate | IPC 는 `lockedByEnv: boolean` 만 반환하고 원문 env 문자열을 렌더러로 보내지 않는다 (Task 1) |
| T-06-04 | Information Disclosure | 설정 파일에 민감정보가 섞여 들어감 | medium | mitigate | 스키마를 3필드로 고정하고 화이트리스트 파싱. 자격증명은 기존 `credentials.enc`(safeStorage)에만 남긴다 — prohibitions 로 고정 |
| T-06-05 | Spoofing | 렌더러가 임의 문자열을 모드로 주입 | low | mitigate | main 이 `"api"` 정확 일치만 승격하고 나머지는 `"browser"` 로 정규화 (Task 1) |
| T-06-SC | Tampering | npm/pip/cargo 설치 | high | mitigate | **이 플랜은 패키지를 설치하지 않는다.** `06-RESEARCH.md` § Package Legitimacy Audit 이 신규 의존성 0건을 확인했고, `[ASSUMED]`/`[SUS]` 항목이 없어 인간 확인 체크포인트 대상이 없다. prohibitions 의 "새 npm 런타임 의존성 추가 금지"가 실행 시점 가드다 |

블로킹 임계값: high. T-06-SC 는 "설치 없음"으로 해소되며 미해소 high 위협은 없다.
</threat_model>

<verification>
- `npx vitest run src/main/services/__tests__/settings-store.test.ts src/main/__tests__/login-mode.test.ts src/shared/__tests__/api-mode-notice.test.ts` 전부 통과
- `npm test` 전체 스위트 통과 (기존 163개 테스트가 이 플랜의 변경으로 깨지지 않는지 확인 — 특히 `login-mode.test.ts` 기존 6개)
- `npm run typecheck` + `npm run typecheck:main` 모두 통과
- `git diff --stat package.json` 이 비어 있다 (신규 의존성 없음)
</verification>

<success_criteria>
- 앱 재시작 후에도 선택한 로그인 방식이 복원된다 (R016)
- 설정 파일 부재·손상·부분 기록 어느 경우에도 앱이 예외 없이 기본값으로 동작한다
- env 가 설정되면 저장값을 덮어쓰고 그 사실이 `lockedByEnv` 로 렌더러까지 전달된다 (D-06)
- OTP 입력 화면이 렌더러에서 완전히 사라졌다 (D-02 렌더러 몫)
- 로그인된 상태에서 모드를 바꿔도 세션이 유지된다 (D-07)
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-01-SUMMARY.md` when done.
SUMMARY 에는 `<planner_assumptions>` 의 R016/concurrency 가정을 그대로 옮겨 적고,
확정한 IPC 채널 이름과 `settings.json` 스키마를 명시할 것 (후속 플랜과 Phase 07 이 참조한다).
</output>
