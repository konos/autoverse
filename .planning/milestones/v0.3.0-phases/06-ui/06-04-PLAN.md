---
phase: 06-ui
plan: 04
type: execute
wave: 2
depends_on: ["06-01", "06-02"]
files_modified:
  - src/main/services/auth-service.ts
  - src/main/services/__tests__/auth-service.test.ts
  - src/main/services/api-auth-client.ts
  - src/main/services/__tests__/api-auth-client.test.ts
  - src/main/ipc-handlers.ts
  - src/main/preload.ts
  - src/shared/types.ts
autonomous: true
requirements: [R016, R020]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "API 모드에서 이메일/비밀번호로 로그인하면 헤드리스 자동입력 경로 하나만 실행된다 — 모드에 따라 다른 로그인 구현으로 갈라지는 분기가 코드에 존재하지 않는다 (D-01)"
    - "2026-08-25 HAR 실측으로 반증된 3단계 계정 API 로그인 경로가 코드베이스에서 실행될 수 없다 — 호출자도 메서드도 남아 있지 않다 (D-02)"
    - "저장된 자격증명으로 사용자 클릭 없이 외부 로그인 요청이 발생하는 경로가 두 모드 모두에서 존재하지 않는다 (D-03)"
    - "살아 있는 쿠키 토큰으로의 세션 복원은 두 모드 모두에서 그대로 동작한다 — 재시작 후 바로 사용할 수 있는 경험이 유지된다 (D-03)"
    - "R019 자산(사다리·서비스 토큰 교환·토큰 검증)은 이 정리 작업으로 손상되지 않는다"
    - "테스트 이름이 실제 동작과 일치한다 — '기존 동작 무변경' 이라고 주장하면서 변경된 동작을 검증하는 테스트가 남지 않는다"
  prohibitions:
    - "저장된 자격증명으로 사용자 클릭 없이 외부 로그인 요청을 발생시키지 않는다"
    - "reCAPTCHA 를 우회·자동 해결하는 코드를 작성하지 않는다"
    - "무인 로그인 가드를 약화시키는 방향의 변경을 하지 않는다 — 조건을 정정하되 보호 범위는 넓히기만 한다"
    - "R019 자산인 사다리·교환·검증 메서드를 삭제하지 않는다"
  artifacts:
    - src/main/services/auth-service.ts
    - src/main/services/api-auth-client.ts
    - src/main/services/__tests__/api-auth-client.test.ts
    - src/main/services/__tests__/auth-service.test.ts
  key_links:
    - "`auth:credential-login` IPC 핸들러 ↔ `AuthService.credentialLogin()` — 분기 제거 후 이 한 줄이 API 모드의 유일한 진입점이다"
    - "`tryAutoLogin()` 최상단 게이트 ↔ `main.ts` 앱 시작 경로 ↔ `auth:auto-login` IPC — 두 진입점이 이 게이트 하나로 수렴한다"
    - "`CredentialLoginResult` 필드 구성 ↔ `preload.ts` ↔ `LoginPanel.tsx` — 세 곳이 동시에 바뀌지 않으면 렌더러가 컴파일되지 않는다"
---

<objective>
반증된 3단계 계정 API 로그인 계약 위에 세워진 코드를 제거하고, 무인 자동 로그인 가드를
"모드 조건"에서 "외부 로그인 요청을 발생시키는가" 조건으로 재정의한다.

Purpose: 남겨두면 두 가지가 무너진다 — ① 모드 분기가 살아 있어 누군가 반증된 경로를 다시
배선할 수 있고 ② 05-01 에서 실제로 발생한 "저장된 다른 계정에 헤드리스 로그인이 시도돼 알림
메일이 발송된" 사고가 재발할 수 있다.
Output: D-01 이 확정한 단일 자격증명 로그인 경로, 두 모드 공통 무인 로그인 차단 가드.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-RESEARCH.md
@.planning/phases/06-ui/06-PATTERNS.md
@.planning/phases/06-ui/06-01-SUMMARY.md
@.planning/phases/06-ui/06-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 반증된 계정 API 로그인 코드 제거 + 자격증명 로그인 단일 경로 확정 (D-01/D-02)</name>
  <files>src/main/services/auth-service.ts, src/main/ipc-handlers.ts, src/main/preload.ts, src/shared/types.ts</files>
  <read_first>
    - .planning/phases/06-RESEARCH.md 가 아니라 .planning/phases/06-ui/06-RESEARCH.md (§"D-02 제거 블라스트 반경 — 정밀 목록" 표 — 대상별 정확한 라인 번호와 유지/삭제 판정)
    - .planning/phases/05-api/05-01-SUMMARY.md (§무효화된 전제 — 왜 이 코드가 반증됐는지의 1차 근거. 삭제 판단의 정당성 확인용)
    - src/main/services/auth-service.ts (1-70 필드 선언부, 190-210 자동 로그인 결과 처리, 440-460 DOM 폴링 결과 분기, 490-700 OTP·API 로그인 메서드 구간)
    - src/main/ipc-handlers.ts (49-61 모드 분기 2곳, 136-157 unregister 대칭)
    - src/main/preload.ts (5-16 auth 네임스페이스)
    - src/shared/types.ts (243-292 AuthEvent / CredentialLoginResult / IpcApi)
    - src/shared/login-failure.ts (06-02 결과 — 이 태스크가 소비할 매핑 함수 시그니처)
  </read_first>
  <action>
    D-02 의 제거 목록을 `06-RESEARCH.md` 의 블라스트 반경 표대로 실행한다. **삭제 순서가 중요하다**
    — 호출자를 먼저 지워야 중간 상태에서도 타입체크가 통과한다.

    (1) `auth-service.ts` 에서 삭제: 계정 API 로그인 진입 메서드(`credentialLoginApi`), API 경로
    OTP 제출 메서드(`submitOtpApi`), 두 메서드만 호출하던 private 완료 헬퍼(`finishApiLogin`),
    그리고 그 셋만 읽고 쓰던 진행 상태 필드(`apiLoginState`). 헤드리스 창에 코드를 타이핑하던
    `submitOtp()` 메서드도 함께 삭제한다 — 렌더러 화면이 06-01 에서 사라져 호출자가 0이며, 남겨두면
    D-02 가 경계하는 "다시 배선될 수 있는 죽은 경로"가 된다. **이는 CONTEXT D-02 의 명시 목록을
    넘어서는 확대 삭제이므로**(`06-RESEARCH.md` Open Question 1·2), 근거와 함께 SUMMARY 에 반드시
    기록한다: HAR 436 entries 에서 관련 호출 0건, 렌더러 소비자 0, git 이력에 원문 보존.

    (2) `shared/types.ts` — `CredentialLoginResult` 에서 OTP 요구 여부 불리언 필드를 제거하고
    `reason?: LoginFailureReason` 를 추가한다(`src/shared/login-failure.ts` 에서 import).
    `IpcApi.auth` 에서 OTP 제출 메서드 시그니처를 제거한다. `AuthEvent` union 은 이 태스크에서
    건드리지 않는다(06-05 소관).

    (3) `preload.ts` — `auth` 네임스페이스에서 OTP 제출 항목을 제거한다. `settings` 네임스페이스는
    06-01 이 이미 추가했으므로 손대지 않는다.

    (4) `ipc-handlers.ts` — `auth:credential-login` 핸들러의 모드 삼항을 없애고
    `authService.credentialLogin(email, password)` 한 줄로 만든다(D-01: 헤드리스 자동입력 경로가
    곧 API 모드 구현이다). `auth:submit-otp` 핸들러는 채널째 삭제하고 `unregisterIpcHandlers()` 의
    대응 `removeHandler` 줄도 함께 지운다(등록/해제 1:1 대칭 유지). 이 파일이 더 이상
    `resolveLoginMode` 를 직접 쓰지 않게 되면 06-01 이 추가한 `settings:get-login-mode` 핸들러 쪽
    사용만 남는지 확인하고, 미사용 import 를 정리한다.

    (5) 위 삭제로 컴파일이 깨지는 잔여 참조 2곳을 같이 고친다:
    - 자동 로그인 결과 처리에서 OTP 요구 여부를 읽던 분기(파일 200줄 부근)는 필드 자체가 사라지므로
      제거한다. 이 블록은 Task 3 의 게이트 재작성에서 어차피 사라진다.
    - DOM 폴링 결과의 `'otp'` 분기(파일 449줄 부근)는 이 태스크에서 **최소 변경**만 한다: 제거된
      필드를 반환하지 않도록 고치고, 사용자 문구는 06-02 의 `mapLoginFailure("unknown", ...)`
      결과를 사용하며 `reason` 을 함께 담는다. 캡차 신호를 별도 사유로 분리하는 진짜 수정은
      06-05 소관이다. **반증된 이메일 코드 안내 문구를 이 단계에서 재작성해 남기지 말 것** —
      06-02 매핑 함수의 반환값만 쓴다.

    (6) 유지 확인: `login()`(브라우저 모드 본체), `credentialLogin()`(헤드리스 본체),
    `validateToken()`, `runAccountTokenLadderSpike()`, `extractTokenFromCookies()`,
    `attachAccountTokenCapture()` 는 전부 그대로 둔다.
  </action>
  <verify>
    <automated>npm run typecheck &amp;&amp; npm run typecheck:main</automated>
    <automated>npx vitest run src/main/services/__tests__/auth-service.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` 와 `npm run typecheck:main` 이 각각 0 으로 종료한다
    - `grep -vE '^\s*(//|\*|/\*)' src/main/services/auth-service.ts | grep -cE "credentialLoginApi|submitOtpApi|finishApiLogin|apiLoginState"` 가 0 이다
    - `grep -vE '^\s*(//|\*|/\*)' src/main/ipc-handlers.ts | grep -c "auth:submit-otp"` 가 0 이다
    - `grep -c "removeHandler(\"auth:submit-otp\")" src/main/ipc-handlers.ts` 가 0 이다
    - `grep -vE '^\s*(//|\*|/\*)' src/main/preload.ts | grep -c "auth:submit-otp"` 가 0 이다
    - `grep -vE '^\s*(//|\*|/\*)' src/shared/types.ts | grep -c "needOtp"` 가 0 이다
    - `grep -c "reason?: LoginFailureReason" src/shared/types.ts` 가 1 이다
    - `grep -c "resolveLoginMode() === \"api\"" src/main/ipc-handlers.ts` 가 0 이다
    - `grep -c "authService.credentialLogin(email, password)" src/main/ipc-handlers.ts` 가 1 이다
    - `grep -c "runAccountTokenLadderSpike\|extractTokenFromCookies" src/main/services/auth-service.ts` 가 여전히 1 이상이다 (R019 자산 보존)
  </acceptance_criteria>
  <!-- planner-discipline-allow: credentialLoginApi -->
  <!-- planner-discipline-allow: submitOtpApi -->
  <!-- planner-discipline-allow: finishApiLogin -->
  <!-- planner-discipline-allow: apiLoginState -->
  <!-- planner-discipline-allow: needOtp -->
  <!-- planner-discipline-allow: auth:submit-otp -->
  <!-- planner-discipline-allow: resolveLoginMode() === "api" -->
  <!-- planner-discipline-allow: credentialLoginApi|submitOtpApi|finishApiLogin|apiLoginState -->
  <reversibility rating="costly">D-01 이 API 모드의 실체를 헤드리스 자동입력 경로로 확정한다. 되돌리려면 여기서 지운 계정 API 코드를 복원하고 IPC·UI 문구·요구사항 서술을 함께 되돌려야 한다. 다만 계약이 또 바뀐다면 05-01-SUMMARY.md 의 정본 기록을 근거로 재작성하는 편이 낫다.</reversibility>
  <done>자격증명 로그인 진입점이 하나뿐이고, 반증된 계약 위의 메서드·채널·타입 필드가 전부 사라졌으며 타입체크가 통과한다.</done>
</task>

<task type="auto">
  <name>Task 2: ApiAuthClient 고아 메서드 제거 + 교차 의존 테스트 재작성</name>
  <files>src/main/services/api-auth-client.ts, src/main/services/__tests__/api-auth-client.test.ts</files>
  <read_first>
    - src/main/services/api-auth-client.ts (전체 341줄 — 삭제 대상과 유지 대상이 한 클래스 안에 섞여 있다)
    - src/main/services/__tests__/api-auth-client.test.ts (전체 — 7개 describe 중 5개가 삭제 대상 메서드를 호출한다)
    - .planning/phases/06-ui/06-RESEARCH.md (§"영향받는 테스트 (전체 목록)" 표 — describe 별 삭제/유지/재작성 판정과 라인 번호)
    - .planning/phases/06-ui/06-CONTEXT.md (D-02 유지 대상 목록 — 사다리·교환·검증은 R019 자산)
  </read_first>
  <action>
    Task 1 이 호출자를 모두 제거했으므로 이제 `ApiAuthClient` 의 고아 메서드를 지운다.

    삭제: OTP 세션 요청 메서드(`requestOtpSession`), 자격증명 로그인 메서드(`loginWithCredentials`),
    OTP 검증 메서드(`verifyOtp`), 그리고 이들 전용 부속인 `OtpSession` 인터페이스와
    `REFRESH_TOKEN_COOKIE_TTL` 상수. `verifyOtp` 는 CONTEXT D-02 의 명시 목록에 이름이 없지만
    유일한 호출자가 Task 1 에서 사라져 고아가 된다 — `06-RESEARCH.md` Open Question 2 의 권고대로
    함께 제거하고, 확대 삭제임을 SUMMARY 에 기록한다.

    **유지 (R019 자산, 손대지 말 것):** `acquireFaneventToken()` 사다리, `probeFaneventToken()`,
    `exchangeForService()`, `ApiAuthError`, 생성자 주입 `fetchFn` 패턴, 공통 헤더 상수.

    테스트 정리 — `06-RESEARCH.md` 의 판정표를 그대로 따른다:
    - OTP 세션 / OTP 검증 / 에러 매핑 describe 3개는 삭제 대상 메서드 전용이므로 블록째 삭제한다.
    - 교환 describe 와 사다리 describe 2개는 **무변경 유지**한다.
    - 트레이서 describe 는 삭제된 4개 메서드를 순서대로 체이닝하므로 전면 재작성한다: 남은 사다리
      경로만으로 "계정 토큰 → 팬이벤트 토큰 + fanId" 를 얻는 종단 시나리오로 다시 쓴다.
    - 민감정보 describe 는 취지("비밀번호/코드가 로그에 남지 않는다")가 여전히 유효하므로 남은
      메서드를 대상으로 재작성한다. 로그 스파이 단언은 유지한다.

    삭제한 describe 가 덮고 있던 **의미 있는 커버리지 손실이 있는지** 확인하고, 있다면 남은
    메서드에 대한 테스트로 옮긴다(예: 에러 매핑 describe 가 검증하던 `ApiAuthError` 생성 규칙은
    사다리 실패 경로로 옮겨 유지).
  </action>
  <verify>
    <automated>npx vitest run src/main/services/__tests__/api-auth-client.test.ts</automated>
    <automated>npm run typecheck:main</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/services/__tests__/api-auth-client.test.ts` 가 0 으로 종료한다
    - `npm run typecheck:main` 이 0 으로 종료한다
    - `grep -vE '^\s*(//|\*|/\*)' src/main/services/api-auth-client.ts | grep -cE "requestOtpSession|loginWithCredentials|verifyOtp|OtpSession|REFRESH_TOKEN_COOKIE_TTL"` 가 0 이다
    - `grep -c "acquireFaneventToken\|exchangeForService\|probeFaneventToken" src/main/services/api-auth-client.ts` 가 3 이상이다 (R019 자산 보존)
    - `grep -c "describe(" src/main/services/__tests__/api-auth-client.test.ts` 가 4 이하다 (7개에서 3개 이상 삭제됨)
    - 테스트 파일에 `ApiAuthError` 를 단언하는 케이스가 최소 1개 남아 있다
  </acceptance_criteria>
  <!-- planner-discipline-allow: requestOtpSession -->
  <!-- planner-discipline-allow: loginWithCredentials -->
  <!-- planner-discipline-allow: verifyOtp -->
  <!-- planner-discipline-allow: OtpSession -->
  <!-- planner-discipline-allow: REFRESH_TOKEN_COOKIE_TTL -->
  <!-- planner-discipline-allow: requestOtpSession|loginWithCredentials|verifyOtp|OtpSession|REFRESH_TOKEN_COOKIE_TTL -->
  <reversibility rating="reversible">순수 삭제이며 git 이력에 남는다. 캡차 폴백 경로가 실재로 확인되면 05-01-SUMMARY.md 의 정정된 계약을 근거로 다시 작성하는 편이 낫다.</reversibility>
  <done>반증된 계약 관련 메서드가 클라이언트에서 사라지고, 남은 테스트 전부가 실제 존재하는 메서드만 호출한다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 무인 자동 로그인 가드 재정의 — 모드 조건에서 외부 요청 조건으로 (D-03)</name>
  <files>src/main/services/auth-service.ts, src/main/services/__tests__/auth-service.test.ts</files>
  <read_first>
    - src/main/services/auth-service.ts (160-237 — `tryAutoLogin()` 과 재로그인 메서드의 현재 게이트 구조 전체, 주석 포함)
    - src/main/services/auth-service.ts (1055-1150 — 재로그인 메서드의 두 호출 지점과 실패 시 token-expired 처리)
    - src/main/services/__tests__/auth-service.test.ts (172-224 — 재작성 대상 describe 3개. 특히 "기존 동작 무변경 (D-01 회귀 확인)" 이름의 테스트)
    - .planning/phases/06-ui/06-CONTEXT.md (D-03 전문 — 허용/차단 목록과 근거 3가지, 로드맵 SC1 편차)
    - .planning/phases/06-ui/06-RESEARCH.md (§Common Pitfalls 5 — 이름이 거짓말하게 되는 테스트)
  </read_first>
  <behavior>
    - API 모드 + 저장된 자격증명 있음 → `tryAutoLogin()` 이 헤드리스 로그인을 호출하지 않는다
    - 브라우저 모드 + 저장된 자격증명 있음 → `tryAutoLogin()` 이 헤드리스 로그인을 **역시** 호출하지 않는다 (D-03 의 핵심 회귀 — 이전에는 호출했다)
    - 두 모드 모두 쿠키에 살아 있는 토큰이 있으면 `tryAutoLogin()` 이 true 를 반환한다 (세션 복원 허용)
    - 두 모드 모두 쿠키 토큰이 없으면 `tryAutoLogin()` 이 false 를 반환하고 사용자 개입을 로그로 알린다
    - 재로그인 경로(토큰 만료·401)에서도 저장된 자격증명으로 로그인을 시도하지 않는다
    - 재로그인 경로가 쿠키 복원에 성공하면 true 를 반환해 기존 세션 복구 동작이 유지된다
  </behavior>
  <action>
    가드의 조건을 *모드* 에서 *"외부에 로그인 요청을 발생시키는가"* 로 바꾼다. 이것은 가드를
    **약화하는 것이 아니라 두 모드로 확대**하는 변경이다.

    (1) `tryAutoLogin()` — 최상단의 모드 검사와 그 아래 "API 모드는 매 로그인마다 OTP 필요" 주석
    블록을 제거한다(사유가 반증됐다). 대신 다음 구조로 만든다: 쿠키 토큰 복원 시도 →
    성공하면 true. 실패하면 **저장된 자격증명을 불러와 로그인하는 하단 블록 전체를 제거**하고
    false 를 반환하며, 사용자 클릭이 필요하다는 사실을 로그로 남긴다. 새 주석에는 정정된 사유
    두 가지를 적는다: ① 외부 로그인 요청은 항상 사용자 클릭에서만 발생한다 ② 05-01 에서 이
    가드 누락으로 저장된 다른 계정에 헤드리스 로그인이 시도돼 실제 알림 메일이 발송됐다.
    반증된 "매 로그인마다 OTP 필요" 서술을 주석에 남기지 말 것.

    (2) 재로그인 메서드 — 저장된 자격증명으로 로그인하는 본문을 제거하고 쿠키 세션 복원만
    남긴다. 그 결과 이 메서드는 더 이상 어떤 형태의 로그인도 수행하지 않으므로 이름을
    `trySessionRestore()` 로 바꾼다(호출 지점 2곳도 함께). "자동 재로그인 중..." 진행 문구는
    사실이 아니게 되므로 제거한다 — 복원 실패 시의 기존 만료 안내 경로는 그대로 살린다.
    진행 중 중복 실행을 막던 불리언 플래그는 유지한다.

    (3) 테스트 재작성 — `auth-service.test.ts` 의 게이트 describe 를 D-03 문구로 다시 쓴다.
    describe 이름은 모드 게이트가 아니라 "무인 로그인 차단(두 모드 공통)" 취지를 반영해야 한다.
    "기존 동작 무변경 (D-01 회귀 확인)" 이라는 이름의 테스트는 **삭제하지 말고 다시 쓴다** —
    D-03 은 브라우저 모드 동작을 의도적으로 바꾸므로 그 이름은 사실이 아니게 된다. 대신
    `<behavior>` 의 "브라우저 모드 + 저장된 자격증명 있음" 케이스를 진짜 회귀 테스트로 추가한다:
    자격증명이 존재하도록 준비한 상태에서 헤드리스 로그인 스파이가 호출되지 않는 것을 단언한다
    (이전 코드였다면 호출됐을 조건이다). 쿠키 복원 허용 케이스도 함께 추가한다.
    사다리 스파이크 describe 는 무변경 유지한다.
  </action>
  <verify>
    <automated>npx vitest run src/main/services/__tests__/auth-service.test.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/services/__tests__/auth-service.test.ts` 가 0 으로 종료한다
    - `npm test` 전체 스위트가 0 으로 종료한다
    - `sed -n '/async tryAutoLogin/,/^  }/p' src/main/services/auth-service.ts | grep -c "this.credentialLogin("` 가 0 이다
    - `sed -n '/async tryAutoLogin/,/^  }/p' src/main/services/auth-service.ts | grep -c "extractTokenFromCookies"` 가 1 이상이다
    - `grep -vE '^\s*(//|\*|/\*)' src/main/services/auth-service.ts | grep -c "tryAutoRelogin"` 가 0 이다
    - `grep -c "trySessionRestore" src/main/services/auth-service.ts` 가 3 이상이다 (정의 1 + 호출 2)
    - `grep -vE '^\s*(//|\*|/\*)' src/main/services/auth-service.ts | grep -c "resolveLoginMode"` 가 0 이다 (게이트가 더 이상 모드를 보지 않는다)
    - `grep -c "기존 동작 무변경" src/main/services/__tests__/auth-service.test.ts` 가 0 이다
    - 테스트 파일에 브라우저 모드 + 저장된 자격증명 조건에서 헤드리스 로그인 미호출을 단언하는 케이스가 존재한다
    - 동작: 저장된 자격증명이 있어도 앱 시작 시 Weverse 로 로그인 요청이 나가지 않는다 (로그 패널에 로그인 시도 라인이 없다)
  </acceptance_criteria>
  <!-- planner-discipline-allow: tryAutoRelogin -->
  <!-- planner-discipline-allow: resolveLoginMode -->
  <!-- planner-discipline-allow: 기존 동작 무변경 -->
  <!-- planner-discipline-allow: this.credentialLogin( -->
  <reversibility rating="reversible">게이트 조건 한 곳의 변경이다. 다만 되돌리는 방향(무인 로그인 재허용)은 05-01 에서 실증된 계정 오용 사고를 되살리므로 되돌리지 않는 것이 기본값이다.</reversibility>
  <done>두 모드 모두에서 외부 로그인 요청이 사용자 클릭에서만 발생하고, 쿠키 세션 복원은 그대로 살아 있으며, 테스트 이름이 실제 동작과 일치한다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (플랜 06-04 몫)

**신규 심볼**
- `trySessionRestore()` (`auth-service.ts` — 기존 자동 재로그인 메서드의 정직한 후신, 로그인을 수행하지 않고 쿠키 세션 복원만 한다)
- `CredentialLoginResult.reason` 필드 (`src/shared/types.ts`, 타입은 06-02 의 `LoginFailureReason`)

**제거되는 심볼 (하류 참조 금지)**
- `AuthService.credentialLoginApi()`, `AuthService.submitOtpApi()`, `AuthService.finishApiLogin()`, `AuthService.submitOtp()`, `AuthService.apiLoginState`, `AuthService.tryAutoRelogin()`
- `ApiAuthClient.requestOtpSession()`, `ApiAuthClient.loginWithCredentials()`, `ApiAuthClient.verifyOtp()`, `OtpSession`, `REFRESH_TOKEN_COOKIE_TTL`
- `CredentialLoginResult.needOtp`, `IpcApi.auth.submitOtp`, IPC 채널 `auth:submit-otp`
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 앱 → Weverse 서버 | 외부 로그인 요청은 실제 계정에 알림 메일·보안 이벤트를 발생시킨다. 누가 이 요청을 촉발하는지가 신뢰 경계다 |
| 저장된 자격증명(`credentials.enc`) → 로그인 실행 | 디스크의 자격증명이 사용자 의사 없이 사용될 수 있는 지점 |
| 렌더러 → main (IPC) | 삭제되는 채널의 잔여 호출은 런타임 오류가 아니라 조용한 무응답이 될 수 있다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-12 | Spoofing (계정 오용) | `tryAutoLogin()` / 재로그인 경로의 무인 `credentialLogin()` 호출 | high | mitigate | Task 3 에서 자격증명 기반 무인 호출 블록을 두 모드 모두에서 제거하고, "브라우저 모드 + 저장된 자격증명" 회귀 테스트로 고정. 05-01 에서 실제 발생한 사고의 직접 대응 |
| T-06-13 | Tampering | 반증된 로그인 경로가 코드에 남아 재배선 | high | mitigate | Task 1·2 에서 메서드·채널·타입 필드·테스트까지 전부 제거하고 부정 grep 으로 잔존 여부를 검증 |
| T-06-14 | Denial of Service (사용자) | 삭제된 IPC 채널을 렌더러가 계속 호출해 무응답 | medium | mitigate | 렌더러 호출부는 06-01 에서 이미 제거됐고, 타입에서 시그니처를 지워 컴파일 타임에 잔여 호출이 드러나게 한다 |
| T-06-15 | Information Disclosure | 삭제 과정에서 R019 자산(사다리·교환)이 함께 손상 | medium | mitigate | 유지 목록을 prohibitions 로 고정 + 사다리/교환 describe 무변경 유지 + 잔존 grep |
| T-06-16 | Elevation of Privilege | reCAPTCHA 우회 코드가 정리 과정에 섞여 들어감 | high | mitigate | R013 은 영구 제외. 캡차 관련 코드는 감지만 하고 해결하지 않는다 — prohibitions 로 고정하고 06-05 가 감지 결과를 사용자 안내로만 소비한다 |
| T-06-SC | Tampering | npm/pip/cargo 설치 | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — `06-RESEARCH.md` § Package Legitimacy Audit 신규 의존성 0건, `[ASSUMED]`/`[SUS]` 없음 |

블로킹 임계값: high. T-06-12 / T-06-13 / T-06-16 은 이 플랜 안에서 코드 + 테스트로 해소된다.
</threat_model>

<verification>
- `npm test` 전체 스위트 통과 (D-02 삭제로 인한 컴파일 깨짐이 가장 먼저 드러나는 관문)
- `npm run typecheck` + `npm run typecheck:main` 통과
- `grep -rn "credential-login" src/main/ipc-handlers.ts` 결과가 모드 분기 없는 한 줄 위임이다
- 앱을 실행해 로그인하지 않은 상태로 두었을 때 로그 패널에 외부 로그인 시도 라인이 나타나지 않는다
</verification>

<success_criteria>
- 자격증명 로그인 경로가 하나뿐이고 모드 분기가 코드에 없다 (D-01)
- 반증된 계약 기반 메서드·채널·타입·테스트가 전부 제거됐다 (D-02)
- 무인 외부 로그인 요청이 두 모드 모두에서 불가능하고, 쿠키 세션 복원은 유지된다 (D-03)
- R019 자산이 무손상이다
- 전체 테스트 스위트와 두 타입체크가 green 이다
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-04-SUMMARY.md` when done.
SUMMARY 에 반드시 포함할 것: ① CONTEXT D-02 명시 목록을 넘어 확대 삭제한 항목
(`AuthService.submitOtp()`, `ApiAuthClient.verifyOtp()` 및 부속)과 그 근거
② 삭제된 테스트가 덮던 커버리지를 어디로 옮겼는지 ③ `trySessionRestore()` 로의 개명 사실.
</output>
