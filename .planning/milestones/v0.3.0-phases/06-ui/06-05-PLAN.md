---
phase: 06-ui
plan: 05
type: execute
wave: 3
depends_on: ["06-02", "06-04"]
files_modified:
  - src/main/services/auth-service.ts
  - src/main/services/__tests__/auth-service.test.ts
  - src/shared/login-failure.ts
  - src/shared/__tests__/login-failure.test.ts
  - src/shared/types.ts
autonomous: true
requirements: [R020]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Weverse 보안 확인(캡차)에 막히면 사용자는 캡차 때문이라는 설명과 브라우저 로그인 전환 안내를 받는다 — 오지 않을 메일을 기다리게 하는 안내가 어떤 경로로도 표시되지 않는다 (D-13, R020)"
    - "DOM 셀렉터가 Weverse 배포로 깨져 아무 신호도 잡히지 않으면 미매핑 폴백 안내로 떨어진다 — 조용한 무응답이 되지 않는다 (D-13 주의사항, D-14)"
    - "이론상 도달 불가능한 OTP 입력 폼이 실제로 뜨더라도 사용자는 원인 불명의 무응답 대신 미매핑 폴백 안내와 식별자를 받는다 (06-RESEARCH.md Open Question 1 권고 (a))"
    - "렌더러로 돌아가는 실패 문구와 식별자는 마스킹을 통과한 값뿐이다 — 토큰·비밀번호·URL 쿼리스트링이 화면에 노출되지 않는다 (R010, D-14)"
    - "로그인은 성공했지만 서비스 토큰 확보에 실패한 경우에도 사용자가 그 사실과 다음 행동을 안내받는다 (D-12 사다리 실패 행)"
    - "실패 사유가 `CredentialLoginResult.reason` 으로 렌더러까지 전달돼, 렌더러가 문구를 되파싱하지 않고 후속 동작(전환 버튼 노출)을 결정할 수 있다"
  prohibitions:
    - "reCAPTCHA 를 우회하거나 자동으로 해결하는 코드를 작성하지 않는다 — 감지해서 사용자에게 알리는 것까지만 한다"
    - "숨겨진 헤드리스 창을 사용자에게 보여 캡차를 직접 풀게 하는 복구 흐름을 만들지 않는다 (이 phase 범위 밖, deferred)"
    - "렌더러로 반환되는 실패 문구·식별자에 마스킹을 거치지 않은 원문을 넣지 않는다"
    - "실패를 조용히 삼키지 않는다 — 미매핑 사유도 일반 안내 + 식별자 + 로그 원문을 남긴다"
  artifacts:
    - src/shared/login-failure.ts
    - src/main/services/auth-service.ts
  key_links:
    - "DOM 폴링이 반환하는 원시 신호 문자열 ↔ `classifyCredentialLoginSignal()` ↔ `LoginFailureReason` — 어휘가 어긋나면 모든 실패가 미매핑으로 떨어진다"
    - "`maskSensitive()` 적용 지점 ↔ `CredentialLoginResult.message`/`identifier` — 로그 자동 마스킹이 닿지 않는 경로라 여기가 R010 의 유일한 관문이다"
    - "`login-failed` AuthEvent ↔ App.tsx 의 loginError 상태 ↔ LoginPanel 의 error prop — 사다리 실패 안내가 사용자에게 도달하는 경로"
---

<objective>
캡차를 OTP 로 오독하던 분류를 고치고, 실패 사유를 타입드 값으로 렌더러까지 배선한다.
식별자는 마스킹 관문을 반드시 통과시킨다.

Purpose: 이 phase 의 대표 수정 항목이다. 현재 사용자는 캡차에 막혔는데 "이메일로 코드가
발송되었습니다" 를 보고 오지 않을 메일을 기다린다 — 문구 오류가 아니라 사용자의 시간을 직접
낭비시키는 결함이다.
Output: 재분류된 DOM 신호, `reason` 을 실은 로그인 결과, 마스킹된 식별자.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-UI-SPEC.md
@.planning/phases/06-ui/06-02-SUMMARY.md
@.planning/phases/06-ui/06-04-SUMMARY.md
</context>

<planner_assumptions>
⚠ **FLAGGED ASSUMPTION — R020 / concurrency (edge probe: unresolved / unclassified)**
결정적 edge probe 가 R020 에 대해 "중단되거나 병렬로 실행되면 무엇이 보장되는가?"를 물었고
`unresolved` 로 남았다. 이 플랜은 다음을 **가정**하고 진행한다:

- 자격증명 로그인은 한 번에 하나만 진행된다고 가정한다. 현재 UI 는 진행 중 버튼을 비활성화하지만,
  main 프로세스에는 두 번째 `credentialLogin()` 호출을 막는 락이 없다. 두 번 겹쳐 호출되면
  두 번째 호출이 첫 번째 헤드리스 창을 정리하고 자기 창을 연다(기존 `cleanupHeadless()` 동작).
  이 경우 **마지막 시도의 결과만 사용자에게 표시된다** — 실패 메시지는 누적되지 않고 교체된다.
- 로그인 진행 중 사용자가 모드를 바꿔도 진행 중인 시도는 취소되지 않는다(D-07: 세션 유지). 결과는
  시도 당시의 경로 기준으로 안내된다.
- 이 가정이 틀리다면(예: 향후 다중 창) 두 시도의 안내가 뒤섞일 수 있다. 그 시점에 main 쪽 진행
  중 플래그를 도입할 것.

이 가정은 조용히 넘어가지 않고 SUMMARY 에 그대로 옮겨 적어야 한다.
</planner_assumptions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DOM 신호 재분류 — 캡차를 캡차로 (D-13)</name>
  <files>src/shared/login-failure.ts, src/shared/__tests__/login-failure.test.ts, src/main/services/auth-service.ts</files>
  <read_first>
    - src/main/services/auth-service.ts (392-420 — `executeJavaScript` 안의 DOM 폴링 문자열 원문. 캡차 위젯을 찾고도 OTP 신호를 돌려주는 바로 그 줄)
    - src/shared/login-failure.ts (06-02 결과 — 확장할 union 과 매핑 함수)
    - src/shared/__tests__/login-failure.test.ts (06-02 결과)
    - .planning/phases/06-ui/06-CONTEXT.md (D-13 전문 — 셀렉터 실패 시 폴백을 반드시 정의하라는 요구 포함)
    - .planning/phases/06-ui/06-RESEARCH.md (§Common Pitfalls 3 + §Open Questions 1 — OTP DOM 분기의 처리 방향과 권고)
  </read_first>
  <behavior>
    - `classifyCredentialLoginSignal("captcha")` → `{ reason: "captcha" }`
    - `classifyCredentialLoginSignal("otp-form")` → `{ reason: "unknown", detail }` — 도달 불가에 가까운 신호를 미매핑 폴백으로 라우팅
    - `classifyCredentialLoginSignal("error:비밀번호가 올바르지 않습니다.")` → `{ reason: "form-error", detail: "비밀번호가 올바르지 않습니다." }`
    - `classifyCredentialLoginSignal("timeout")` → `{ reason: "timeout" }`
    - `classifyCredentialLoginSignal(null)` → `{ reason: "unknown" }` — 셀렉터가 아무것도 못 찾은 경우
    - `classifyCredentialLoginSignal("완전히 새로운 문자열")` → `{ reason: "unknown", detail: "완전히 새로운 문자열" }`
    - `classifyCredentialLoginSignal("error:")` (본문 없음) → `{ reason: "unknown" }` — 빈 폼 오류를 폼 오류로 취급하지 않는다
  </behavior>
  <action>
    (1) `src/shared/login-failure.ts` 에 원시 신호 → 사유 분류 함수를 추가한다:
    `classifyCredentialLoginSignal(raw: string | null): { reason: LoginFailureReason; detail?: string }`.
    이 함수가 DOM 폴링 결과 문자열과 06-02 의 사유 어휘를 잇는 유일한 지점이며, 순수 함수이므로
    렌더링·Electron 없이 전수 테스트가 가능하다. 매핑 규칙은 `<behavior>` 에 적힌 그대로다.
    접두 `error:` 는 뒤의 본문을 `detail` 로 떼어내되 본문이 비어 있으면 미매핑으로 보낸다.

    (2) `auth-service.ts` 의 `executeJavaScript` DOM 폴링 문자열을 고친다. 현재는 캡차 위젯
    셀렉터를 찾고도 OTP 신호를 반환한다 — **이것이 D-13 이 지목한 오분류다.** 캡차 위젯이 잡히면
    `'captcha'` 를 반환하도록 분리하고, 인증코드 입력창 셀렉터가 잡히면 `'otp-form'` 이라는 별도
    신호를 반환하게 한다(제거하지 않는 이유: 서버가 실제로 그 폼을 띄우는 상황에서 조용한 무응답이
    되는 것을 막기 위해서다. R018 을 삭제 대신 보류로 남긴 이 프로젝트의 기존 원칙과 같은 판단이며,
    `06-RESEARCH.md` Open Question 1 의 권고 (a) 다). 폼 오류 셀렉터 분기와 `null` 반환은
    그대로 둔다 — 셀렉터가 Weverse 배포로 깨지면 자연스럽게 `null` 이 되어 미매핑 폴백으로
    떨어지는 것이 의도된 설계다. **DOM 문자열 안에서 예외를 던지지 말 것.**

    (3) 폴링 결과를 처리하는 분기들이 이 분류 함수를 거치도록 바꾼다 — 분기마다 문구를 직접
    적지 않는다. 캡차 신호에는 절대 이메일 코드 서사를 붙이지 않는다(R013 상 우회는 영구 제외이므로
    할 수 있는 일은 정확히 알리는 것뿐이다).
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/login-failure.test.ts</automated>
    <automated>npm run typecheck:main</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/__tests__/login-failure.test.ts` 가 0 으로 종료한다
    - `grep -c "export function classifyCredentialLoginSignal" src/shared/login-failure.ts` 가 1 이다
    - `grep -c "recapcha_wrapper" src/main/services/auth-service.ts` 가 1 이다 (셀렉터는 유지)
    - `sed -n "/const domState = await/,/as string | null/p" src/main/services/auth-service.ts | grep -c "return 'captcha'"` 가 1 이다
    - `sed -n "/const domState = await/,/as string | null/p" src/main/services/auth-service.ts | grep -c "return 'otp'"` 가 0 이다
    - `npm run typecheck:main` 이 0 으로 종료한다
    - 테스트에 `null` 입력 케이스와 알 수 없는 문자열 케이스가 모두 존재한다
  </acceptance_criteria>
  <!-- planner-discipline-allow: return 'otp' -->
  <reversibility rating="reversible">분류 함수 하나와 DOM 폴링 문자열의 반환값 변경이다. 셀렉터가 깨지면 폴백 경로가 이미 정의돼 있어 안전하게 되돌릴 필요 자체가 적다.</reversibility>
  <done>캡차가 캡차로 분류되고, 미지의 신호와 셀렉터 실패 모두 미매핑 폴백으로 안전하게 떨어진다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 실패 사유 · 마스킹된 식별자를 렌더러까지 배선</name>
  <files>src/main/services/auth-service.ts, src/shared/types.ts</files>
  <read_first>
    - src/main/services/auth-service.ts (435-500 — 로그인 결과 분기 전체: 토큰 획득 / 캡차 / 타임아웃 / 폼 오류 / 예외)
    - src/main/services/auth-service.ts (사다리 스파이크 호출 지점과 그 catch 블록 — 로그인 성공 후 비동기로 도는 경로)
    - src/shared/mask.ts (전체 — `maskSensitive()` 가 무엇을 잡고 무엇을 놓치는지)
    - src/shared/types.ts (AuthEvent union + `CredentialLoginResult` — 06-04 가 `reason` 을 추가한 상태)
    - src/shared/login-failure.ts (Task 1 결과 + 06-02 의 매핑 함수)
    - .planning/phases/06-ui/06-RESEARCH.md (§Common Pitfalls 2 — 렌더러 반환 필드는 로그 자동 마스킹 경로를 타지 않는다)
  </read_first>
  <behavior>
    - 캡차 신호 → `credentialLogin()` 이 `{ success: false, reason: "captcha", message: <UI-SPEC 캡차 문구> }` 를 반환한다
    - 타임아웃 → `{ success: false, reason: "timeout", message: <UI-SPEC 타임아웃 문구> }`
    - 폼 오류 → `{ success: false, reason: "form-error", message: <전처리된 폼 텍스트> }`
    - 예외 발생 → `{ success: false, reason: "network-error", message: <UI-SPEC 네트워크 문구>, identifier: <마스킹된 값> }`
    - 캡차·타임아웃 사유의 반환 객체에는 `identifier` 속성이 아예 없다 (undefined 문자열이 화면에 찍히는 렌더링이 불가능)
    - 반환되는 `message`/`identifier` 에 토큰 형태 문자열을 심어도 마스킹된 형태로만 나온다
    - 사다리 실패 시 `login-failed` 이벤트의 message 가 사다리 실패 안내 문구이고 마스킹을 통과한다
    - `AuthEvent` union 에 더 이상 OTP 요구 이벤트 타입이 없다
  </behavior>
  <action>
    (1) `credentialLogin()` 의 실패 분기 5개(캡차 / 타임아웃 / 폼 오류 / 미매핑 / 예외)를 전부
    Task 1 의 분류 함수 + 06-02 의 `mapLoginFailure()` 결과로 통일한다. 각 분기는
    `{ success: false, reason, message }` 를 반환하고, 문구를 분기 안에서 직접 쓰지 않는다.
    `_emit({ type: "login-failed", ... })` 의 message 도 같은 매핑 결과를 쓴다.

    (2) **마스킹 관문(R010).** `mapLoginFailure()` 가 돌려준 `identifier` 와 `message` 는 렌더러로
    직접 반환되는 값이라 로그 자동 마스킹 경로를 타지 않는다. 따라서 `CredentialLoginResult` 에
    싣기 직전에 `maskSensitive()` 를 명시적으로 적용한다 — 예외 메시지·스택에 URL 쿼리스트링이
    섞여 들어오는 경우가 실제 위험이다(Phase 05 T-05-17 이 같은 계열의 결함이었다).
    식별자는 문장에 이어붙이지 말고 `CredentialLoginResult` 의 별도 선택 필드
    `identifier?: string` 로 싣는다(`shared/types.ts` 에 추가) — 렌더러가 이것을 작은 칩으로
    따로 렌더링해야 하기 때문이다(UI-SPEC "Identifier format"). 식별자가 없는 사유에서는 필드를
    아예 넣지 않는다(UI-SPEC E5 partial). 잘리지 않은 원문(`logDetail`)은 `logService` 로 보낸다 —
    그쪽은 자동 마스킹이 적용된다.
    단 사다리 실패처럼 **비동기 이벤트**로만 전달되는 경로는 구조화된 필드를 실을 자리가 없으므로
    문장 뒤에 `(식별자: <마스킹된 값>)` 형태로 병기한다. 두 경로의 차이를 SUMMARY 에 기록할 것.

    (3) **사다리 실패 안내.** 로그인 성공 후 비동기로 도는 사다리 스파이크의 catch 블록은 현재
    로그만 남긴다. 여기에 `login-failed` 이벤트 발행을 추가해
    `mapLoginFailure("token-ladder-failed", <오류 요약>)` 문구가 사용자에게 도달하게 한다
    (App.tsx 가 이미 이 이벤트를 loginError 로 받아 LoginPanel 에 내려보낸다 — 새 배선이 필요 없다).
    이벤트 message 역시 마스킹을 통과시킨다.

    (4) `shared/types.ts` 의 `AuthEvent` union 에서 OTP 요구 이벤트 타입을 제거한다 — 발행자가
    사라졌고 렌더러에 소비자가 없다.

    (5) **브라우저 모드 실패 안내의 범위 (CONTEXT 재량 항목 결정).** CONTEXT 는 브라우저 모드
    `login()` 의 실패 안내를 어디까지 손볼지를 구현자 재량으로 남겼다. 이 플랜의 결정은
    **부분 재사용**이다: 두 모드가 공유하는 경로(사다리 실패, 토큰 검증 실패)는 위 (3)에서
    같은 매핑을 쓰므로 브라우저 모드에도 그대로 적용된다. 반면 `login()` 자체의 보이는 창
    흐름은 이번 phase 에서 건드리지 않는다 — 사용자가 실제 브라우저 창에서 오류를 직접 보고
    있어 추가 번역의 이득이 작고, D-03 이 이미 브라우저 모드 동작을 한 번 바꾸는 상황에서
    변경 표면을 더 넓히면 회귀 판정이 어려워진다. 이 결정과 근거를 SUMMARY 에 기록한다.
  </action>
  <verify>
    <automated>npx vitest run src/main/services/__tests__/auth-service.test.ts</automated>
    <automated>npm run typecheck &amp;&amp; npm run typecheck:main</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "maskSensitive" src/main/services/auth-service.ts` 가 1 이상이다
    - `grep -c "mapLoginFailure" src/main/services/auth-service.ts` 가 1 이상이다
    - `grep -c "identifier?: string" src/shared/types.ts` 가 1 이다
    - `grep -vE '^\s*(//|\*|/\*)' src/shared/types.ts | grep -c "otp-required"` 가 0 이다
    - `grep -vE '^\s*(//|\*|/\*)' src/main/services/auth-service.ts | grep -c "이메일 OTP 인증이 필요합니다"` 가 0 이다
    - `sed -n "/runAccountTokenLadderSpike(\"credentialLogin\")/,/});/p" src/main/services/auth-service.ts | grep -c "_emit"` 가 1 이상이다
    - `npm run typecheck` 와 `npm run typecheck:main` 이 각각 0 으로 종료한다
    - 동작: 실패한 로그인 시도 후 로그 패널에는 원문 상세가, 화면에는 마스킹된 안내가 표시된다
  </acceptance_criteria>
  <!-- planner-discipline-allow: otp-required -->
  <!-- planner-discipline-allow: 이메일 OTP 인증이 필요합니다 -->
  <reversibility rating="reversible">분기 내부의 문자열 출처를 매핑 함수로 옮기는 변경이다.</reversibility>
  <done>모든 실패 경로가 하나의 매핑을 통과하고, 렌더러로 나가는 모든 문자열이 마스킹 관문을 지난다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: 실패 안내 회귀 테스트 — 캡차 사유와 마스킹 관문</name>
  <files>src/main/services/__tests__/auth-service.test.ts</files>
  <read_first>
    - src/main/services/__tests__/auth-service.test.ts (전체 — 06-04 Task 3 이 재작성한 상태. 기존 electron 모킹 방식과 describe 구성)
    - src/main/services/auth-service.ts (Task 1·2 결과)
    - src/shared/__tests__/mask.test.ts (마스킹 단언 작성 관례 — 어떤 입력에 어떤 출력을 기대하는지)
    - .planning/phases/06-ui/06-VALIDATION.md (§Per-Task Verification Map — 이 태스크가 채워야 할 행)
  </read_first>
  <behavior>
    - 캡차 신호로 종료된 로그인 시도의 반환 `reason` 이 `"captcha"` 이고 message 에 브라우저 로그인 안내가 포함된다
    - 같은 반환 message 에 이메일 코드 서사가 포함되지 않는다 (부정 단언)
    - 토큰 형태 문자열을 담은 예외로 로그인이 실패하면 반환 message 에 원문 토큰이 남지 않는다
    - 사다리 실패 시 발행된 `login-failed` 이벤트 message 가 사다리 안내 문구다
    - 타임아웃 신호의 반환 `reason` 이 `"timeout"` 이다
  </behavior>
  <action>
    `auth-service.test.ts` 에 실패 안내 describe 를 추가한다. 이 저장소는 DOM 테스트 환경이 없으므로
    BrowserWindow 를 실제로 열지 않는다 — 기존 파일의 electron 모킹 방식을 그대로 쓰고, DOM 폴링
    결과를 주입할 수 있는 최소 지점(모킹된 `executeJavaScript` 반환값)을 통해 분기를 태운다.
    주입이 지나치게 얽히면 `classifyCredentialLoginSignal()` + `mapLoginFailure()` 조합을 직접
    호출하는 계약 테스트로 대체하되, **마스킹 관문 케이스만은 반드시 `auth-service` 쪽에서**
    검증한다 — 마스킹을 적용하는 책임이 그 파일에 있기 때문이다(06-RESEARCH.md Pitfall 2).

    부정 단언(이메일 코드 서사 부재)은 사유별로 반복하지 말고 실패 사유 전체를 순회하는 한 개의
    테이블 테스트로 작성해, 새 사유가 추가돼도 자동으로 덮이게 한다.

    `06-VALIDATION.md` 의 Per-Task Verification Map 에서 이 플랜에 해당하는 행의 Status 를
    green 으로 갱신한다.
  </action>
  <verify>
    <automated>npx vitest run src/main/services/__tests__/auth-service.test.ts</automated>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/main/services/__tests__/auth-service.test.ts` 가 0 으로 종료한다
    - `npm test` 전체 스위트가 0 으로 종료한다
    - 테스트 파일에 마스킹 회귀 케이스가 존재한다: `grep -c "maskSensitive\|마스킹" src/main/services/__tests__/auth-service.test.ts` 가 1 이상이다
    - 테스트 파일에 캡차 사유 케이스가 존재한다: `grep -c "captcha" src/main/services/__tests__/auth-service.test.ts` 가 1 이상이다
    - `06-VALIDATION.md` 의 해당 행 Status 가 `⬜ pending` 이 아니다
  </acceptance_criteria>
  <done>캡차 오진과 마스킹 누락 두 결함이 회귀 테스트로 잠겼다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (플랜 06-05 몫)

**신규 심볼**
- `classifyCredentialLoginSignal()` (`src/shared/login-failure.ts`)
- `CredentialLoginResult.identifier` 필드 (`src/shared/types.ts` — 마스킹을 통과한 식별자, 렌더러의 칩 렌더링용)

**신규 DOM 폴링 신호 값** (`auth-service.ts` 의 `executeJavaScript` 반환 어휘)
- `'captcha'` (reCAPTCHA 위젯 감지 — 기존 OTP 신호에서 분리)
- `'otp-form'` (인증코드 입력창 감지 — 미매핑 폴백으로 라우팅)

**제거되는 심볼**
- `AuthEvent` union 의 `"otp-required"` 타입
- DOM 폴링의 `'otp'` 반환값
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Weverse 로그인 페이지 DOM → 앱 | 셀렉터도 텍스트도 Weverse 배포에 따라 언제든 바뀐다. 신호 부재를 정상 경로로 다뤄야 한다 |
| main 예외/에러 문자열 → 렌더러 화면 | 스택·URL·응답 본문이 섞일 수 있는 경로. 로그 자동 마스킹이 닿지 않는다 |
| 사다리 비동기 실패 → 사용자 | 로그인 성공 직후 조용히 실패할 수 있는 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-17 | Information Disclosure | `CredentialLoginResult.message` / 식별자가 토큰·쿼리스트링을 담은 채 화면에 노출 | high | mitigate | Task 2 에서 렌더러 반환 직전 `maskSensitive()` 명시 적용 + Task 3 의 마스킹 회귀 테스트. Phase 05 T-05-17 과 동일 계열의 결함이므로 반드시 테스트로 잠근다 |
| T-06-18 | Elevation of Privilege | 캡차 우회/자동 해결 코드 유입 | high | mitigate | R013 영구 제외. 캡차는 감지→안내까지만. 숨은 창 노출 복구 흐름도 범위 밖(prohibitions) |
| T-06-19 | Denial of Service (사용자 시간) | 캡차를 OTP 로 오독해 사용자가 오지 않을 메일을 기다림 | high | mitigate | Task 1 의 신호 분리 + Task 3 의 전 사유 부정 단언 테이블 테스트 |
| T-06-20 | Repudiation | 사다리 실패가 로그에만 남고 사용자에게 도달하지 않음 | medium | mitigate | Task 2 에서 `login-failed` 이벤트 발행 추가 |
| T-06-21 | Tampering (신뢰성) | 해시 기반 CSS 모듈 셀렉터가 깨져 실패를 오분류 | medium | mitigate | 셀렉터 실패는 예외가 아니라 `null` → 미매핑 폴백으로 설계 (Task 1). 테스트에 `null` 케이스 포함 |
| T-06-SC | Tampering | npm/pip/cargo 설치 | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — `06-RESEARCH.md` § Package Legitimacy Audit 신규 의존성 0건, `[ASSUMED]`/`[SUS]` 없음 |

블로킹 임계값: high. T-06-17 / T-06-18 / T-06-19 는 이 플랜 안에서 코드 + 테스트로 해소된다.
</threat_model>

<verification>
- `npm test` 전체 통과 + `npm run typecheck` + `npm run typecheck:main` 통과
- 실패 사유 전 항목에 대해 반증된 이메일 코드 서사가 등장하지 않음을 테이블 테스트가 단언한다
- 마스킹 회귀 테스트가 `auth-service` 레벨에 존재한다
</verification>

<success_criteria>
- 캡차 실패가 캡차 안내로 표시된다 (D-13, R020)
- 셀렉터 파손·미지 신호·OTP 폼 출현 모두 미매핑 폴백으로 안전하게 안내된다 (D-14)
- 렌더러로 나가는 모든 실패 문자열이 마스킹을 통과한다 (R010)
- 사다리 실패가 사용자에게 도달한다 (D-12)
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-05-SUMMARY.md` when done.
`<planner_assumptions>` 의 R020/concurrency 가정을 그대로 옮겨 적고, 확정된 DOM 신호 어휘
(`'captcha'` / `'otp-form'` / `'error:…'` / `'timeout'` / `null`)를 명시할 것.
</output>
