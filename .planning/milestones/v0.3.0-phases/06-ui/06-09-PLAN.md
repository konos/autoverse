---
phase: 06-ui
plan: 09
type: execute
wave: 1
depends_on: []
gap_closure: true
files_modified:
  - src/shared/token-validation-failure.ts
  - src/shared/__tests__/token-validation-failure.test.ts
  - src/main/services/auth-service.ts
  - src/main/services/__tests__/auth-service.test.ts
  - .planning/phases/06-ui/06-05-SUMMARY.md
autonomous: true
requirements: [R020]

estimate:
  tokens: 54000
  raw_tokens: 54000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "렌더러로 돌아가는 실패 문구와 식별자는 마스킹을 통과한 값뿐이다 — 토큰·비밀번호·URL 쿼리스트링이 화면에 노출되지 않는다 (06-05-PLAN must_haves, R010)"
    - "validateToken() 의 네 개 실패 경로가 서버 응답 원문을 사용자 화면으로 전달하지 않는다 — 사용자는 상태 코드 기반의 확정 한국어 안내와 짧은 식별자만 본다 (D-12/D-14)"
    - "서버 응답 원문은 사라지지 않고 logService 진단 경로에 그대로 남아 개발자가 추적할 수 있다 — 조용히 무시하지 않는다는 이 저장소의 관측성 원칙을 지킨다"
    - "두 로그인 모드가 공유하는 실패 경로에도 '렌더러로 나가는 문구는 전부 마스킹 관문을 통과한다'는 전제가 실제로 성립한다 — 06-05 가 세운 전제가 코드와 일치한다"
    - "06-05-SUMMARY.md 의 잘못된 완료 선언이 원문 보존 상태로 정정되어, 다음 phase 가 그것을 근거로 재검증을 건너뛸 수 없다 (D-11 [VOID] + 정정문 관례)"
  prohibitions:
    - "LoginFailureReason union 에 값을 추가하지 않는다 — ROADMAP SC3 정정본과 D-12 가 확정한 6가지 신호 계약을 건드리지 않는다"
    - "서버 응답 본문을 사용자 화면 문구에 어떤 길이로도 끼워 넣지 않는다"
    - "logService 로 가는 진단 로그를 줄이거나 삭제하지 않는다 — 사용자 노출을 줄이는 것이지 관측성을 줄이는 것이 아니다"
    - "06-05-SUMMARY.md 의 반증된 원문 서술을 삭제하거나 고쳐 쓰지 않는다 — 삭제 대신 [VOID] 마킹 + 정정문 병기 (D-11)"
    - "credentialLogin() 의 실패 경로와 buildFailureResult() 를 이 플랜에서 수정하지 않는다 — SC3 는 손상되지 않았고 변경 표면을 넓히면 회귀 판정이 어려워진다"
    - "새 npm 의존성을 추가하지 않는다"
    - "실계정 로그인이나 Weverse 서버 접속으로 검증하지 않는다 — 모든 검증은 스텁 fetch 기반 단위 테스트로 닫는다"
  artifacts:
    - src/shared/token-validation-failure.ts
    - src/shared/__tests__/token-validation-failure.test.ts
  key_links:
    - "`validateToken()` 의 네 실패 지점 ↔ `describeTokenValidationFailure()` ↔ `maskSensitive()` ↔ `_emit()` — 렌더러로 나가는 유일한 경로이며 이 배선이 끊기면 원문이 다시 샌다"
    - "`_emit()` → `auth-event` IPC → `ipc-handlers.forwardAuthEvent` → `App.tsx` 의 `loginError` — main 의 이벤트 message 가 그대로 화면 문구가 되는 지점"
    - "`06-05-SUMMARY.md` 의 완료 선언 ↔ 다음 phase 의 재검증 판단 — 잘못된 선언이 검증 생략의 근거가 되는 경로"
---

<objective>
06-VERIFICATION.md gap 2(CR-02)를 닫는다: 두 로그인 모드가 공유하는 `validateToken()` 의 네 개
실패 emit 지점이 서버 응답 원문을 마스킹 관문을 우회해 렌더러 화면으로 흘려보내는 정보 노출을
제거하고, 이 경로가 이미 커버된다고 잘못 선언한 06-05-SUMMARY.md 를 D-11 관례로 정정한다.

Purpose: 이 phase 는 `buildFailureResult()` 를 "렌더러로 나가는 모든 실패 문구의 유일한 마스킹
관문"으로 세웠다(R010, D-14). 그 전제가 두 모드가 공유하는 경로 하나에서 성립하지 않는다.
전제가 거짓인 채로 남으면 마스킹 규칙을 아무리 보강해도 이 경로는 계속 새며, 더 나쁘게는
06-05-SUMMARY.md 의 잘못된 완료 선언이 다음 phase 의 재검증 생략 근거가 된다.

Output: 상태 코드 기반 확정 한국어 안내를 만드는 순수 모듈, 네 emit 지점의 재배선, 원문이
화면에 도달하지 않음을 증명하는 회귀 테스트, 그리고 정정된 06-05-SUMMARY.md.
</objective>

## Artifacts this phase produces

| Artifact | Kind | Note |
|---|---|---|
| `src/shared/token-validation-failure.ts` | 신규 모듈 | 순수 함수. `login-failure.ts` 와 같은 tier·같은 관용구(exhaustive switch)이지만 **다른 union** 을 쓴다 |
| `TokenValidationFailureKind` | 신규 export (type) | `"unauthorized" \| "http-error" \| "parse-error" \| "missing-fan-id"` |
| `TokenValidationGuidance` | 신규 export (interface) | `{ message: string; identifier?: string }` |
| `describeTokenValidationFailure(kind, context?)` | 신규 export (function) | `context` 는 `{ status?: number }` 만 받는다 — 서버 응답 텍스트를 받는 파라미터가 존재하지 않는다 |
| `src/shared/__tests__/token-validation-failure.test.ts` | 신규 테스트 파일 | 4개 kind 전수 + 부정 단언 |
| `AuthService.emitTokenValidationFailure()` | 신규 private 메서드 | `validateToken()` 의 네 실패 지점이 공유하는 단일 emit 관문 |
| `06-05-SUMMARY.md` 정정 블록 | 문서 변경 | D-11 `[VOID]` + 정정문. 원문 2곳 보존 |

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
@.planning/phases/06-ui/06-UI-SPEC.md
@.planning/phases/06-ui/06-VERIFICATION.md
@.planning/phases/06-ui/06-REVIEW.md
@.planning/phases/06-ui/06-05-SUMMARY.md
@.planning/phases/06-ui/06-PATTERNS.md
</context>

<gap_context>
**출처:** `06-VERIFICATION.md` frontmatter `gaps[1]` (status: partial) — 06-REVIEW.md CR-02 와
동일 결함을 검증자가 독립적으로 재확인한 것.

**결함의 정확한 형태:** `src/main/services/auth-service.ts` 의 `validateToken()` 안에 있는 네 개
`_emit()` 호출(대략 955-960 / 964-967 / 974-978 / 983-988행 — 401 세션 복원 실패, `!res.ok`,
JSON 파싱 실패, `fanId` 없음)이 서버 응답 원문 최대 200자를 `AuthEvent.message` 에 그대로 담는다.
`buildFailureResult()` 와 `mapLoginFailure()` 를 **전혀 거치지 않는다.** `_emit()` 은 logService
(자동 마스킹이 걸린 쪽)를 거치지 않고 `auth-event` → `ipc-handlers.forwardAuthEvent` →
`webContents.send("auth:event", ...)` 로 렌더러에 직접 전달되며, `App.tsx` 의 `onAuthEvent`
리스너가 그 `message` 를 그대로 `loginError` 로 화면에 렌더링한다.

**이 코드는 phase 06 이 만든 결함이 아니다.** 검증자가 `git log -S"rawBody.slice(0, 200)"` 로
확인한 결과 커밋 `816970a`, **2026-05-14** — Phase 06(2026-08-26)보다 훨씬 이전부터 있던 경로다.
플랜과 SUMMARY 는 이 사실을 그대로 서술해야 하며 phase 06 이 회귀를 일으켰다고 암시해서는 안
된다. phase 06 의 책임은 다른 데 있다: **"buildFailureResult() 가 유일한 마스킹 관문" 이라는
전제를 세우면서 그 전제가 성립하지 않는 공유 경로를 그대로 남겨뒀고**, 06-05-SUMMARY.md 가 그
경로를 "자동으로 혜택을 받는다" 고 **명시적으로, 그리고 틀리게** 기록했다.

**SC3 는 손상되지 않았다.** `credentialLogin()` 의 6가지 실패 신호 매핑은
`buildFailureResult()` → `maskSensitive()` 를 정확히 거친다(06-VERIFICATION.md truth 3 = VERIFIED).
이 플랜의 범위는 `validateToken()` 하나로 한정한다.

**검증자가 제시한 두 선택지와 이 플랜의 판단:**

| 선택지 | 판단 |
|---|---|
| (A) 네 지점을 `maskSensitive(...)` 로 감싼다 (최소 조치) | **기각.** `maskSensitive()` 는 `SENSITIVE_PATTERNS` 전부가 `accessToken=` 같은 **키 이름이 값 바로 앞에 붙은 경우만** 매칭한다(06-REVIEW WR-02). 서버 응답 본문은 임의 구조의 문자열이고, 문맥 없는 토큰 형태 문자열은 이 규칙을 그대로 통과한다. 즉 (A)는 노출을 **줄이는 것처럼 보이지만 보장하지 않는다** — 이 phase 가 이미 한 번 당한 "검증되지 않은 완전성 주장" 을 형태만 바꿔 반복하는 셈이다 |
| (B) D-12 매핑 테이블처럼 상태 코드 기반 고정 안내 문구로 대체한다 | **채택.** 서버 응답 텍스트가 화면 경로에 **아예 진입하지 않으므로** 마스킹 규칙의 완전성에 의존하지 않는다 — 패턴 매칭이 아니라 구조로 막는다. D-12/D-14 가 이 phase 에 세운 "사용자는 원문 대신 이해할 수 있는 한국어 설명을 본다" 는 방향과도 일치한다. 비용이 (A)보다 크다는 지적은 맞지만, 신규 순수 모듈 1개 + 네 지점 재배선 규모이며 진단 정보는 이미 같은 함수 안의 `logService.info` 라인이 응답 본문 500자를 남기고 있어 **잃는 것이 없다** |

D-14 의 "일반 안내 + 식별자 병기" 요구는 (B) 안에서 만족한다: 식별자는 서버 원문이 아니라
우리가 만든 짧은 값(HTTP 상태 코드 등)이다. 그럼에도 최종 문장은 `maskSensitive()` 를 통과시킨다
— 그래야 "렌더러로 나가는 문구는 전부 마스킹 관문을 통과한다" 는 전제가 이 경로에서도 **실제로
참**이 되고, 06-05 가 세웠다가 깨진 바로 그 전제가 복구된다.
</gap_context>

<review_disposition>
06-REVIEW.md Warning/Info 중 이 플랜이 판정한 항목:

| ID | 판정 | 근거 |
|---|---|---|
| WR-04 (`settings:set-login-mode` 가 `mode` 를 런타임 검증하지 않음) | **DEFER — 후속 항목으로 등록** | 두 gap 어느 쪽과도 뿌리를 공유하지 않는다(계약 불일치도, 마스킹 관문 우회도 아닌 입력 검증 문제다). 영향도 낮다 — `readSettings()` 가 읽기 시점에 정규화하므로 디스크 파일이 영구히 오염되지 않고, 현재 렌더러 경로에서는 TypeScript union 이 값을 보장한다. 리뷰 자신도 "향후 리팩터링 실수" 라는 가정적 진입점을 근거로 든다. ASVS L1 · block on high 기준에서 severity low 이며 블로킹 임계값 아래다. gap closure 를 미니 phase 로 부풀리지 않기 위해 명시적으로 미룬다 — Phase 07 이 `settings.json` 위에 설정 항목을 더 쌓을 때(D-05 가 예고한 시점) 함께 처리하는 것이 자연스럽다 |
| IN-01 (`readSettings()` 오류 로그가 원인과 무관하게 항상 파싱 실패로 기록) | **DEFER — 후속 항목으로 등록** | 로그 문구의 정확성 문제이며 사용자 노출도 데이터 무결성도 건드리지 않는다. `String(err)` 가 병기돼 실제 원인 추적은 이미 가능하다. 두 gap 과 뿌리를 공유하지 않고, 고치려면 `settings-store.ts` 의 읽기 경로를 재구성해야 해 gap closure 의 변경 표면을 넓힌다. WR-04 와 같은 파일이므로 함께 처리하는 것이 효율적이다 |

WR-01 / IN-03 / IN-04 는 `06-08-PLAN.md` 가 판정했다.
WR-02 / WR-03 / IN-02 는 `06-10-PLAN.md` 가 판정한다.
</review_disposition>

<tracer_rationale>
TRACER_MODE 는 켜져 있으나 **이 플랜에서는 선행 트레이서를 만들지 않는다.** 근거를 남긴다:
gap 2 는 이미 존재하는 함수 안의 네 지점을 재배선하는 국소 수정이고, 관통해야 할 미증명 레이어가
없다 — `_emit()` → `auth-event` → `forwardAuthEvent` → 렌더러 경로는 이 phase 이전부터 동작 중이며
06-VERIFICATION.md 도 그 경로 자체는 `✓ WIRED` 로 확인했다. 여기서 얇은 종단 슬라이스를 하나 더
만드는 것은 Task 2 의 회귀 테스트와 정확히 같은 것을 두 번 하는 셈이고, 정보를 늘리지 않는
형식적 `type="tracer"` 태스크가 된다. 대신 Task 1 을 TDD 로 시작해 문구 계약을 먼저 고정하고,
Task 2 가 "토큰 형태 원문이 화면 경로에 도달하지 않는다" 를 종단(스텁 fetch → emit 이벤트)으로
단언한다. 06-08 은 사정이 달라 실제 트레이서를 둔다.
</tracer_rationale>

<!-- planner-discipline-allow: LoginFailureReason -->
<!-- 근거: 부정 그렙의 대상은 신규 파일 `token-validation-failure.ts` 안에 이 타입 이름이
     등장하지 않아야 한다는 것이다. 액션이 이 이름을 언급하는 것은 `login-failure.ts` 의
     기존 union 을 건드리지 말라는 금지 지시이며, 신규 파일에 이 문자열을 쓰라는 지시가
     아니다. -->

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 토큰 검증 실패 → 확정 한국어 안내 순수 모듈을 신설한다</name>
  <files>src/shared/token-validation-failure.ts, src/shared/__tests__/token-validation-failure.test.ts</files>
  <read_first>
    - src/shared/login-failure.ts (전체 — exhaustive switch 관용구, `LoginFailureGuidance` 의 필드 구성, 한국어 확정 문구의 어조. 이 모듈은 그 형태를 따르되 **다른 union** 을 쓴다)
    - src/shared/__tests__/login-failure.test.ts (테스트 구성·`it.each` 전수 검증 관례)
    - src/main/services/auth-service.ts (866-1000행 — `validateToken()` 전체. 네 실패 분기가 각각 어떤 조건에서 발생하고 어떤 이벤트 타입을 내는지 확인할 것)
    - .planning/phases/06-ui/06-CONTEXT.md (D-12 매핑 테이블의 형태와 D-14 의 "일반 안내 + 식별자 병기" 요구)
    - .planning/phases/06-ui/06-UI-SPEC.md (§Copywriting Contract — 확정 한국어 문구의 어조·길이 기준)
    - .planning/phases/06-ui/06-PATTERNS.md (§login-failure.ts 행 — 순수 모듈이 `src/shared/` 에 놓이는 근거)
  </read_first>
  <behavior>
    - Test 1: 네 kind 각각에 대해 `message` 가 비어 있지 않은 한국어 문장이며 서로 다르다.
    - Test 2: `"http-error"` 에 `{ status: 503 }` 을 주면 `identifier` 에 그 상태 코드가 담긴다.
      `status` 를 주지 않으면 `identifier` 는 상태 코드를 지어내지 않는다.
    - Test 3 (전수 부정 단언): 네 kind 전부에 대해 반환된 `message` 와 `identifier` 어디에도
      반증된 이메일 인증코드 서사가 들어가지 않는다 — `login-failure.test.ts` 가 6개 사유에 대해
      쓰는 부정 정규식과 같은 계열의 단언을 쓴다.
    - Test 4 (계약 단언): 이 함수의 두 번째 인자 타입에는 서버 응답 텍스트를 담을 수 있는 필드가
      없다. 런타임 단언이 어려우므로 **타입 수준 단언**(의도적으로 잘못된 프로퍼티를 넘기는
      `@ts-expect-error` 라인)으로 고정한다 — `npm run typecheck` 가 이 계약의 가드가 된다.
    - Test 5: `identifier` 가 없는 kind 의 반환 객체에는 그 프로퍼티 자체가 존재하지 않는다
      (`"identifier" in result === false`) — `login-panel-view.ts` 의 `buildFailureView()` 가 세운
      "정의되지 않은 값이 화면에 찍히지 않는다" 관례와 같은 규율.
  </behavior>
  <action>
    `src/shared/token-validation-failure.ts` 를 신규 작성한다.

    - `export type TokenValidationFailureKind` — 네 값: `"unauthorized"`(401 이후 세션 복원까지
      실패), `"http-error"`(2xx 아님), `"parse-error"`(응답 JSON 파싱 실패), `"missing-fan-id"`
      (200 이지만 fanId 부재).
    - `export interface TokenValidationGuidance` — `message: string` 과 선택적 `identifier?: string`.
    - `export function describeTokenValidationFailure(kind: TokenValidationFailureKind, context?: { status?: number }): TokenValidationGuidance`
      — `login-failure.ts` 의 `mapLoginFailure()` 와 동일하게 `default` 없는 exhaustive switch 로
      작성한다(union 에 값이 추가되면 컴파일 타임에 누락이 잡힌다).

    문구 방향(정확한 문장은 구현자 재량 — D-08 § Claude's Discretion 의 "문장 다듬기는 구현자
    재량" 이 여기에도 적용된다). 네 문장 모두 한국어이고, 사용자가 **다음에 할 행동**을 담으며,
    서버 응답 텍스트도 영문 HTTP 이유구도 담지 않는다:
    - `unauthorized` — 세션이 만료됐으니 다시 로그인해야 한다는 안내.
    - `http-error` — 서버가 요청을 처리하지 못했고 잠시 후 재시도하거나 로그 패널에서 자세한
      내용을 볼 수 있다는 안내. `identifier` 는 `context.status` 가 주어졌을 때만, HTTP 상태
      코드를 담은 짧은 문자열로 만든다.
    - `parse-error` — 서버 응답을 이해하지 못했으니 로그 패널을 보라는 안내. `identifier` 는
      응답 형식 문제임을 알리는 짧은 고정 문자열.
    - `missing-fan-id` — 계정 정보를 확인하지 못했으니 다시 로그인하라는 안내. `identifier` 는
      어떤 값이 없었는지 알리는 짧은 고정 문자열.

    `identifier` 는 **전부 이 모듈이 스스로 만든 값**이다 — 서버가 준 문자열이 identifier 로
    흘러들 수 있는 경로를 두지 않는다. 그래서 두 번째 인자 타입에 텍스트 필드를 두지 않는다.
    파일 머리 주석에 이 설계 의도(마스킹 규칙의 완전성에 의존하지 않고 구조로 막는다)와 이것이
    06-VERIFICATION.md gap 2 의 처치라는 사실을 남긴다.

    이어서 `src/shared/__tests__/token-validation-failure.test.ts` 에 `<behavior>` Test 1~5 를 쓴다.
    네 kind 전수 배열을 `login-failure.test.ts` 의 `ALL_LOGIN_FAILURE_REASONS` 와 같은 방식으로
    상수로 두고 `it.each` 로 돌린다.

    `LoginFailureReason` union 은 건드리지 않는다 — ROADMAP SC3 정정본과 D-12 가 확정한 6가지
    신호 계약이며 `auth-service.test.ts` 의 전수 테이블 테스트가 그 개수를 잠그고 있다.
  </action>
  <verify>
    <automated>npx vitest run src/shared/__tests__/token-validation-failure.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run typecheck:main</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/shared/__tests__/token-validation-failure.test.ts` 가 0 종료 코드로 끝난다.
    - `grep -c 'export function describeTokenValidationFailure' src/shared/token-validation-failure.ts` == 1
    - `grep -vE '^\s*(//|\*|/\*)' src/shared/token-validation-failure.ts | grep -c 'default:'` == 0
      — exhaustive switch 에 default 절이 없다.
    - `grep -c 'LoginFailureReason' src/shared/token-validation-failure.ts` == 0 — 기존 6가지 신호
      union 을 참조하지도 확장하지도 않는다.
    - `git diff HEAD~1 -- src/shared/login-failure.ts` 가 공백이다 — 6가지 신호 계약 무변경.
    - `npm run typecheck` 와 `npm run typecheck:main` 이 각각 0 에러다 (Test 4 의 `@ts-expect-error`
      라인이 실제로 에러를 잡고 있다는 뜻이기도 하다 — 잡지 못하면 unused-expect 로 타입체크가 깨진다).
    - `git diff --stat HEAD~1 -- package.json package-lock.json` 이 공백이다.
  </acceptance_criteria>
  <reversibility rating="reversible">신규 순수 모듈 + 신규 테스트. 아직 어디에서도 import 되지 않는다.</reversibility>
  <done>토큰 검증 실패 네 종류의 사용자 대면 문구가 서버 응답과 무관한 확정 값으로 고정됐고, 서버 텍스트가 이 함수로 들어올 타입 경로가 없다.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: validateToken() 의 네 실패 emit 을 단일 관문으로 재배선하고, 응답 원문이 화면 경로에 도달하지 않음을 회귀 테스트로 잠근다</name>
  <files>src/main/services/auth-service.ts, src/main/services/__tests__/auth-service.test.ts</files>
  <read_first>
    - src/main/services/auth-service.ts (866-1000행 `validateToken()` 전체, 440-520행 `buildFailureResult()` 와 `buildLadderFailureEvent()` — 식별자를 문장 뒤에 병기하는 기존 관용구가 여기 있다, 1038-1042행 `_emit()`)
    - src/shared/token-validation-failure.ts (Task 1 이 만든 시그니처)
    - src/shared/mask.ts (전체 — `maskSensitive()` 의 규칙이 전부 키-값 문맥에 의존한다는 사실. 이것이 원문 감싸기 대신 고정 문구를 택한 근거다)
    - src/main/services/__tests__/auth-service.test.ts (318-340행 `makeFetchQueue` — 스텁 응답 헬퍼의 기존 형태. **`json()` 만 제공하므로 `text()` 를 제공하는 헬퍼가 새로 필요하다**. 1-40행의 electron 모크와 `cookieFixtureBox` — 401 경로가 타는 세션 복원이 이 모크 위에서 결정적으로 실패한다. 551-625행 `buildFailureResult` describe — 마스킹 회귀 테스트의 기존 작성 스타일)
    - src/main/ipc-handlers.ts (`forwardAuthEvent` — `_emit` 된 이벤트가 렌더러로 나가는 실제 경로 확인용)
    - .planning/phases/06-ui/06-VERIFICATION.md (gaps[1] — 네 지점의 행 번호와 각 분기의 조건)
  </read_first>
  <behavior>
    새 describe 블록 하나로 묶는다. 모든 케이스가 공통으로 쓰는 픽스처: 서버 응답 본문 안에
    **키 이름 접두사 없이** 놓인 150자 이상의 토큰 형태 문자열(문맥 없는 원문 — `maskSensitive()`
    의 키-값 규칙이 잡지 못하는 바로 그 형태).
    - Test 1 (`!res.ok`): 스텁 fetch 가 503 과 그 본문을 돌려주면 emit 된 `auth-event` 의 `message`
      에 토큰 형태 문자열이 **포함되지 않는다**. 대신 확정 안내 문구가 들어 있고, 이벤트 타입은
      `login-failed` 다.
    - Test 2 (JSON 파싱 실패): 스텁 fetch 가 200 과 JSON 이 아닌 본문을 돌려주면 같은 부정 단언이
      성립하고 이벤트 타입은 `login-failed` 다.
    - Test 3 (`fanId` 없음): 스텁 fetch 가 200 과 `fanId` 가 없는 유효 JSON 을 돌려주면 같은 부정
      단언이 성립하고 이벤트 타입은 `login-failed` 다.
    - Test 4 (401 + 세션 복원 실패): 스텁 fetch 가 401 과 그 본문을 돌려주고 쿠키 픽스처가 비어
      세션 복원이 실패하면, 같은 부정 단언이 성립하고 이벤트 타입은 `token-expired` 다.
    - Test 5 (진단 보존): 위 네 케이스 중 하나에서 `logService` 쪽 진단 경로에는 응답 본문이 여전히
      남는다 — 사용자 노출을 줄이는 것이지 관측성을 줄이는 것이 아니라는 것을 단언으로 고정한다.
      (`logService` 를 spy 하거나 이미 존재하는 진단 라인을 소스 수준으로 확인하는 방식 중
      기존 테스트 파일의 관례에 맞는 쪽을 고른다.)
  </behavior>
  <action>
    <!-- planner-discipline-allow: rawBody.slice(0, 200) -->

    `AuthService` 에 private 메서드 `emitTokenValidationFailure(kind, eventType, context?)` 를
    추가한다. 이 메서드가 하는 일:
    - `describeTokenValidationFailure(kind, context)` 로 안내와 식별자를 얻는다.
    - 식별자가 있으면 `buildLadderFailureEvent()` 가 이미 쓰는 것과 **동일한 병기 관용구**로 문장
      뒤에 붙인다(비동기 이벤트 경로에는 구조화된 필드를 실을 자리가 없다는 같은 이유다 —
      새 관용구를 만들지 말고 그 형태를 따를 것).
    - 최종 문자열을 `maskSensitive()` 에 통과시킨 뒤 `_emit()` 한다. 이 값은 우리가 만든 확정
      문구뿐이라 마스킹이 실제로 바꿀 것은 없지만, "렌더러로 나가는 문구는 전부 관문을 통과한다"
      는 전제를 이 경로에서도 참으로 만들기 위해 넣는다 — 06-05 가 세웠다가 깨진 바로 그 전제다.
      이 의도를 메서드 JSDoc 에 남긴다.

    `validateToken()` 의 네 실패 지점을 전부 이 메서드 호출로 교체한다:
    - 401 이후 세션 복원 실패 → kind 는 unauthorized, 이벤트 타입은 기존 그대로 `token-expired`.
    - `!res.ok` → kind 는 http-error, `context.status` 에 `res.status` 를 넘긴다. 이벤트 타입은
      기존 그대로 `login-failed`. 이 분기가 지금 별도로 만들던 `msg` 지역 변수도 함께 정리하되,
      같은 내용의 `logService.error` 진단은 **유지**한다(사용자 문구와 진단 문구를 분리한다).
    - JSON 파싱 실패 → kind 는 parse-error, 이벤트 타입 `login-failed`.
    - `fanId` 없음 → kind 는 missing-fan-id, 이벤트 타입 `login-failed`.

    네 지점 어디에서도 응답 본문 문자열을 사용자 문구에 끼워 넣지 않는다 —
    `rawBody.slice(0, 200)` 표현은 이 함수에서 완전히 사라진다. 반면 응답 본문 500자를 남기는
    기존 `logService.info` 진단 라인과 `JSON.parse` 입력으로서의 사용은 **그대로 둔다**.

    `validateToken()` 의 나머지(로컬 만료 검사, 타임아웃/네트워크 에러 분기, 성공 경로)는 건드리지
    않는다. 네트워크 에러 분기(929행 부근)는 이미 서버 응답이 아니라 JS 예외 메시지를 담고 있어
    이 gap 의 대상이 아니다 — 범위를 넓히지 않는다.

    `src/main/services/__tests__/auth-service.test.ts` 에 `<behavior>` 의 describe 를 추가한다.
    `makeFetchQueue` 는 `json()` 만 제공하므로 `text()` 와 `statusText` 를 제공하는 스텁 헬퍼를
    새로 만든다(기존 헬퍼는 다른 describe 가 쓰고 있으므로 수정하지 말고 나란히 둔다).
    `cachedToken` 은 private 이므로 이 파일이 이미 쓰는 캐스팅 관용구(`asFailureBuilders` 와 같은
    형태의 좁은 인터페이스 캐스트)로 주입한다. 캐시 토큰은 JWT 로 파싱되지 않는 긴 문자열이면
    되고(그러면 `isTokenExpired()` 가 파싱 실패 시 false 를 돌려줘 네트워크 분기까지 진행한다),
    이벤트는 `service.on("auth-event", ...)` 로 수집한다. 기존 테스트는 한 줄도 수정하지 않는다.
  </action>
  <verify>
    <automated>npx vitest run src/main/services/__tests__/auth-service.test.ts</automated>
    <automated>awk '/async validateToken\(/,/^  \}$/' src/main/services/auth-service.ts | grep -c 'this.emitTokenValidationFailure('</automated>
    <automated>grep -c 'rawBody.slice(0, 200)' src/main/services/auth-service.ts</automated>
    <automated>npm test</automated>
    <automated>npm run typecheck:main</automated>
  </verify>
  <acceptance_criteria>
    - `awk '/async validateToken\(/,/^  \}$/' src/main/services/auth-service.ts | grep -c 'this.emitTokenValidationFailure('` == 4
    - `grep -c 'rawBody.slice(0, 200)' src/main/services/auth-service.ts` == 0
    - `grep -c 'rawBody.slice(0, 500)' src/main/services/auth-service.ts` == 1 — 진단 라인은 살아 있다.
    - `awk '/async validateToken\(/,/^  \}$/' src/main/services/auth-service.ts | grep -c 'this._emit('` == 2
      — 남는 직접 emit 은 네트워크/타임아웃 분기와 성공(`token-validated`) 분기 둘뿐이다.
    - `grep -c 'describeTokenValidationFailure' src/main/services/auth-service.ts` >= 1
    - `npx vitest run src/main/services/__tests__/auth-service.test.ts` 가 0 종료 코드로 끝나고
      새 describe 의 5개 테스트가 모두 통과한다.
    - `git diff HEAD~1 -- src/main/services/__tests__/auth-service.test.ts` 에 **삭제된 줄이 없다**
      (`git diff --numstat` 의 deleted 열이 0) — 기존 테스트 무수정.
    - `npm test` 가 0 실패로 끝나고 총 테스트 수가 315보다 크다.
    - `npm run typecheck:main` 과 `npm run typecheck` 가 각각 0 에러다.
    - `git diff --stat HEAD~1 -- package.json package-lock.json` 이 공백이다.
  </acceptance_criteria>
  <reversibility rating="reversible">기존 함수 안의 네 지점 재배선. 응답 본문은 logService 진단 경로에 그대로 남아 있어 되돌려도 잃는 정보가 없다.</reversibility>
  <done>두 로그인 모드가 공유하는 토큰 검증 실패 경로에서 서버 응답 원문이 사용자 화면에 도달하지 않으며, 문맥 없는 토큰 형태 문자열로 그것을 증명하는 회귀 테스트가 스위트에 들어갔다.</done>
</task>

<task type="auto">
  <name>Task 3: 06-05-SUMMARY.md 의 잘못된 완료 선언을 D-11 관례로 정정한다 (원문 보존)</name>
  <files>.planning/phases/06-ui/06-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/06-ui/06-05-SUMMARY.md (전체 — 특히 frontmatter `key-decisions` 의 세 번째 항목과 본문 "3. 브라우저 모드(login())의 실패 안내는 건드리지 않았다" 절. **두 곳 모두**가 정정 대상이다)
    - .planning/phases/06-ui/06-03-SUMMARY.md (D-11 관례를 실제로 적용한 정본 사례 — [VOID] 마킹 + 정정문 병기가 어떤 형태로 쓰였는지 그대로 따를 것)
    - .planning/REQUIREMENTS.md (§R020 / §R021 — 같은 관례가 적용된 결과물)
    - .planning/phases/06-ui/06-VERIFICATION.md (gaps[1] 의 `reason` — 정정문이 인용할 근거)
    - .planning/phases/06-ui/06-CONTEXT.md (D-11 — 삭제하지 않는 이유가 서술된 지점)
  </read_first>
  <action>
    06-05-SUMMARY.md 의 **두 곳**을 정정한다. 어느 쪽에서도 원문 서술을 삭제하거나 고쳐 쓰지
    않는다 — D-11 이 확립한 방식대로 원문을 그대로 둔 채 `[VOID]` 마킹과 정정문을 병기한다.
    (근거: 틀린 전제가 코드보다 넓게 전파된다는 것을 05-01 이 실증했고, 왜 틀렸는지를 남겨야
    재발을 막는다.)

    (1) frontmatter `key-decisions` 의 세 번째 항목 — 공유 경로가 같은 매핑/emit 경로를 타므로
    자동으로 혜택을 받는다고 서술한 그 항목. 항목 문자열 **앞머리**에 VOID 마킹을 붙이고,
    문자열 **끝**에 정정문을 이어 붙인다. 정정문이 담을 것: 토큰 검증 실패 경로는 실제로는
    `buildFailureResult()`/`mapLoginFailure()` 를 거치지 않았다는 사실, 그 근거가
    06-VERIFICATION.md gap 2(CR-02)라는 것, 그리고 06-09 가 이 경로를 상태 코드 기반 확정 문구로
    대체해 해소했다는 것. **YAML 을 깨뜨리지 않을 것** — 이 항목은 큰따옴표로 감싼 단일 스칼라이며,
    문자열 안에 큰따옴표를 넣지 않고 줄바꿈도 넣지 않는다.

    (2) 본문의 세 번째 항목 절 — 같은 주장을 문장으로 풀어 쓴 곳. 그 문단 바로 앞이나 뒤에
    `> **[VOID — ...]**` 형태의 인용 블록으로 정정문을 병기한다. 원 문단은 손대지 않는다.
    정정문에 반드시 담을 사실 세 가지:
    - 이 경로가 `mapLoginFailure()`/`buildFailureResult()` 를 거치지 않아 서버 응답 원문이 마스킹
      없이 렌더러로 나갔다는 것.
    - 이 코드는 **phase 06 이전(2026-05-14, 커밋 `816970a`)부터 있던 경로**이며 06-05 가 도입한
      결함이 아니라는 것 — 06-05 의 실제 잘못은 검증 없이 "자동으로 혜택을 받는다" 고 선언한 것이다.
    - 06-09 가 어떻게 해소했는지(상태 코드 기반 확정 문구로 대체 + 회귀 테스트)와 정정 시점.

    (3) SUMMARY 문서의 다른 어떤 부분도 수정하지 않는다 — `coverage` 항목의 판정, 커밋 해시,
    시간 기록은 실행 기록이므로 사후 편집 대상이 아니다.
  </action>
  <verify>
    <automated>node ~/.claude/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/phases/06-ui/06-05-SUMMARY.md --schema summary</automated>
    <automated>grep -c 'VOID' .planning/phases/06-ui/06-05-SUMMARY.md</automated>
    <automated>grep -c '혜택' .planning/phases/06-ui/06-05-SUMMARY.md</automated>
    <automated>grep -c '816970a' .planning/phases/06-ui/06-05-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `node ~/.claude/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/phases/06-ui/06-05-SUMMARY.md --schema summary` 의 출력 JSON 에서 `"valid": true` 다 — frontmatter YAML 이 깨지지 않았다.
    - `grep -c 'VOID' .planning/phases/06-ui/06-05-SUMMARY.md` >= 2 — 두 곳 모두 마킹됐다.
    - `grep -c '혜택' .planning/phases/06-ui/06-05-SUMMARY.md` >= 2 — 원문 서술 두 곳이 **삭제되지 않고 보존**돼 있다.
    - `grep -c '816970a' .planning/phases/06-ui/06-05-SUMMARY.md` >= 1 — 이 코드가 phase 06 이전부터 존재했다는 근거 커밋이 명시됐다.
    - `grep -c 'CR-02' .planning/phases/06-ui/06-05-SUMMARY.md` >= 1 — 정정 근거 문서가 인용됐다.
    - `grep -c '06-09' .planning/phases/06-ui/06-05-SUMMARY.md` >= 1 — 해소한 플랜이 명시됐다.
    - `git diff --numstat HEAD~1 -- .planning/phases/06-ui/06-05-SUMMARY.md` 의 삭제 줄 수가 2 이하다 — frontmatter 스칼라 1줄 교체 외에 원문이 지워지지 않았다.
    - `git diff HEAD~1 --stat -- src/` 가 공백이다 — 이 태스크는 소스를 건드리지 않는다.
  </acceptance_criteria>
  <reversibility rating="reversible">문서 병기 변경이며 원문이 보존된다.</reversibility>
  <done>06-05-SUMMARY.md 를 읽는 다음 phase 가 "이 경로는 이미 커버됐다" 는 근거로 재검증을 건너뛸 수 없다. 왜 틀렸는지와 누가 언제 무엇으로 해소했는지가 원문과 나란히 남았다.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Weverse 서버 응답 본문 → 앱 | 완전히 통제 밖의 임의 문자열. 길이·구조·내용 어느 것도 가정할 수 없다 |
| main 프로세스 `AuthEvent.message` → 렌더러 화면 | `_emit()` 은 logService 자동 마스킹을 거치지 않는다. 여기 실린 문자열이 그대로 사용자 화면 문구가 된다 |
| 실행 기록 문서(SUMMARY) → 다음 phase 의 검증 판단 | 잘못된 완료 선언이 재검증 생략의 근거가 되는 경로 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-34 | Information Disclosure | `validateToken()` 의 네 실패 emit 이 서버 응답 원문 200자를 마스킹 없이 렌더러 화면에 노출 (CR-02, R010 위반) | high | mitigate | Task 2 가 네 지점을 상태 코드 기반 확정 문구로 대체해 서버 텍스트가 화면 경로에 **진입하지 못하게** 한다. Task 1 이 그 문구 함수의 인자 타입에서 텍스트 필드를 제거해 구조로 봉인한다 |
| T-06-35 | Information Disclosure | 마스킹 규칙의 키-값 문맥 의존(WR-02) 때문에 원문을 `maskSensitive()` 로 감싸는 최소 조치가 실제로는 보호를 보장하지 못함 | high | mitigate | 이 플랜은 감싸기(A안)를 명시적으로 기각하고 구조적 제거(B안)를 택했다. Task 2 의 회귀 테스트가 **키 접두사 없는** 토큰 형태 문자열을 픽스처로 써 A안이라면 통과했을 케이스를 잡는다 |
| T-06-36 | Repudiation | 06-05-SUMMARY.md 의 거짓 완료 선언이 다음 phase 의 재검증 생략 근거가 됨 | medium | mitigate | Task 3 이 D-11 관례로 두 곳 모두 정정하고, 원문을 보존해 왜 틀렸는지가 남는다. grep 기반 acceptance criteria 로 두 곳 모두 마킹됐음을 강제 |
| T-06-37 | Denial of Service (진단 가치) | 고정 문구로 대체하면서 진단 정보까지 사라져 사용자 문의를 추적할 수 없게 됨 | low | mitigate | 응답 본문 500자를 남기는 기존 `logService` 진단 라인을 유지하고, Task 2 의 acceptance criterion 이 그 라인의 생존을 grep 으로 강제한다. 사용자에게는 D-14 대로 짧은 식별자를 병기한다 |
| T-06-38 | Tampering | 6가지 실패 신호 union 에 값이 추가되어 ROADMAP SC3 정정본 계약이 흔들림 | medium | mitigate | 새 union 을 별도 모듈에 두고 `LoginFailureReason` 을 참조조차 하지 않는다. Task 1 의 acceptance criteria 가 `login-failure.ts` 무변경과 참조 0건을 강제 |
| T-06-SC | Tampering | npm/pip/cargo 설치 | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 — `06-RESEARCH.md` § Package Legitimacy Audit 신규 의존성 0건, `[ASSUMED]`/`[SUS]` 없음. 세 태스크 모두 `package.json`/`package-lock.json` 무변경을 acceptance criterion 으로 강제한다 |

블로킹 임계값: high (ASVS L1). T-06-34 / T-06-35 / T-06-SC 는 이 플랜 안에서 코드 + 테스트로 해소된다.
</threat_model>

<verification>
- `npx vitest run src/shared/__tests__/token-validation-failure.test.ts` — 전수 통과
- `npx vitest run src/main/services/__tests__/auth-service.test.ts` — 기존 + 신규 5개 전부 통과
- `npm test` — 전체 스위트 0 실패, 총 테스트 수 > 315
- `npm run typecheck` / `npm run typecheck:main` — 각 0 에러
- `npm run build` — 0 종료
- `grep -c 'rawBody.slice(0, 200)' src/main/services/auth-service.ts` — 0
- `awk '/async validateToken\(/,/^  \}$/' src/main/services/auth-service.ts | grep -c 'this.emitTokenValidationFailure('` — 4
- `node ~/.claude/gsd-core/bin/gsd-tools.cjs query frontmatter.validate .planning/phases/06-ui/06-05-SUMMARY.md --schema summary` — `"valid": true`
- `git diff --stat 06-09-시작-커밋 -- package.json package-lock.json` — 공백

**실행자가 하지 말아야 할 것:** 실계정 로그인, Weverse 서버(`weverse.io` / `accountapi.weverse.io`)
로의 어떤 요청도. 이 플랜의 모든 검증은 스텁 fetch 기반 단위 테스트로 닫힌다(05-CONTEXT D-06).
</verification>

<success_criteria>
1. `validateToken()` 의 네 실패 경로가 서버 응답 원문을 사용자 화면 문구에 담지 않는다 —
   문맥 없는 토큰 형태 문자열 픽스처로 자동 테스트가 증명한다.
2. 사용자는 상태 코드 기반의 확정 한국어 안내와 짧은 식별자를 본다 (D-12/D-14).
3. 서버 응답 본문은 logService 진단 경로에 그대로 남는다 — 관측성이 줄지 않았다.
4. 6가지 실패 신호 계약(`LoginFailureReason`)과 `credentialLogin()` 경로가 무변경이다.
5. 06-05-SUMMARY.md 의 두 잘못된 서술이 원문 보존 상태로 `[VOID]` 마킹 + 정정문 병기됐고,
   frontmatter YAML 이 여전히 유효하다.
6. 신규 의존성 0건, 전체 스위트 green, 타입체크 2종 0 에러.
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-09-SUMMARY.md` when done.

SUMMARY 에 반드시 남길 것:
- 검증자가 제시한 두 선택지 중 어느 쪽을 왜 택했는지 — 특히 `maskSensitive()` 감싸기를 기각한
  근거(키-값 문맥 의존, WR-02)를 회귀 테스트 픽스처와 함께 서술할 것.
- 이 코드가 **phase 06 이전부터 존재했다**는 사실(커밋 `816970a`, 2026-05-14)을 명시하고,
  phase 06 의 실제 책임은 전제를 세우고 검증하지 않은 것임을 구분해 적을 것.
- Task 3 의 정정이 어느 두 곳에 어떤 형태로 들어갔는지.
- WR-04 / IN-01 을 왜 미뤘는지(이 플랜의 `<review_disposition>` 을 그대로 옮길 것).
</output>
