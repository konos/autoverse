---
phase: "03"
plan: "04"
type: "implementation"
---

<objective>
전체 빌드 검증 + 기존 테스트 수정 + 최종 확인
</objective>

<context>
Phase: 03 (s03) — Milestone: M001-ksbtje

T01~T03에서 console.* → logService.log() 교체로 인해 기존 테스트(auth-service.test.ts, profile-store.test.ts 등)에서 console.log spy/assertion이 깨질 수 있다. 기존 테스트를 수정하여 logService 모킹으로 전환하고, 전체 tsc/test/build를 통과시킨다.
</context>

<must_haves>

- npm test 전체 통과 (기존 57개 + T01 신규 테스트)
- npx tsc --noEmit (renderer+shared) 통과
- npx tsc -p tsconfig.main.json --noEmit 통과
- npm run build 성공
- grep console.log/error/warn src/main/services/ (테스트 제외) 결과 0개

</must_haves>
