---
phase: "03"
plan: "02"
type: "implementation"
---

<objective>
기존 서비스 console.* 호출을 LogService로 교체 + IPC 배선
</objective>

<context>
Phase: 03 (s03) — Milestone: M001-ksbtje

auth-service.ts, apply-engine.ts, timing-service.ts, weverse-api.ts, profile-store.ts의 모든 console.log/error/warn 호출을 logService.log()로 교체한다. ipc-handlers.ts에 LogService 이벤트 → renderer 포워딩(log:entry 채널) + log:download 핸들러(dialog.showSaveDialog → fs.copyFile)를 추가한다. preload.ts에 onLogEntry, downloadLog API를 노출하고 IpcApi 타입을 확장한다.
</context>

<must_haves>

- 5개 서비스 파일에 console.log/error/warn 호출이 0개
- log:entry IPC 채널로 LogEntry가 renderer에 전달
- log:download가 SaveDialog → 파일 복사 수행
- IpcApi 타입에 log 네임스페이스 포함

</must_haves>
