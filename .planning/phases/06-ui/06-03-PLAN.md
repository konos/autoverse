---
phase: 06-ui
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [R020, R021]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "R021 과 Phase 06 성공 기준 2를 읽는 사람은 '매 로그인마다 이메일 OTP 필요' 가 반증된 서술임을 문서 안에서 즉시 알 수 있다 (D-11)"
    - "R020 의 에러코드 목록이 D-02 로 삭제되는 순수 HTTP 경로에서만 발생한다는 사실과, 실제 매핑 대상이 무엇인지가 문서에 남는다 (D-12)"
    - "반증된 원문이 삭제되지 않고 그대로 남아 있다 — 왜 틀렸는지가 학습 자산으로 보존된다 (Phase 05 가 세운 원칙)"
    - "verify 단계가 D-03 의 SC1 편차(브라우저 모드 자격증명 자동 로그인 제거)를 회귀가 아니라 의도된 변경으로 판정할 수 있도록 ROADMAP 에 편차가 명시돼 있다"
    - "모든 정정문이 근거 파일 경로를 함께 제시해 독자가 1차 증거로 되돌아갈 수 있다"
  prohibitions:
    - "반증된 서술을 삭제하지 않는다 — VOID 마킹 + 정정문 병기로만 처리한다"
    - "이 플랜에서 소스 코드 파일을 수정하지 않는다"
    - "ROADMAP.md 를 통째로 다시 쓰지 않는다 — 대상 구간만 국소 치환한다"
  artifacts:
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - "REQUIREMENTS.md R021 Description ↔ ROADMAP Phase 06 성공 기준 2 — 같은 반증된 문구가 두 곳에 있어 한쪽만 고치면 불일치가 남는다"
    - "정정문 ↔ `.planning/phases/05-api/05-01-SUMMARY.md` — 모든 정정의 1차 근거"
---

<objective>
2026-08-25 HAR 실측으로 반증된 서술을 요구사항·로드맵에서 정정한다. 삭제가 아니라
`[VOID]` 마킹 + 정정문 병기로 처리해 "왜 틀렸는지"를 기록으로 남긴다 (D-11).

Purpose: 이 프로젝트는 이미 틀린 전제가 코드보다 넓게 전파되는 것을 한 번 겪었다. 코드에서
OTP 경로를 지우면서 문서를 그대로 두면 다음 사람이 같은 전제를 다시 주워 든다.
Output: 정정된 `REQUIREMENTS.md` R020/R021, 정정된 `ROADMAP.md` Phase 06 성공 기준.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ui/06-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: REQUIREMENTS.md R021 · R020 정정 (VOID 마킹 + 정정문)</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (§R016 / §R017 / §R018 / §R020 / §R021 + Traceability 표 — R018 항목이 이 저장소가 채택한 VOID/보류 표기 포맷의 정본 예시다)
    - .planning/phases/05-api/05-01-SUMMARY.md (§무효화된 전제 — 모든 정정의 1차 근거. 특히 otpSessionId 필드의 실제 정체)
    - .planning/phases/06-ui/06-CONTEXT.md (D-08 / D-11 / D-12 — 무엇을 어떻게 정정할지의 결정)
  </read_first>
  <action>
    `REQUIREMENTS.md` 의 두 항목을 국소 편집한다. **원문을 지우지 않는다.**

    (1) **R021 — API 모드 제약 사전 고지.** 제목 줄에 `[정정 — 2026-08-26]` 표기를 붙인다
    (R018 이 `[보류 — 2026-08-25]` 로 쓴 것과 같은 형태). Description 의 "매 로그인마다 이메일
    OTP 입력 필요" 문장을 `**[VOID — 2026-08-25 HAR 반증]**` 접두로 감싸 원문을 남기고, 바로
    아래에 정정문을 병기한다. 정정문의 내용은 D-08 이 확정한 실제 제약 2가지다:
    ① Weverse 보안 확인(reCAPTCHA)이 뜨면 이 방식으로는 로그인이 실패할 수 있고 그때는 브라우저
    방식을 써야 한다, ② 자동 재로그인이 없어 앱 재시작·토큰 만료 시 직접 로그인해야 한다.
    Notes 에는 근거 경로 `.planning/phases/05-api/05-01-SUMMARY.md` 와 "HAR 436 entries 에서 OTP
    관련 호출 0건" 이라는 관측 사실을 남긴다.

    (2) **R020 — API 로그인 실패 사유 한국어 안내.** Notes 의 에러코드 목록
    (`-25003` / `-25044` / `-26000` / `-26004` / `RESTRICTED_OVERSEAS_LOGIN` / `PASSWORD_RESET_REQUIRED`)
    을 삭제하지 말고 `**[VOID — 도달 불가]**` 로 마킹한다. 이 코드들은 D-02 가 제거하는 순수 HTTP
    경로에서만 발생하므로 구현해도 도달할 수 없다. 그 아래에 D-12 가 재정의한 실제 매핑 대상
    6가지 사유(캡차 / 폼 오류 / 타임아웃 / 네트워크 오류 / 토큰 사다리 실패 / 미매핑)를 정정문으로
    병기하고, 구현 위치가 `src/shared/login-failure.ts` 임을 명시한다. R010 마스킹 규칙 유지
    문장은 그대로 살린다.

    (3) **Traceability 표.** R020 / R021 행의 Proof 열을 `unmapped` 에서
    `2026-08-26 Phase 06 D-11 정정 — 반증 서술 VOID 마킹, 실제 매핑 대상으로 재정의` 로 갱신한다.
    Status 는 `active` 를 유지한다(이 phase 가 구현을 완료하면 검증은 06-07 이 판정한다).

    Coverage Summary 의 수치는 상태 전이가 없으므로 변경하지 않는다.
  </action>
  <verify>
    <automated>grep -c "VOID" .planning/REQUIREMENTS.md</automated>
    <automated>grep -c "05-01-SUMMARY.md" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "VOID" .planning/REQUIREMENTS.md` 가 2 이상이다 (R020·R021 각각)
    - `grep -c "login-failure.ts" .planning/REQUIREMENTS.md` 가 1 이상이다
    - `grep -c "\-25044" .planning/REQUIREMENTS.md` 가 여전히 1 이상이다 (원문 보존 — 삭제하지 않았다는 증거)
    - `grep -c "reCAPTCHA\|보안 확인" .planning/REQUIREMENTS.md` 가 1 이상이다
    - R021 섹션 안에 정정문과 원문이 모두 존재한다 (`sed -n '/^### R021/,/^### /p' .planning/REQUIREMENTS.md` 출력에 VOID 표기와 정정 서술이 함께 보인다)
    - Traceability 표의 R020·R021 행 Proof 열이 더 이상 `unmapped` 가 아니다
  </acceptance_criteria>
  <reversibility rating="reversible">문서 편집이며 git 이력에 원문이 남는다. 계약이 또 바뀌면 같은 방식으로 한 층 더 정정하면 된다.</reversibility>
  <done>R020·R021 을 읽는 사람이 반증된 서술과 정정된 사실을 한 화면에서 구분할 수 있다.</done>
</task>

<task type="auto">
  <name>Task 2: ROADMAP.md Phase 06 성공 기준 정정 + SC1 의도적 편차 명시</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (Phase 05 섹션 — `**[VOID — 2026-08-25 HAR 반증]**` 표기와 경고 블록이 이 파일에서 이미 쓰인 정본 포맷이다. Phase 06 섹션 — 편집 대상)
    - .planning/phases/06-ui/06-CONTEXT.md (D-03 의 "⚠ 로드맵 SC1과의 의도적 편차" 문단, D-08, D-11)
    - .planning/phases/05-api/05-01-SUMMARY.md (근거)
  </read_first>
  <action>
    `.planning/ROADMAP.md` 의 **Phase 06 섹션만** 국소 치환한다. 다른 phase 항목이나 진행 표를
    건드리지 말 것.

    (1) **성공 기준 2** — 현재 문구 `"매 로그인마다 이메일 OTP 필요, 자동 재로그인 불가"라는 안내를
    확인해야만 진행할 수 있다 를 Phase 05 섹션이 쓴 것과 동일한 `**[VOID — 2026-08-25 HAR 반증]**`
    접두로 마킹하고, 바로 아래에 정정된 기준을 새 항목으로 병기한다. 정정된 기준의 내용:
    "사용자가 API 모드를 처음 선택하면 ① Weverse 보안 확인 시 로그인이 실패할 수 있고 브라우저
    방식을 써야 한다는 점 ② 자동 재로그인이 없다는 점 — 두 가지 고지를 확인해야만 진행할 수 있다."
    "확인해야만 진행" 이라는 차단 요건 자체는 유지된다(D-09).

    (2) **성공 기준 1** — 문구 자체는 유지하되 항목 아래에 편차 주석을 덧붙인다:
    D-03 에 따라 브라우저 모드의 저장된 자격증명 기반 무인 자동 로그인도 함께 제거되므로
    "기존 브라우저 로그인 동작은 변경 없이" 는 **의도적으로 위반**된다는 것, 그 근거가 05-01 에서
    실제로 발생한 다른 계정 알림 메일 발송 사고와 캡차 챌린지의 예측 불가능성이라는 것, 그리고
    verify 단계는 이를 회귀가 아니라 의도된 변경으로 판정해야 한다는 것. 근거 경로로
    `.planning/phases/06-ui/06-CONTEXT.md` (D-03) 를 함께 적는다.

    (3) **성공 기준 3** — `-25003`/`-25044`/`-26000`/`-26004`/해외 로그인 차단 나열은 원문을 남긴 채
    `**[VOID — 도달 불가]**` 로 마킹하고, D-12 가 재정의한 실제 실패 사유 기준을 병기한다.

    Phase 06 의 `**Plans**` 줄과 플랜 목록은 이미 플래너가 채워두었으므로 건드리지 않는다.
  </action>
  <verify>
    <automated>grep -c "VOID" .planning/ROADMAP.md</automated>
    <automated>grep -c "06-CONTEXT.md" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^### Phase 06/,/^### Phase 07/p' .planning/ROADMAP.md | grep -c "VOID"` 가 2 이상이다
    - `sed -n '/^### Phase 06/,/^### Phase 07/p' .planning/ROADMAP.md | grep -c "의도적 편차\|의도된 변경"` 가 1 이상이다
    - `sed -n '/^### Phase 06/,/^### Phase 07/p' .planning/ROADMAP.md | grep -c "06-CONTEXT.md"` 가 1 이상이다
    - `grep -c "### Phase 05\|### Phase 06\|### Phase 07" .planning/ROADMAP.md` 가 3 이다 (다른 섹션이 손상되지 않았다는 증거)
    - `sed -n '/^### Phase 06/,/^### Phase 07/p' .planning/ROADMAP.md | grep -c "06-01-PLAN.md"` 가 1 이다 (플랜 목록 보존)
    - Phase 05 섹션의 기존 VOID 마킹이 그대로 남아 있다
  </acceptance_criteria>
  <done>로드맵을 읽는 사람과 verify 에이전트 모두가 어떤 기준이 반증됐고 어떤 편차가 의도된 것인지 문서만으로 판정할 수 있다.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (플랜 06-03 몫)

**신규 심볼:** 없음 — 이 플랜은 소스 코드를 만들지 않는다.

**문서 마커 (신규 도입)**
- `[정정 — 2026-08-26]` — REQUIREMENTS.md 항목 제목 접미 표기
- `**[VOID — 도달 불가]**` — 구현해도 도달할 수 없는 서술의 마킹 (기존 `**[VOID — 2026-08-25 HAR 반증]**` 과 병행)
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 문서 → 후속 구현자/에이전트 | 요구사항·로드맵은 하류 에이전트의 입력이다. 틀린 서술이 남으면 코드로 전파된다 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-06-09 | Tampering (정보 무결성) | 반증된 서술이 문서에 남아 재전파 | medium | mitigate | VOID 마킹 + 정정문 병기 + 근거 경로 명시 (Task 1·2) |
| T-06-10 | Repudiation | 삭제 편집으로 "왜 틀렸는지" 기록이 사라짐 | medium | mitigate | 삭제 금지 prohibition + 원문 보존 검증(`-25044` 잔존 grep) |
| T-06-11 | Tampering | 전체 덮어쓰기로 다른 phase 항목이 소실 | medium | mitigate | 국소 치환만 허용하는 prohibition + Phase 05/07 섹션 잔존 검증 grep |
| T-06-SC | Tampering | npm/pip/cargo 설치 | high | mitigate | 이 플랜은 패키지를 설치하지 않는다 (문서 편집 전용) |

블로킹 임계값: high. 미해소 high 위협 없음.
</threat_model>

<verification>
- `git diff --stat` 결과에 `.planning/REQUIREMENTS.md` 와 `.planning/ROADMAP.md` 만 나타난다 (소스 코드 무변경)
- Phase 05 / Phase 07 섹션의 내용이 diff 에 나타나지 않는다
- `npm test` 는 이 플랜의 영향을 받지 않는다 (사후 확인용)
</verification>

<success_criteria>
- 반증된 서술 3곳(R021 Description, R020 Notes, ROADMAP SC2/SC3)이 원문 보존 + VOID 마킹 + 정정문 병기로 처리됐다 (D-11)
- SC1 의 의도적 편차가 로드맵에 명시돼 verify 단계의 오판을 예방한다 (D-03)
- 모든 정정문이 1차 근거 경로를 동반한다
</success_criteria>

<output>
Create `.planning/phases/06-ui/06-03-SUMMARY.md` when done.
</output>
