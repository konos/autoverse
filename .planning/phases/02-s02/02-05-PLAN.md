---
phase: "02"
plan: "05"
type: "implementation"
---

<objective>
단위 테스트 — 페이로드 빌더, 시간 동기화, 폼 파싱, 안전 가드
</objective>

<context>
Phase: 02 (s02) — Milestone: M001-ksbtje

S02의 순수 로직을 fixture 기반 단위 테스트로 검증한다. 페이로드 빌더는 스펙 §3.3과 deep-equal 비교, 시간 동기화는 가짜 Date 헤더로 오프셋 보정 검증, 폼 파서는 조합별 유효성 검증, 안전 가드는 시간 가드와 재시도 차단 검증.
</context>

<must_haves>

- 페이로드 빌더 테스트가 스펙 §3.3 예시 바디와 deep-equal 비교
- 시간 동기화 테스트가 ±2초 오프셋 + RTT/2 보정을 정확히 검증
- 시간 가드 테스트: startAt-50ms 전 차단, 이후 통과
- 폼 파서 테스트: responseType, applyToken, formOpenAt 유효성 검증
- 전체 테스트가 npm test로 통과

</must_haves>
