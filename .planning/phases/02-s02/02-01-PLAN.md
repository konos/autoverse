---
phase: "02"
plan: "01"
type: "implementation"
---

<objective>
폼 스키마 타입 정의 + Weverse API 클라이언트 + 페이로드 빌더 구현
</objective>

<context>
Phase: 02 (s02) — Milestone: M001-ksbtje

Weverse fan-api/apply-api와 통신하는 API 클라이언트와, 폼 스키마 응답을 파싱하여 POST 페이로드를 조립하는 빌더를 구현한다. 스펙 §3.2 GET application 응답의 전체 타입을 정의하고, §3.3 POST 바디 구조와 deep-equal 비교할 수 있는 빌더를 작성한다.
</context>

<must_haves>

- FormSchema 타입이 스펙 §3.2 응답 구조와 완전히 일치
- buildApplyPayload 출력이 스펙 §3.3 예시와 deep-equal (phone 형식, birthDate 하이픈, consent IDs 포함)
- API 클라이언트의 모든 로그에서 Authorization, applyToken 평문 출력 없음
- applyHost를 하드코딩하지 않고 FormSchema에서 동적으로 추출
- 400 APPLICATION_001(폼 미오픈) 에러를 구조화된 에러 타입으로 변환

</must_haves>
