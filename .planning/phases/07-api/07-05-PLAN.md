---
phase: 07-api
plan: 05
type: execute
wave: 4
depends_on: ["07-03"]
files_modified:
  - src/renderer/auth-event-navigation.ts
  - src/renderer/__tests__/auth-event-navigation.test.ts
  - src/renderer/App.tsx
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [R022, R023]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "대기 화면 경고의 '다시 로그인' 이 API 모드에서는 저장된 자격증명으로, 브라우저 모드에서는 로그인 창으로 각각 실제 재로그인을 시작한다 (D-15, Pitfall 2)"
    - "재로그인이 끝나면 새 토큰의 exp 로 만료가 다시 판정되어 경고가 갱신되거나 사라진다 (D-10)"
    - "신청 대기 중에 인증 이벤트(로그인 성공·실패·토큰 만료·쿠키 추출 실패)가 발생해도 armed 상태와 폼 스키마가 유지되어 사용자가 처음부터 다시 시작하지 않는다 (Pitfall 3)"
    - "사용자가 직접 로그아웃한 경우에만 대기 화면에서 로그인 화면으로 되돌아간다 — 명시적 행위와 사고를 구분한다"
    - "ROADMAP Phase 07 SC1 과 REQUIREMENTS R022/R023 의 반증된 OTP 서술이 삭제되지 않고 [VOID] 마킹 + 정정문 병기로 보존된다 (D-11/06)"
    - "정정문이 D-01 의 의도적 편차(비밀번호 칸은 채워지지 않는다)를 명시해, 검증 단계가 이를 회귀가 아니라 의도된 변경으로 판정할 수 있다"
  artifacts:
    - "src/renderer/auth-event-navigation.ts — AppStep / AuthEventNavigationDecision / decideAuthEventNavigation()"
    - "src/renderer/__tests__/auth-event-navigation.test.ts"
    - "App.tsx 의 대기 중 재로그인 핸들러와 이벤트 네비게이션 배선"
    - ".planning/ROADMAP.md Phase 07 SC1 의 [VOID] + 정정문"
    - ".planning/REQUIREMENTS.md R022 / R023 Why it matters 의 [VOID] + 정정문"
  key_links:
    - "ApplyExecution 의 onRelogin → App.tsx 의 모드 분기 재로그인 → checkTokenExpiry() 재판정 → 배너 갱신 — 이 사슬이 끊기면 경고가 막다른 안내가 된다"
    - "decideAuthEventNavigation() → setStep/setFormSchema 호출 여부 — 이 판정이 없으면 대기 중 재로그인이 armed 상태를 날려 선착순 이벤트를 통째로 놓친다"
    - "ROADMAP SC1 정정문 → verify 단계의 판정 기준 — 정정하지 않으면 존재하지 않는 OTP 단계를 기준으로 이 phase 가 미달 판정된다"
  prohibitions:
    - "반증된 문서 서술을 삭제하지 않는다 — [VOID] 마킹 + 정정문 병기로만 처리한다 (D-11/06)"
    - "대기 중 재로그인 흐름이 armed 상태나 formSchema 를 초기화하지 않는다 (Pitfall 3)"
    - "재로그인 버튼을 위해 새 모달이나 새 화면을 만들지 않는다 — 대기 화면 인라인 경고 안에 둔다 (D-15)"
    - "저장된 자격증명이 없거나 쓸 수 없는 상태에서 재로그인 버튼이 조용히 아무 일도 하지 않게 두지 않는다 — 이유를 사용자에게 알린다"
    - "REQUIREMENTS.md 의 R022/R023 Description·Class·Status 필드를 바꾸지 않는다 — 정정 대상은 Why it matters 서술뿐이다"
---

<objective>
R022 의 마지막 구간을 잇는다: 대기 화면 경고 안의 재로그인이 **모드에 맞는 실제 로그인**을
시작하고, 끝나면 새 토큰으로 **재판정**되며, 그 과정에서 **armed 상태가 살아남는다.** 그리고
Phase 06 이 확립한 D-11 절차대로 이 phase 의 반증된 문서 서술 3곳을 정정한다.

Purpose: `App.tsx` 의 인증 이벤트 핸들러들은 "로그인 실패 = 처음으로 돌아간다" 라는 전제로
작성됐고, "이미 신청을 준비 중인데 인증 이벤트가 발생한다" 는 케이스는 이 phase 가 처음 만드는
상황이다. 이 전제를 고치지 않으면 사용자가 경고를 보고 재로그인하는 순간 armed 상태와 폼
스키마가 사라져 — 선착순 이벤트에서 **경고 자체가 함정이 된다.**

Output: `decideAuthEventNavigation()` 순수 함수와 테스트, `App.tsx` 재로그인·네비게이션 배선,
ROADMAP/REQUIREMENTS 정정. 자동 테스트가 닿지 않는 대기 화면 동작은 `<verify><human-check>` 로
남겨 phase 말미 UAT 에서 한 번에 확인한다(`workflow.human_verify_mode = end-of-phase`).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-api/07-CONTEXT.md
@.planning/phases/07-api/07-01-SUMMARY.md
@.planning/phases/07-api/07-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 07-05-01: 대기 중 재로그인 흐름 + 인증 이벤트가 armed 상태를 날리지 않게 하는 판정 (D-10/D-15, Pitfall 2/3)</name>

  <files>
    src/renderer/auth-event-navigation.ts (신규),
    src/renderer/__tests__/auth-event-navigation.test.ts (신규),
    src/renderer/App.tsx
  </files>

  <read_first>
    - src/renderer/App.tsx — 전체. `type AppStep` 선언, `useEffect` 안의 `onAuthEvent` 핸들러 7갈래(`login-success` / `token-validated` / `login-failed` / `token-expired` / `cookie-extraction-failed` / `logged-out` 처리와 각각의 `setStep`·`setFormSchema` 호출), `handleLogin()`, `handleArmed()`, `<ApplyExecution ... />` 호출부(07-01 이 넘긴 `onRelogin`).
    - src/renderer/login-mode-actions.ts — 부수효과를 컴포넌트 밖 액션 모듈로 분리한 06 의 선례(strict/safe 분리)와 그 JSDoc.
    - src/shared/types.ts — `AuthEvent["type"]` 유니온 전체(7개)와 07-03 이 추가한 `IpcApi.auth.getStoredCredentials` / `credentialLoginStored`, `IpcApi.apply.checkTokenExpiry`.
    - src/renderer/components/ApplyExecution.tsx — 07-01 이 만든 `onRelogin` prop 과 경고 배너.
    - .planning/phases/07-api/07-RESEARCH.md 의 Common Pitfalls 2·3 — 대기 화면에 로그인 폼이 없다는 구조적 사실과 `token-expired`/`logged-out` 핸들러의 파괴적 네비게이션.
  </read_first>

  <behavior>
    `decideAuthEventNavigation(eventType: AuthEvent["type"], step: AppStep): AuthEventNavigationDecision`

    - `step === "apply-execution"` 이고 `eventType` 이 `"logged-out"` → `{ action: "to-login-and-clear-schema" }`
      (사용자가 직접 로그아웃 버튼을 누른 명시적 행위이므로 되돌아가는 것이 정직하다)
    - `step === "apply-execution"` 이고 그 밖의 모든 이벤트 → `{ action: "stay" }`
      (armed 상태와 폼 스키마를 지킨다 — 재로그인 성공도 `profile` 로 끌고 가지 않는다)
    - `step !== "apply-execution"` 일 때는 기존 동작을 그대로 재현한다:
      `"login-success"` / `"token-validated"` → `{ action: "to-profile" }`,
      `"login-failed"` / `"token-expired"` / `"cookie-extraction-failed"` → `{ action: "to-login" }`,
      `"logged-out"` → `{ action: "to-login-and-clear-schema" }`,
      `"credential-login-progress"` → `{ action: "stay" }`
    - 반환 타입은 discriminated union 이고 `switch` 에 default 분기를 두지 않는다

    `App.tsx` 의 대기 중 재로그인 핸들러
    - `loginMode === "browser"` → 기존 `openLogin()` 흐름을 그대로 탄다
    - `loginMode === "api"` → 저장 스냅샷을 조회해 `available` 이면 `credentialLoginStored(email)` 를 호출한다
    - `available` 이 아니면(없음/손상/불가) 아무 일도 없이 끝내지 않고 그 이유를 로그인 배너 문구로 사용자에게 알린다
    - 재로그인 시도가 끝나면(성공·실패 무관) `checkTokenExpiry()` 를 호출해 경고를 재판정한다
    - 재로그인 중에도 `step` 은 `apply-execution` 을 유지한다
  </behavior>

  <action>
`src/renderer/auth-event-navigation.ts` 를 신설한다. `App.tsx` 에 있던 `type AppStep` 선언을 이
파일로 옮겨 export 하고(`App.tsx` 는 여기서 import 한다 — 순환 참조가 생기지 않는 방향이다),
`AuthEventNavigationDecision` 유니온(`{ action: "stay" }` / `{ action: "to-profile" }` /
`{ action: "to-login" }` / `{ action: "to-login-and-clear-schema" }`)과
`decideAuthEventNavigation()` 을 export 한다. 파일 상단 JSDoc 에 이 모듈이 존재하는 이유를
남긴다: `App.tsx` 의 인증 이벤트 핸들러는 "로그인 실패 = 처음으로 돌아간다" 전제로 쓰였고,
신청 대기 중 재로그인이라는 새 상황에서는 그 전제가 armed 상태를 파괴한다. 판단을 컴포넌트 밖
순수 함수로 고정해 두 조건(이벤트 종류 × 현재 단계)의 조합이 테스트로 덮이게 한다 —
`login-panel-view.ts` / `login-mode-actions.ts` 가 세운 관례와 같다.

`App.tsx` 의 `onAuthEvent` 핸들러를 재구성한다. **상태 갱신(`setAuthStatus`, `setLoginError`)은
기존 그대로 유지하고**, 네비게이션(`setStep`, `setFormSchema`)만 `decideAuthEventNavigation()` 의
반환값으로 분기한다 — `"stay"` 면 두 setter 를 호출하지 않고, `"to-profile"` 은 기존처럼 `fanId`
가 있을 때만 이동하며, `"to-login-and-clear-schema"` 는 `setStep("login")` + `setFormSchema(null)`
을 함께 수행한다. 대기 중 `token-expired` 로 `authStatus.isLoggedIn` 이 `false` 가 되면
`LoginPanel` 의 로그인 폼이 다시 나타나는 것은 의도된 결과다 — 사용자에게 재로그인 경로가 하나 더
열린다.

`App.tsx` 에 대기 중 재로그인 핸들러(예: `handleReloginFromWaiting`)를 추가하고
`<ApplyExecution ... onRelogin={...} />` 에 07-01 이 임시로 연결해 둔 `handleLogin` 대신 이
핸들러를 넘긴다. 브라우저 모드는 `window.api.auth.openLogin()`, API 모드는
`window.api.auth.getStoredCredentials()` 로 스냅샷을 확인한 뒤 `available` 일 때만
`window.api.auth.credentialLoginStored(snapshot.email)` 를 호출한다. `available` 이 아니면
`setLoginError()` 로 이유를 표시한다(문구는 스냅샷 상태별로 07-04 가 쓰는 것과 같은 방향으로,
없음/손상/불가를 구분해서). 어느 경로든 마지막에 `window.api.apply.checkTokenExpiry()` 를 호출해
D-10 의 재판정을 트리거한다 — 이 호출은 실패해도 흐름을 멈추지 않게 감싸고, 재판정 결과는
`apply:event` 로 배너에 반영된다. 재로그인 중 중복 클릭을 막기 위해 기존 `loginLoading` 상태를
재사용한다(메인의 in-flight 가드가 최종 방어선이고 이건 UX 보조다).

`src/renderer/__tests__/auth-event-navigation.test.ts` 를 신설해 `AuthEvent["type"]` 7종 ×
`AppStep` 5종의 의미 있는 조합을 덮는다. 최소한 다음을 명시적으로 단언한다: `apply-execution`
에서 `login-success`/`token-validated`/`token-expired`/`login-failed`/`cookie-extraction-failed`
가 전부 `"stay"` 로 판정된다, `apply-execution` 에서 `logged-out` 만
`"to-login-and-clear-schema"` 다, `apply-form`/`profile`/`event-setup`/`login` 단계에서는 기존
동작(`to-profile` / `to-login` / `to-login-and-clear-schema`)이 그대로 재현된다.
  </action>

  <verify>
    <automated>npx vitest run src/renderer/__tests__/auth-event-navigation.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npm test</automated>
    <human-check>앱 실행 명령은 `npm start` 다 (`npm run dev` 는 이 저장소에 존재하지 않는다 — Phase 06 UAT Test 9 에서 발견된 문서 결함). ① 만료가 임박한(또는 `exp` 를 읽을 수 없는) 토큰 상태로 arm 해 대기 화면에 진입하면 경고와 "다시 로그인" 버튼이 보이고, 경고가 떠 있는 동안에도 신청 실행 버튼이 눌린다. ② 대기 화면에서 "다시 로그인" 을 누르면 API 모드에서는 비밀번호 재입력 없이, 브라우저 모드에서는 로그인 창으로 재로그인이 시작된다. ③ 재로그인 성공·실패 어느 쪽이든 대기 화면과 카운트다운이 그대로 유지되고 처음 화면으로 돌아가지 않는다. ④ 재로그인이 성공하면 경고가 사라지거나 갱신된다. ⑤ 저장된 자격증명이 없는 API 모드에서 "다시 로그인" 을 누르면 아무 일도 일어나지 않는 대신 이유가 화면에 표시된다.</human-check>
  </verify>

  <acceptance_criteria>
    - `src/renderer/auth-event-navigation.ts` 가 `export function decideAuthEventNavigation(` 와 `export type AppStep` 을 포함한다
    - `src/renderer/App.tsx` 가 `decideAuthEventNavigation` 을 import 하고, `onAuthEvent` 핸들러 안의 `setStep(` 호출이 이 함수의 반환값 분기 안에서만 일어난다
    - `apply-execution` 단계 + `token-expired` 조합의 판정이 `{ action: "stay" }` 다 (테스트로 단언)
    - `apply-execution` 단계 + `logged-out` 조합의 판정이 `to-login-and-clear-schema` 다 (테스트로 단언)
    - `App.tsx` 가 `credentialLoginStored` 와 `checkTokenExpiry` 를 모두 호출한다
    - `App.tsx` 의 `<ApplyExecution` 호출이 `onRelogin={handleReloginFromWaiting}` 형태로 전용 핸들러를 넘긴다
    - `npx vitest run src/renderer/__tests__/auth-event-navigation.test.ts` 가 exit 0
    - `npm test` 전체가 exit 0, `npm run typecheck` / `npm run typecheck:main` 이 모두 exit 0
  </acceptance_criteria>

  <reversibility rating="reversible">
    신규 순수 모듈 1개 + `App.tsx` 의 분기 재배치 + 핸들러 1개. 기존 이벤트 계약과 IPC 는 그대로다 (CONTEXT D-10/D-15 reversible).
  </reversibility>

  <done>
    대기 화면 경고에서 재로그인한 사용자가 armed 상태를 잃지 않고, 새 토큰으로 만료가 재판정되어
    경고가 갱신되거나 사라진다. 사용자가 직접 로그아웃한 경우에만 로그인 화면으로 되돌아간다.
  </done>
</task>

<task type="auto">
  <name>Task 07-05-02: 반증된 문서 서술 정정 — ROADMAP SC1 · REQUIREMENTS R022/R023 (D-11/06 절차)</name>

  <files>
    .planning/ROADMAP.md,
    .planning/REQUIREMENTS.md
  </files>

  <read_first>
    - .planning/ROADMAP.md — Phase 07 섹션 전체, 그리고 **Phase 05·06 섹션의 기존 [VOID] 마킹 3곳**(성공 기준 항목에 `**[VOID — 사유]**` 를 앞에 붙이고 다음 줄부터 `**정정된 기준 (날짜, 결정번호):**` 를 들여쓰기로 병기하는 형식). 새 정정도 정확히 같은 형식을 쓴다.
    - .planning/REQUIREMENTS.md — R022 / R023 항목 전문과, R017 Notes · R021 Description 의 기존 `**[VOID — 사유]**` + `**정정 — 날짜, 결정번호:**` 형식.
    - .planning/phases/07-api/07-CONTEXT.md 의 `<domain>` 절 — 정정 대상 3곳을 지목한 표와 정정문 원문(정정된 SC1, 정정된 R022 사유).
    - .planning/phases/05-api/05-01-SUMMARY.md — 반증의 근거(HAR 436 entries 중 OTP 관련 호출 0건)를 인용할 정본 경로 확인용.
  </read_first>

  <action>
**ROADMAP.md Phase 07 Success Criteria 1.** 기존 문장을 지우지 않고 앞에
`**[VOID — 2026-08-25 HAR 반증]**` 을 붙인 뒤, 다음 줄부터 들여쓰기로
`**정정된 기준 (2026-08-27, D-01/D-11(06)):**` 을 병기한다. 정정문에는 세 가지가 반드시 들어간다:
① 사용자가 API 모드로 한 번 로그인하면 다음 로그인 시 이메일이 자동으로 채워지고 비밀번호를 다시
입력하지 않고 로그인할 수 있다(safeStorage 암호화 저장 재사용), ② OTP 단계는 실제 로그인 흐름에
존재하지 않으며 Phase 06 D-02 가 관련 코드를 이미 전량 제거했다(근거:
`.planning/phases/05-api/05-01-SUMMARY.md`), ③ **D-01 의 의도적 편차** — 비밀번호 input 칸에는
저장된 값이 채워지지 않는다. 평문 비밀번호는 IPC·렌더러·React state 어디에도 존재하지 않으며
사용자 체감(재입력 없이 로그인)만 충족된다. 검증 단계는 이를 회귀가 아니라 의도된 변경으로
판정해야 한다.

Success Criteria 2 는 손대지 않는다 — 반증된 서술이 없다.

**REQUIREMENTS.md R022 의 `Why it matters`.** 기존 문장을 지우지 않고 앞에
`**[VOID — 2026-08-25 HAR 반증]**` 을 붙인 뒤 `**정정 — 2026-08-27, D-11(06):**` 로 정정문을
병기한다. 정정 내용: 자동 재로그인이 불가능한 실제 이유는 OTP 가 아니라 **reCAPTCHA 관문의 예측
불가능성과 Phase 06 D-03 의 무인 로그인 차단 정책**이다. 또한 Phase 06 D-03 의 부수효과로
**이 요구사항의 적용 범위가 API 모드 한정에서 두 로그인 모드 전체로 확대됐다**는 사실을 함께
기록한다. "대기 중 만료되면 이벤트를 통째로 놓친다 / 사람이 개입할 시간을 미리 확보한다" 는
서술은 그대로 유효하므로 정정 대상이 아니다.

**REQUIREMENTS.md R023 의 `Why it matters`.** 같은 형식으로 `**[VOID — 2026-08-25 HAR 반증]**`
+ `**정정 — 2026-08-27, D-11(06):**` 을 붙인다. 정정 내용: OTP 입력 단계는 존재하지 않으며,
재입력 부담의 실체는 매 로그인마다 이메일과 비밀번호를 다시 타이핑해야 한다는 점이다. R023 의
`Notes` 에 있는 "safeStorage 불가 환경에서는 저장을 건너뛴다(기존 동작 유지)" 는 여전히 유효하며
D-04 가 그대로 따르므로 손대지 않는다.

세 항목 모두에서 `Class` / `Status` / `Description` / `Source` / `Primary owning slice` 필드는
바꾸지 않는다 — 이 태스크의 정정 대상은 반증된 **서술**뿐이다. 마지막으로 `REQUIREMENTS.md`
하단 Traceability 표의 R022 / R023 행 `Proof` 열에 정정 사실을 한 줄로 기록한다(R020/R021 행이
쓴 문구 형식을 그대로 따른다).
  </action>

  <verify>
    <automated>grep -c "VOID" .planning/ROADMAP.md</automated>
    <automated>grep -c "VOID" .planning/REQUIREMENTS.md</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/ROADMAP.md` 의 Phase 07 Success Criteria 1 줄이 `[VOID` 를 포함한다 (`sed -n '/### Phase 07/,/^## Progress/p' .planning/ROADMAP.md | grep -c "\[VOID" 가 1 이상)
    - 같은 구간에 `정정된 기준 (2026-08-27` 문자열이 존재한다
    - 같은 구간의 정정문이 비밀번호 칸이 채워지지 않는다는 D-01 편차를 명시한다 (`grep -c "D-01" .planning/ROADMAP.md` 가 1 이상)
    - `.planning/REQUIREMENTS.md` 의 R022 항목과 R023 항목이 각각 `[VOID` 와 `정정 — 2026-08-27` 을 포함한다
    - `grep -c "VOID" .planning/REQUIREMENTS.md` 가 정정 전보다 2 이상 증가했다
    - R022 / R023 의 `- Class:` / `- Status:` / `- Description:` 줄이 정정 전과 동일하다 (`git diff` 로 확인)
    - Traceability 표의 R022 / R023 행 `Proof` 열이 더 이상 `unmapped` 만 담고 있지 않다
  </acceptance_criteria>

  <reversibility rating="reversible">
    문서 서술만 추가한다 — 원문을 지우지 않으므로 되돌리면 추가된 문단만 제거된다. D-11(06) 이 확립한 절차의 적용이다.
  </reversibility>

  <done>
    반증된 OTP 서술 3곳이 삭제 없이 [VOID] 마킹 + 정정문으로 보존됐고, 검증 단계가 존재하지 않는
    OTP 단계를 기준으로 이 phase 를 오판할 여지가 사라졌다.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| main(auth-event) → renderer 화면 전이 | 인증 이벤트가 화면 상태를 파괴적으로 되돌릴 수 있다. 신청 대기 중에는 그 파괴가 선착순 이벤트 상실로 직결된다. |
| renderer → main (대기 중 재로그인 요청) | 대기 중 사용자 클릭이 실제 외부 로그인 요청을 발생시킨다 — D-03/06 의 "사용자 클릭에서만" 조건을 지키는 경로다. |
| 문서(ROADMAP/REQUIREMENTS) → 검증 판정 | 반증된 서술이 남아 있으면 검증자가 존재하지 않는 기능을 기준으로 이 phase 를 오판한다. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-17 | Denial of Service | `App.tsx` 인증 이벤트 핸들러의 파괴적 네비게이션 | high | mitigate | `decideAuthEventNavigation()` 이 `apply-execution` 단계에서 명시적 로그아웃을 제외한 모든 인증 이벤트를 `stay` 로 판정해 armed 상태와 폼 스키마를 보존한다(Pitfall 3). |
| T-07-18 | Repudiation | 재로그인 버튼이 조용히 실패하는 경로 | medium | mitigate | 저장 스냅샷이 `available` 이 아닐 때 이유를 배너로 표시한다 — 눌렀는데 아무 일도 일어나지 않는 상태를 만들지 않는다. |
| T-07-02 | Tampering | 대기 중 저장 자격증명 로그인 | high | mitigate | 재로그인도 `credentialLoginStored(email)` 을 통해 메인의 이메일 게이트(07-02)를 그대로 통과한다 — 대기 화면이 별도 우회 경로를 만들지 않는다. |
| T-07-19 | Repudiation | 반증된 문서 서술 잔존 | medium | mitigate | D-11(06) 절차대로 [VOID] 마킹 + 정정문 병기. 원문을 삭제하지 않아 학습 자산으로 남기고, 정정문이 검증 기준을 대체한다. |
| T-07-SC | Tampering | npm/pip/cargo installs | high | accept | 이 플랜에는 패키지 설치 태스크가 없다(신규 외부 패키지 0건). 설치가 필요해지면 이 인수는 무효이며 legitimacy 게이트를 먼저 통과해야 한다. |
</threat_model>

<artifacts_produced>
## Artifacts this phase produces

**신규 파일**
- `src/shared/token-expiry.ts`, `src/shared/__tests__/token-expiry.test.ts`
- `src/renderer/components/apply-execution-view.ts`, `src/renderer/components/__tests__/apply-execution-view.test.ts`
- `src/renderer/auth-event-navigation.ts`, `src/renderer/__tests__/auth-event-navigation.test.ts`

**신규 타입/상수/함수**
- `TokenExpiryState`, `RELOGIN_HEADROOM_MS`, `parseJwtExpMs()`, `evaluateTokenExpiry()`
- `TokenExpiryNotice`, `describeTokenExpiryNotice()`
- `maskEmail()` (`src/shared/mask.ts`)
- `StoredCredentialsSnapshot` (`src/shared/types.ts`)
- `AuthService.getStoredCredentialsSnapshot()`, `AuthService.loginWithStoredCredentials()`, `AuthService.restoreTokenIfLost()`(private), `credentialLoginInFlight`(private 필드)
- `ApplyEngine.checkTokenExpiry()`, `ApplyEngine._evaluateCurrentTokenExpiry()`(private), `execute()` 의 `freshToken`
- `StoredLoginState`, `resolveStoredLoginState()` (`src/renderer/components/login-panel-view.ts`)
- `AppStep`(App.tsx 에서 이관), `AuthEventNavigationDecision`, `decideAuthEventNavigation()` (`src/renderer/auth-event-navigation.ts`)
- `App.tsx` 의 `handleReloginFromWaiting()`

**신규 IPC 채널 / preload 브리지**
- `auth:get-stored-credentials` → `window.api.auth.getStoredCredentials()`
- `auth:credential-login-stored` → `window.api.auth.credentialLoginStored(email)`
- `auth:clear-credentials` → `window.api.auth.clearStoredCredentials()`
- `apply:check-token-expiry` → `window.api.apply.checkTokenExpiry()`

**신규 이벤트 타입**
- `ApplyEventType` 멤버 `token-expiry-checked`

**기존 디스크 아티팩트(신규 아님)**
- `credentials.enc`, `settings.json` (`app.getPath("userData")` 하위)
</artifacts_produced>

<verification>
- `npm test` 전체 green, `npm run typecheck` / `npm run typecheck:main` exit 0
- `apply-execution` 단계에서 인증 이벤트가 `setStep` 을 호출하지 않는다(단위 테스트 + 수동 확인)
- ROADMAP Phase 07 SC1 과 REQUIREMENTS R022/R023 에 [VOID] + 정정문이 존재한다
- 대기 화면 관련 수동 확인 항목이 `<verify><human-check>` 로 남아 phase 말미 UAT 로 수집된다
</verification>

<success_criteria>
1. 대기 화면 재로그인이 모드에 맞는 실제 로그인을 시작한다.
2. 재로그인 후 만료가 재판정된다.
3. 대기 중 인증 이벤트가 armed 상태를 파괴하지 않는다.
4. 반증된 문서 서술 3곳이 [VOID] + 정정문으로 정정됐다.
5. 대기 화면 경고·재로그인·armed 유지가 `<human-check>` 항목으로 phase 말미 UAT 에 수집된다.
</success_criteria>

<output>
Create `.planning/phases/07-api/07-05-SUMMARY.md` when done
</output>
